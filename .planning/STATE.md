---
gsd_state_version: '1.0'
status: planning
progress:
  total_phases: 19
  completed_phases: 3
  total_plans: 24
  completed_plans: 24
  percent: 100
  note: >-
    All 24 plans across phases 0-19 (17 skipped) are CODE-COMPLETE. "completed_phases: 3"
    = the 3 fully verified/done (0 fork-setup, 1 Camoufox, 19 unit-normalization). Phases
    2-16 + 18 are code-complete but await the LEAD's human-verify (docker:build + Chrome
    e2e + live deploy) — that gate, not code, is what's outstanding. Upstream v0.19.0-beta
    is available and not yet incorporated (see ROADMAP "Upstream tracking").
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-12)

**Core value:** Reliable recipe import & management for Kiran's groups, incl. bot-protected sources, with no extra setup vs upstream.
**Current focus (2026-06-25):** Branch `claude/overnight-20260622`. Phase 19 (ingredient-unit-normalization, UNIT-NORM-01) is COMPLETE and verified green — the first plan executed end-to-end through the cross-AI worker under native review. All earlier feature phases (2–16, 18) are CODE-COMPLETE on their branches and await the LEAD's human-verify (docker:build + Chrome e2e + live deploy); that gate is the bottleneck, not code. Two open threads surfaced this session: (a) **upstream v0.19.0-beta** is available and not yet incorporated (large overlap with our fork — see ROADMAP "Upstream tracking"); (b) the **gsd agent pack is not installed** (`.claude/agents/` empty → no gsd-planner/checker/verifier gates; the director authors plans + reviews inline). See `## Milestone log` below.

**Historical focus (superseded — kept for context):** Phase 4 — Recipe sharing (SHARE-01 + RATE-01) both code-complete; Phases 2/3/5/6 also code-complete, human-verify pending.

## Session log — 2026-06-25

- **Phase 19 (UNIT-NORM-01)** landed + verified (first real cross-AI worker delegation). Commits dfe3ef17/bee88d06/f4501079 on `claude/overnight-20260622`.
- **gsd agent pack installed** (commit cd0c1fbc) — `.claude/agents/*.md` (gsd-planner/plan-checker/verifier/executor + 29 more), pinned to vendored gsd-core 1.4.4. `agents_installed` is now `true`, so the plan-checker/verifier quality gates can spawn (previously the director ran them inline). Full skill+command surface also surfaced; trim with `gsd:surface` if the cold-start overhead is unwanted.
- **Upstream v0.19.0-beta** assessed + scheduled as **Phase 20** (ROADMAP "Upstream tracking" + vault `norish-upstream-0.19.0-incorporation-assessment`). Large, high-overlap merge — its own integration branch.
- **IN PROGRESS: push-to-live (the deploy ask).** CORRECTED topology (an earlier note in this session wrongly said "14 unmerged branches"): the feature work is ALREADY integrated on `main` as a linear fast-forward stack (the `feat/*` pointers are stale post-merge leftovers). `main` = phases 1–18 (39 migrations incl. 0037/0038 multi-household+sharing, camofox in, `playwright.ts` removed, WorkOS in). `claude/overnight-20260622` = `main` + cross-AI tooling + Phase 19 + the gsd-agent install. Live `norish-app` runs image `norish:live` built **2026-06-14 22:03** — BEHIND main (missing the June-15 phases 14–18 + cross-AI + Phase 19). **Live DB is already at migration 39** (same as main) → a deploy applies **NO new migrations** (low risk; image swap is rollback-able to `norish:live`).

  **LEAD DECISIONS (2026-06-25, locked):** (1) **Straight to live + rollback ready** — `pg_dump` the live DB first, deploy the new image to `norish-app`, keep `norish:live` tagged for instant rollback (NOT the norishp2 verify-stack). (2) **Verify via the `gsd-verifier` agent** (isolated, keeps the main track clean) — NOTE the gsd agents were installed THIS session but only become spawnable in a NEW session; re-run verification there. (3) **Merge `claude/overnight-20260622` → `main`, deploy `main`** (main = single source of truth). (4) **Exclude `feat/17-auth-widget`** (1 commit `242c0695`, untested auth-card UI, no phase dir) from this push.

  **NEXT-SESSION SEQUENCE:** (a) full pre-deploy verification via `gsd-verifier` — typecheck + lint + tests across all packages; db + queue suites need `sg docker -c '…'` (the `claude` user isn't in the docker group in-process; `sg docker` works). Known-green baseline: api ~366 / web ~383 / mobile ~130 / trpc ~256 / shared-server 154 / shared 222 / shared-react 27 (+ config/auth), db+queue green only under `sg docker`. (b) If GREEN: fast-forward `main` to `claude/overnight-20260622`. (c) Build the image: `pnpm docker:build` — DIRECTOR-owned, run detached (`nohup … &` + exit-code sentinel) and POLL; heavy Next.js build needs swap headroom (temp host swapfile + `pct set 110 -swap <MB>`, restore after). NEVER a Monitor/sleep wait-loop in a subagent. (d) `pg_dump` the live DB. (e) Deploy: swap `norish-app` to the new image (keep `norish:live` tagged), restart, health-check `/` + container healthy. (f) Hand the per-phase Chrome e2e UAT gates (multi-household, sharing/ratings, WorkOS, config-as-code toggles) to the human/Claude-in-Chrome. Update STATE/ROADMAP + vault as each step lands.

## Current Position

Phase: 19 of 19 — ingredient-unit-normalization-update-path (COMPLETE, verified 2026-06-25)
Plan: 19-01 COMPLETE — recipe UPDATE path normalizes locale-specific ingredient units to canonical IDs, parity with CREATE (UNIT-NORM-01). Cross-AI worker commit `bee88d06` (byte-identical to the reverted `17cb5659`), native-reviewed: db `ingredient-unit-normalization` suite 8/8 green under `sg docker`, adversarially confirmed, typecheck/lint clean. Commits dfe3ef17 (PLAN) / bee88d06 (fix) / f4501079 (SUMMARY) on `claude/overnight-20260622`. Closes the last code-side red Phase 18 logged as out-of-scope; only environmental red (Docker testcontainers needing `sg docker`) remains.

PREVIOUS Plan: RATE-01 CODE-COMPLETE (recipe ratings: average+count + per-user named-rater list on the AUTHENTICATED detail view, built ON the existing recipe_ratings feature — NO schema change/migration. NEW getRecipeRaters repo join (recipe_ratings INNER JOIN user, decrypted display name like getUsersByIds, null-safe, most-recent-first) + RecipeRaterSchema/RecipeRatersSchema shared zod + a NEW ratings.getRaters authedProcedure that runs `await assertRecipeAccess(ctx, recipeId, "view")` FIRST then getAverageRating+getRecipeRaters in parallel — a user outside the recipe's OWN cookbook gets FORBIDDEN and the rater names are NEVER fetched (HOUSE-06/POLICY-01). A read-only RecipeRaters web component renders the aggregate (★ + average.toFixed(1) + a pluralized count) + the per-user 'rated by <name> ★★★★' list (current viewer labelled 'You', null name -> 'A member') in the EXISTING rating section of BOTH recipe-page-desktop + recipe-page-mobile, under the existing StarRating input; renders nothing while loading or at 0 ratings. createUseRecipeRatersQuery added to the existing createRatingsHooks factory + the web adapter. i18n recipes.detail.{ratingsCount(ICU plural),ratedBy,you,anonymousRater} in all 11 locales (nl+en real, 9 EN-fallback). The pre-existing rate/getUserRating/getAverage procedures stay authed-but-not-access-gated (upstream behavior, out of scope); only the NEW getRaters — the surface exposing member NAMES — gets the gate. PUBLIC-share-view ratings DEFERRED as RATE-02 (privacy: exposing member names on the no-auth /share view; sharedRecipeProcedure + PublicRecipeViewSchema untouched). ADVERSARIAL: `if (false) await assertRecipeAccess(...)` turned EXACTLY the 3 access-control tests RED then reverted byte-identical (never committed). Static verify GREEN: typecheck db/shared/auth/trpc/shared-react/web all EXIT 0 (real tsc on trpc+shared-react has ZERO errors in touched files; the 5 pre-existing shared-server+auth errors are unrelated), i18n:check EXIT 0, lint db/shared/trpc/shared-react/web clean. Tests: trpc recipes+ratings+households 109/109 (raters 5/5 new, ratings 8/8, permissions-integration 22/22), db households.isolation 6/6 (HOUSE-06 intact) + recipe 12/12 + recipe-shares 8/8 (no parse regression). 3 (RATE-01) commits (ee3408a1, 341ddd8a, b900aa84) on feat/phase-2-multi-household; nothing pushed; live containers untouched; no docker/dev build run. SUMMARY written. Human-verify (Chrome: rate a recipe -> see the average + your name in the rater list; a second member's name+stars appear; public /share shows NO ratings) OWNED BY THE LEAD and PENDING.

PREVIOUS Plan: SHARE-01 of 1 CODE-COMPLETE (per-recipe visibility private/household/public on the existing recipe_shares feature; migration 0038 adds the recipe_visibility enum + recipes.visibility NOT NULL default private, backfilled; the no-auth /share/<token> route is gated on visibility=public in sharedRecipeProcedure (opaque NOT_FOUND for private/household, no enumeration) + a repo-level gate in getPublicRecipeView; shareCreate->public, revoke/delete-last->private; a shareSetVisibility authedProcedure behind assertRecipeAccess edit + the recipe Share-panel Visibility control; share token bumped to randomBytes(32); i18n in all 11 locales; visibility carried through FullRecipeSchema/getRecipeFull + omitted from RecipeDashboardSchema so no recipe safeParse breaks; both public gates adversarially RED-when-weakened then reverted; HOUSE-06 6/6 intact). SUMMARY written. Human-verify (Chrome + migration-0038-at-boot) OWNED BY THE LEAD and PENDING.

PREVIOUS Plan: 03-01 of 1 CODE-COMPLETE (POLICY-01: 3 permission_level enum columns on households + migration 0037 backfilled from the server-wide policy; canAccessResource resolved per-cookbook from the recipe's OWN cookbook with admin-edits-any/members-edit-own for edit/delete=household; buildViewPolicyCondition source-swapped to the active cookbook's view_policy with NO cross-cookbook widening; getHouseholdPolicy/setHouseholdPolicy + a households.setPolicy admin mutation; SetHouseholdPolicyInputSchema disallowing per-cookbook view=everyone; an admin-only Recipe Permissions card + i18n in all 11 locales). HOUSE-06 preserved + adversarially re-verified at both seams. SUMMARY written. The 03-01 human-verify (docker build + Chrome visual smoke + migration-0037-at-boot) is OWNED BY THE LEAD and PENDING.
Status: In Progress (code-complete; human-verify pending)
Last activity: 2026-06-14 — Phase 10 / 10-01 (WorkOS-only auth: disable norish-only email/password + auto-redirect the unauthenticated entry straight to WorkOS AuthKit, WORKOS-ONLY-01) CODE-COMPLETE on branch feat/workos-only-auth (off main). Owner wants WorkOS to be the SOLE sign-in/sign-up path. INVESTIGATION: norish ALREADY shipped most of this — apps/web/app/(auth)/login/page.tsx already auto-redirected for a sole OAuth provider (oauthProviders.length===1 && !hasCredential && !justLoggedOut) and login-client.tsx already rendered <AutoSignIn> which calls signIn.oauth2({providerId:'workos'}) -> the AuthKit page; signup/page.tsx already redirect('/login') when !passwordEnabled, and getAvailableProviders only adds the credential provider when PASSWORD_AUTH_ENABLED, so turning password off already removes the email/password form + the signup route (no norish-only accounts). Loop-safety already existed: apps/web/proxy.ts EXCLUDES /login, /signup, /auth-error, /api/auth from the auth matcher and the OAuth flow uses errorCallbackURL='/auth-error'; the WorkOS callback ${AUTH_URL}/api/auth/oauth2/callback/workos is under the excluded api/auth so the post-auth callback can't re-enter the auto-redirect. PASSWORD_AUTH_ENABLED already defaults to !hasOAuthEnvConfigured() (phase 09). WHAT NEEDED CODE: (1) extracted the inline redirect condition into a TESTABLE pure shouldAutoRedirectToSso(providers, escapeRequested) in packages/auth/src/providers.ts (!escapeRequested && oauthProviders.length===1 && !hasCredential — conservative + self-recovering); (2) login/page.tsx + signup/page.tsx call it and add the explicit ?sso=0 recovery escape the task required (the only prior escape was ?logout=true), forwarded through the signup->login redirect; (3) a visible 'Use another sign-in method' -> /login?sso=0 link on the AutoSignIn spinner (new login.useAnotherMethod i18n key in all 11 locales — en/nl/de/fr/es/it translated, da/ko/pl/ru English fallback — keeps check:locale-keys parity); (4) sso-auto-redirect.test.ts (8 tests: sole-provider->redirect, password-on->no, no-provider->no, >1-provider->no, ?sso=0/escape->no). The genericOAuth WorkOS provider (buildWorkOSProviders), the proxy matcher, the better-auth callback, and login-client.tsx's defensive re-check are ALL UNCHANGED. THE FLIP (lead, at deploy): set PASSWORD_AUTH_ENABLED=false (+ WORKOS_CLIENT_ID/WORKOS_API_KEY) — no new flag introduced; ?sso=0 is a URL param, not config. Static verify GREEN: typecheck @norish/auth + @norish/web EXIT 0 (web real tsc); i18n:check EXIT 0; lint @norish/auth + the 3 touched web files clean. Tests: @norish/auth 114/114 (7 files) incl. the 8 new sso-auto-redirect + workos-provider 7/7 + password-auth 9/9 UNCHANGED. Pre-existing OUT-OF-SCOPE failures (untouched): archive-import-overwrite (shared-server), updateRecipeWithRefs (db). gsd PLAN+SUMMARY under .planning/phases/10-workos-only-auth/. 3 commits (b1391406 feat helper+escape, 19875741 feat escape-link+i18n, a3f2727e test) on feat/workos-only-auth; NO docker:build (lead-owned), NO merge to main, live containers/DB + /opt/norish/ UNTOUCHED, NO schema/migration. WORKOS-ONLY-01 satisfied. HUMAN-VERIFY: LEAD = set PASSWORD_AUTH_ENABLED=false + WorkOS env + docker:build + redeploy + Chrome e2e (logged-out lands on AuthKit not the norish UI; /login?sso=0 + /signup behave; recovery with password re-enabled). PENDING.

Prior: 2026-06-14 — Phase 9 / 09-01 (WorkOS auth provider via ENV / config-as-code + admin-UI card REMOVED, WORKOS-ENV-01) CODE-COMPLETE on branch feat/workos-env-config (off main). Kiran (owner) wants WorkOS set purely via env (backend/compose), never the admin UI. INVESTIGATED norish's env->provider pattern: it does NOT read env directly into the provider — seed-config.ts::importEnvAuthProvidersIfMissing() (syncOIDC/syncGitHub/syncGoogle) SEEDS THE DB at boot from SERVER_CONFIG env (insert/update-if-differ/delete on a non-overridden row; never touch isOverridden=true), then loadAuthProvidersIntoCache() loads the DB rows into the global provider-cache that auth.ts consumes. WorkOS (phase 08) was the ONLY provider with NO env vars + NO syncWorkOSProvider (deliberately admin-only). MIRRORED the Google pattern: added WORKOS_CLIENT_ID + WORKOS_API_KEY (both z.string().optional()) to ServerConfigSchema in packages/config/src/env-config-server.ts, and a syncWorkOSProvider() in seed-config.ts wired into importEnvAuthProvidersIfMissing() (envConfig {clientId, apiKey, isOverridden:false}; setConfig sensitive=true so the API key is encrypted; configsDiffer guard; delete when env removed + non-overridden). PRECEDENCE: env over DB — isOverridden:false means every boot re-seeds/updates from env, and since the UI is removed nothing sets isOverridden:true, so env is always authoritative. WorkOS also added to hasOAuthEnvConfigured() so password-auth defaults OFF when only WorkOS env is set. The phase-08 genericOAuth provider (buildWorkOSProviders: providerId workos, api.workos.com/user_management authorize+authenticate, authorizationUrlParams provider=authkit, custom getToken/getUserInfo, apiKey=client_secret), the auth_provider_workos server-config key + AuthProviderWorkOSSchema, provider-cache, and the tRPC/connection-test plumbing are ALL UNCHANGED — only the CONFIG SOURCE moved. ADMIN-UI CARD REMOVED: deleted the key=workos AccordionItem + the tWorkos hook + the authProviderWorkOS destructure + workos from dirtySections in apps/web/app/(app)/settings/admin/components/auth-providers/auth-providers-card.tsx (grep -ni workos on the file = NONE); google/github/oidc cards + the shared generic AuthProviderForm untouched. Docs: an 'Option 5: WorkOS AuthKit' env block (env-only note + callback URI) added to .env.example + docker/docker-compose.example.yml. New env-sync tests (auth-provider-sync.test.ts WorkOS describe, +6): insert-from-env, env-over-DB precedence, no-rewrite-when-equal, isOverridden respected, clientId-only-not-seeded, delete-when-removed. Static verify GREEN: typecheck config/shared/db/auth/trpc/api/web all EXIT 0 (web real tsc); i18n:check EXIT 0; lint api/config/web-card clean (1 pre-existing unrelated api warning). Tests: @norish/api 348/348 (auth-provider-sync 22 incl. 6 new), @norish/auth 106/106 (workos-provider 7 UNCHANGED), @norish/web 379/379, @norish/trpc 255/255. Pre-existing OUT-OF-SCOPE failures: archive-import-overwrite (shared-server), updateRecipeWithRefs (db). gsd PLAN+SUMMARY under .planning/phases/09-workos-env-config/. 4 commits (bf6d57a7 feat env+seed, e3d425a6 feat remove-UI-card, 2b10e323 docs, 6693f7e1 test) on feat/workos-env-config; PUSHED to origin feat/workos-env-config; NO docker:build (lead-owned), NO merge to main, live containers/DB/boot-patch + /opt/norish/ UNTOUCHED, NO schema/migration. WORKOS-ENV-01 satisfied. HUMAN-VERIFY: LEAD = docker:build + set WORKOS_CLIENT_ID in the live compose + redeploy + confirm the WorkOS card is gone & the login button shows; OWNER = set WORKOS_API_KEY in the deploy env (both vars required to activate). PENDING.

Previous activity: 2026-06-14 — Phase 8 / 08-01 (WorkOS AuthKit as an ADDITIONAL better-auth login provider, WORKOS-01) CODE-COMPLETE. Kiran is pivoting norish toward a possible commercial product and wanted WorkOS auth built in; added WorkOS AuthKit as an additive, reversible login provider that better-auth CONSUMES (better-auth stays the session/user/household core; email/password, Google, GitHub, generic-OIDC all untouched). MECHANISM (doc-verified, not guessed): better-auth's `genericOAuth` plugin — NOT the sso plugin, NOT the existing OIDC discovery slot. norish's OIDC slot is ALREADY a genericOAuth provider; the client already loads genericOAuthClient(); the login button dispatches github/google→signIn.social and EVERYTHING ELSE→signIn.oauth2({providerId}), so WorkOS slots in as a 2nd genericOAuth provider with ZERO button-dispatch change. WHY NOT the OIDC slot: WorkOS AuthKit/User Management is a non-standard OAuth2 API at api.workos.com (no usable OIDC discovery for this flow; its token endpoint returns the user DIRECTLY, no /userinfo) — so an explicit-URL genericOAuth provider with a custom getToken + getUserInfo is the documented better-auth path. FLOW (verified workos.com/docs): authorize GET https://api.workos.com/user_management/authorize (client_id, redirect_uri, response_type=code, +provider=authkit for the hosted UI); token POST https://api.workos.com/user_management/authenticate (client_id, client_secret=the WorkOS API Key, grant_type=authorization_code, code) → returns {user, access_token, refresh_token} with the user object directly (id/first_name/last_name/email/email_verified/profile_picture_url). buildWorkOSProviders() in packages/auth/src/auth.ts: getToken does the POST and returns {accessToken, refreshToken, raw:<full response>}; getUserInfo reads tokens.raw.user (NO separate userinfo call) → {id,email,emailVerified,name,image}. 'workos' added to accountLinking.trustedProviders. CALLBACK URI Kiran registers in the WorkOS dashboard: ${AUTH_URL}/api/auth/oauth2/callback/workos → PROD https://norish.knoppsmart.com/api/auth/oauth2/callback/workos; verify-stack http://192.168.2.47:3010/api/auth/oauth2/callback/workos (BUT WorkOS prod requires HTTPS and only allows http for localhost/127.0.0.1, NOT a LAN IP — so realistic testing needs the HTTPS domain or a localhost SSH-tunnel in a WorkOS Sandbox env). CONFIG is admin-runtime (NOT env, NOT hardcoded): new server-config key auth_provider_workos (added IDENTICALLY to the two byte-identical zod twins packages/config/src/zod/server-config.ts + packages/shared/src/contracts/zod/server-config.ts — AuthProviderWorkOSSchema {clientId, apiKey} + Input + types + getSchemaForConfigKey + SENSITIVE_CONFIG_KEYS + RESTART_REQUIRED_KEYS); apiKey auto-encrypted (already in repo SENSITIVE_FIELDS). provider-cache caches it (getCachedWorkOSProvider); seed-config loadAuthProvidersIntoCache loads it at startup. Admin UI: a WorkOS accordion (Settings→Admin→Auth Providers) reusing the GENERIC AuthProviderForm like google/github (Client ID + API Key=client_secret), wired through providers.ts/getAvailableProviders+getConfiguredProviders, the admin tRPC router (updateWorkOS + workos in delete/test), connection-tests testWorkOSProvider, the shared-react useAdminMutations, the web admin context/form/card. The 'Continue with WorkOS' login button (logos:workos-icon) shows ONLY when workos.clientId is set; button text reuses the existing parameterized auth.provider.signInWith (no new login i18n). i18n settings.admin.authProviders.workos in ALL 11 locales (en+nl real, 9 EN-fallback). signup→OWN-COOKBOOK hook fires for WorkOS users (databaseHooks.user.create.after is provider-agnostic — keys on user creation + 0 households, idempotent; the OIDC claim-processor is the only provider-specific hook, runs if providerId===oidc, so WorkOS correctly skips it). Hermetic unit test packages/auth/__tests__/auth/workos-provider.test.ts (mocks the provider cache + auth.ts heavy deps + global fetch, NO real WorkOS network): empty-when-unconfigured/clientId-only; the provider shape (providerId/authorizationUrl/authorizationUrlParams/apiKey→clientSecret); getToken POSTs authenticate with grant_type=authorization_code + preserves raw; getToken throws on failure; getUserInfo maps the WorkOS user + email-name fallback + null when no user. @norish/auth 99→106. Static verify GREEN: typecheck config/shared/db/web (all STRICT real tsc) + auth/trpc/shared-react/api all EXIT 0; strict tsc on auth has ZERO errors in WorkOS code (only the PRE-EXISTING as-AuthInstance cast remains, baseline — also proves better-auth's GenericOAuthConfig accepts authorizationUrlParams/getToken/pkce/responseType); i18n:check EXIT 0; lint auth 0 problems + trpc EXIT 0 + touched web files 0 errors. Tests (no regressions): @norish/auth 106/106 (7 new), trpc 255/255, shared-react 27/27, web 379/379, config 726/726, shared 222/222. The two zod twins stay BYTE-IDENTICAL. Pre-existing OUT-OF-SCOPE failures (NOT in any touched pkg): archive-import-overwrite (shared-server), updateRecipeWithRefs (db). gsd PLAN+SUMMARY under .planning/phases/08-workos-auth/. 6 commits (fa044e42 plan, 04ee4435 feat core wiring, 9f916ff4 feat surface+admin-router, c43e44c2 feat admin-UI+i18n, f05f3baf test, 2283fee2 style) on feat/phase-2-multi-household; PUSHED to origin feat/phase-2-multi-household; NO docker:build (lead-owned), live containers/DB/boot-patch UNTOUCHED, NO schema/migration (server_config is a generic KV table). WORKOS-01 satisfied. HUMAN-VERIFY: LEAD = docker:build + recreate norishp2 + Chrome-verify the WorkOS admin accordion + the login button; KIRAN = WorkOS dashboard setup (activate User Management, register the callback URI, copy Client ID + API Key) + paste them in Settings→Admin→Auth Providers→WorkOS + restart (RESTART_REQUIRED). PENDING.
Previous activity: 2026-06-14 — Phase 7 / 07-01 (Locale-aware recipe extraction, LOCALE-01) CODE-COMPLETE: fixed the Dutch-source→English-recipe bug. ROOT CAUSE: the structured recipe-extraction prompt (loadPrompt("recipe-extraction") → template packages/shared-server/src/ai/prompts/recipe-extraction.txt, byte-identical api twin) is English-only with NO language directive; the three builders in packages/api/src/ai/prompts/builder.ts (HTML/image/video) + the per-call system strings are English and threaded NO locale, so Gemini generated/normalized every free-text field (description, recipeIngredient, recipeInstructions, notes, keywords) in English while only name survived in Dutch (extracted near-verbatim). Bug path: youtube.ts (captions+description first) → extractRecipeFromVideo (video/normalizer.ts) → buildVideoExtractionPrompt → generateText({output:Output.object({schema})}). FIX: NEW fragment packages/api/src/ai/prompts/fragments/language.ts — buildLanguageInstruction(targetLanguage?) appends a LANGUAGE: block (keep name/description/notes/ingredients/steps/keywords in the SAME language as the source, do NOT translate, name the resolved target language when known, KEEP the categories enum English so the normalizer's matcher works) + localeToLanguageName(code?) mapping locale codes/BCP-47 tags → human names via LOCALE_CATALOG (@norish/i18n/locales, base-subtag fallback, pass-through, undefined for empty). Added targetLanguage?: string to RecipeExtractionPromptOptions (inherited by video/image opts) and appended the fragment in all three builders (buildImageExtractionPrompt gained an optional 2nd arg); re-exported from the fragments+prompts barrels. DELIBERATELY did NOT edit recipe-extraction.txt: at runtime loadPrompt reads from getPrompts() (DB/config seeded from the .txt) so editing the seed would NOT fix an already-seeded deployment (the verify stack) — threading through the BUILDER guarantees it applies on every extraction regardless of config state (robust + minimal). Threaded a REAL locale signal at the 3 call sites: video/normalizer.ts targetLanguage = metadata.language || (await getDefaultLocale()) — prefers the video's own BCP-47 audio language (VideoMetadata.language), falls back to the configured default locale (nl); recipe-parser.ts (HTML) + image-recipe-parser.ts use await getDefaultLocale() (no per-source signal there). getDefaultLocale() from @norish/config/server-config-loader returns the deployment default (nl, env-default en); the "match the source content's language" clause is the ultimate guard for mixed-language sources. Pure unit tests (NO mocks/network) in packages/api/__tests__/ai/prompts/builder.test.ts: directive text + categories-English exception + explicit-language line + locale-code→name + base-subtag + undefined fallback — builder.test.ts 17→25 tests. Static verify GREEN: pnpm --filter @norish/api typecheck (tsc --noEmit --noCheck) EXIT 0; strict real tsc --noEmit has ZERO errors in any touched file (only error files are the PRE-EXISTING @norish/auth/src/auth.ts + @norish/shared-server category-matcher.ts + archive/parser.ts — none mine); lint @norish/api 0 errors (1 pre-existing unrelated warning in parser/python/adapter.ts); @norish/api test 27 files / 342 tests PASS EXIT 0 (builder 25/25). Pre-existing failures NOT in scope + NOT in @norish/api: archive-import-overwrite (shared-server), updateRecipeWithRefs normalization (db). MECHANICS: the new language.ts was hardlinked into node_modules/@norish/api/src (hardlinks only mirror existing files; a brand-new source file must be linked); existing-file edits were already live via inode-shared hardlinks. NO env var, NO new dependency, NO schema/migration, NO UI, NO live-container/boot-patch touch, NO docker:build. gsd PLAN+SUMMARY under .planning/phases/07-locale-aware-extraction/. 4 commits (6f0bd61d plan, bbb63ac9 feat fragment+builders, f38bc50d feat call-site threading, f0f121c5 test) on feat/phase-2-multi-household; PUSHED to origin feat/phase-2-multi-household; live containers/DB UNTOUCHED. LOCALE-01 satisfied. HUMAN-VERIFY (LEAD: re-import a Dutch source on the verify stack http://192.168.2.47:3010 norishp2 [working Gemini config] → confirm description/ingredients/steps render in Dutch; categories stay English by design) PENDING.
Previous activity: 2026-06-14 — Phase 6 / 06-01 (DeepSeek V4 AI/LLM provider) CODE-COMPLETE: the DeepSeek recipe-extraction provider (module:"ai", "Creating AI models") was ALREADY fully wired from upstream ("deepseek" in AIProviderSchema in BOTH server-config.ts twins; a factory `case "deepseek"` building the model via createDeepSeek({apiKey,fetch}) from @ai-sdk/deepseek reading the runtime admin AI-config key like openai/google; a live https://api.deepseek.com/models listing config; a connection-test case; the admin ai-config-form <SelectItem key="deepseek"> + cloudProviders/newCloudProviders + the API-key SecretInput; providers.deepseek="DeepSeek" in all 11 locales; @ai-sdk/deepseek@^2.0.32 already a dependency) — verified by exhaustive grep + read. context7 (/websites/ai-sdk_dev_v7) + the installed SDK d.ts (DeepSeekChatModelId = 'deepseek-chat'|'deepseek-reasoner'|(string & {})) confirmed createDeepSeek({apiKey}) is the documented integration and arbitrary v4 ids pass through to /chat/completions — NO new package, NO openai-compatible route, NO SDK change. The ONLY real gap vs the task was surfacing the two v4 models, so I added an optional ProviderConfig.defaultModels + DEEPSEEK_DEFAULT_MODELS ([deepseek-v4-pro, deepseek-v4-flash]) on providerConfigs.deepseek, merged into (and de-duped against) the live /models result in listModelsWithConfig — both v4 ids are now always offered in the admin model picker (the Autocomplete still accepts any typed id too). NO enum/factory/connection-test/admin-form/i18n edit (already correct), NO new dependency, NO env var, NO key in source. Two fetch-mocked unit tests (NO real key/network): __tests__/ai/providers/listing.test.ts (3: live result merges+de-dupes the v4 ids; no dup when the API already returns a v4 id; []+no-fetch with no key) and __tests__/ai/providers/factory.test.ts (3: createModelsFromConfig builds a DeepSeek ModelConfig providerName="DeepSeek" for deepseek-v4-pro; the requested v4 id is the model's modelId; missing key throws "API Key is required for DeepSeek"). Static verify GREEN: typecheck config/shared/shared-server/api/web all EXIT 0; i18n:check EXIT 0 (no new keys); lint listing.ts 0 errors + @norish/shared-server lint RC 0; @norish/shared-server test 150/154 (the two new AI files +6 green; the 4 fails are the PRE-EXISTING archive-import-overwrite zipBytes.buffer.slice failures, baseline 144/4, NOT mine — flagged for a separate task); the two server-config.ts twins still byte-identical (not edited). gsd PLAN+SUMMARY under .planning/phases/06-deepseek-provider/. Commits 01a92721 (plan), df2f516c (feat listing), 40628b35 (test) + the docs commit on feat/phase-2-multi-household; PUSHED to origin feat/phase-2-multi-household; live containers/DB UNTOUCHED; no docker:build (lead-owned). AI-01 satisfied. HUMAN-VERIFY (LEAD: rebuild + recreate norishp2 + Chrome-verify admin AI config shows DeepSeek + the two v4 models; KIRAN: enter the DeepSeek key, pick deepseek-v4-pro, test extraction) PENDING.

Previous activity: 2026-06-14 — Phase 5 / 04-01 (AssemblyAI transcription provider) CODE-COMPLETE: added "assemblyai" to TranscriptionProviderSchema + the enabled/cloud lists (BOTH the canonical packages/config copy and the byte-identical packages/shared/contracts twin) and a real transcribeWithAssemblyAI (upload->request->poll, duplex:"half" streamed upload, 300s/30s/600s timeouts, AIResult contract) as a `case "assemblyai"` in transcribeAudio — API key from video_config.transcriptionApiKey (config-driven, NO env, NO boot-patch), ported from the proven live boot-patch. AssemblyAI is a CLOUD provider (auto-shows the API-key field) with no endpoint/no model-listing; transcriptionModel maps to speech_model (best|nano else default). Exposed in the admin Video Processing form (one SelectItem) + the transcriptionProviders.assemblyai label in all 11 locales (i18n:check green). New unit test (fetch mocked, NO real API): no-key->AUTH_ERROR, upload->create->poll happy path, upload-fail->PROVIDER_ERROR. Static verify GREEN: typecheck config/shared/api/web, i18n:check, lint (0 errors on touched files), @norish/api suite 334/334 incl. the new 4. gsd CONTEXT+PLAN+SUMMARY under .planning/phases/05-assemblyai-transcription/. 3 commits (0b38333d feat-api provider, cdc07ecd feat-web UI+i18n, f0f5b858 test-api) on feat/phase-2-multi-household; nothing pushed; live containers/DB/patch UNTOUCHED; no docker:build (lead-owned). VIDEO-01 satisfied. HUMAN-VERIFY (LEAD: set a real AssemblyAI key in norishp2 + import a short YouTube clip end-to-end) PENDING — recipe at /tmp/PHASE4-VIDEO-SUMMARY.md.

Previous activity: 2026-06-14 — UI BUGFIX (3 user-reported bugs, NOT a planned phase) on feat/phase-2-multi-household, 2 commits: (#2) ed1aae59 fix(web): enlarged timer dock can't be closed with >1 timer — the fixed, bottom-anchored expanded card grew upward and on short/mobile viewports pushed its header (the only close control) off the top of the screen; clamped the card to max-h-[80dvh] flex-col, pinned the header shrink-0, list min-h-0 flex-1 (was max-h-96) — close now always reachable, desktop unchanged & Chrome-verified. (#1) 72677ee2 fix(web): timer-dock positioning wrapper now pointer-events-none + card pointer-events-auto (matching add-grocery-button/floating-recipe-chip) so the dock no longer blocks the md:hidden mobile add-grocery FAB beneath it — MOBILE-ONLY, lead must phone-verify. (#3) add-recipe button NOT REPRODUCED & no fix: it lives in the page header (top), the dock is at the bottom — no overlap; desktop dropdown verified working; flagged connection-status-overlay (fixed inset-0 z-[99999], no pointer-events-none) as the only plausible app-wide tap-blocker but left it (intentionally modal). typecheck:web + lint(timer-dock) clean; web tests add no regressions (re-synced a stale ratings hardlink twin: 23→2 failed files; the 2 left are a pre-existing RATE-01 @/hooks/ratings mock gap missing useRecipeRatersQuery, unrelated, flagged). SUMMARY at /tmp/UI-FIXES-SUMMARY.md (110). Nothing pushed; live containers untouched; no docker build (lead-owned). Chrome phone-verify of #1/#2-mobile + a #3 repro PENDING with the LEAD.

Previous activity: 2026-06-14 — RATE-01 (recipe ratings: average+count + per-user named-rater list) CODE-COMPLETE: built ON norish's existing per-user recipe_ratings feature (table + repo rateRecipe/getUserRating/getAverageRating->{averageRating,ratingCount} + the ratings router + StarRating/RatingStars UI + the useRatingQuery that ALREADY returns averageRating/ratingCount + the dashboard card that ALREADY shows averageRating via RecipeDashboardSchema — all reused, not reinvented). NO schema change/migration (recipe_ratings + user already exist). NEW getRecipeRaters(recipeId) repo fn: recipe_ratings INNER JOIN user, decrypt(users.name) with a null fallback (mirrors getUsersByIds), ORDER BY updatedAt DESC; RecipeRater type. NEW RecipeRaterSchema/RecipeRatersSchema shared zod ({recipeId, averageRating, ratingCount, raters[{userId,name|null,rating,updatedAt}]}). NEW ratings.getRaters authedProcedure: `await assertRecipeAccess(ctx, input.recipeId, "view")` FIRST, then getAverageRating+getRecipeRaters in parallel — a user who can't view the recipe (per the recipe's OWN cookbook policy, POLICY-01) gets TRPCError FORBIDDEN and the rater NAMES are NEVER fetched (no cross-cookbook name leak, HOUSE-06). A read-only RecipeRaters web component renders the aggregate (★ + average.toFixed(1) + a pluralized count via t('ratingsCount',{count})) + the per-user 'rated by <name>' list (each with a 5-star read-only row; current viewer -> t('you'); null name -> t('anonymousRater')) in the EXISTING rating section (the bg-default-100 box under Steps) of BOTH recipe-page-desktop + recipe-page-mobile, directly under the existing StarRating input; renders nothing while loading or at ratingCount===0 (the existing prompt covers empty). createUseRecipeRatersQuery added into the existing createRatingsHooks factory + the web adapter (no new tRPC client wiring). i18n recipes.detail.{ratingsCount(ICU plural),ratedBy{name},you,anonymousRater} in ALL 11 locales (nl+en real, 9 EN-fallback) — i18n:check exits 0 (prettier --check on the locale files passes; a few pl/de/es/fr/ko/ru inline objects were Prettier-normalized). The pre-existing rate/getUserRating/getAverage procedures stay authed-but-not-access-gated (upstream behavior, OUT OF SCOPE); only the NEW getRaters (the member-NAMES surface) gets the gate. PUBLIC /share view UNTOUCHED — ratings/names NOT added; DEFERRED as RATE-02 (privacy decision flagged for Kiran: exposing cookbook member names to anonymous visitors). ACCESS-RESPECTING TEST (packages/trpc/__tests__/ratings/raters.test.ts, 5/5): exercises the REAL getRaters vs the REAL assertRecipeAccess boundary (only the data layer mocked, mirroring permissions-integration) — a cookbook member gets names+stars; a member of ONLY another cookbook AND the personal view both get FORBIDDEN with getRecipeRaters never called (asserted); null name graceful; the rejection is a real TRPCError FORBIDDEN. ADVERSARIAL: `if (false) await assertRecipeAccess(...)` turned EXACTLY the 3 access-control tests RED (the 2 happy-path stayed green) then reverted byte-identical (git grep -c 'if (false)' -> 0; all 13 ratings tests green again); never committed. Static verify GREEN: typecheck db/shared/auth/trpc/shared-react/web all EXIT 0 (trpc/shared-react/auth scripts use --noCheck as since 02-04; a real tsc --noEmit on trpc+shared-react has ZERO errors in touched files — the only real-tsc errors are the 5 pre-existing unrelated shared-server+auth ones; web typecheck is a real tsc --noEmit, EXIT 0); i18n:check EXIT 0; lint db/shared/trpc/shared-react/web clean (0 problems). Tests: trpc recipes+ratings+households 109/109 (raters 5/5 NEW, ratings 8/8, permissions-integration 22/22), db households.isolation 6/6 (HOUSE-06 intact) + recipes 12/12 + recipe-shares 8/8 (no parse regression from the ratings-repo touch). 2 deviations (both test-only, Rule 1): the getRaters test goes through withAuth which OVERWRITES ctx.memberHouseholdIds (drove membership via a per-test getHouseholdsForUser mock keyed by userId + added isUserServerAdmin to the @norish/db mock); the test recipe id had to be a valid v4 UUID (z.uuid() strict). 3 (RATE-01) commits (ee3408a1, 341ddd8a, b900aa84) on feat/phase-2-multi-household; nothing pushed; live containers (norish-app/db/redis) untouched; no docker/dev-server build run. Planning artifacts under .planning/phases/04-sharing/RATE-01-SUMMARY.md. HUMAN-VERIFY (Chrome: rate a recipe -> see the average + count + your name as 'You' with stars in the rater list; a second cookbook member's display name + stars appear; the Dutch strings render; the public /share/<token> view shows NO ratings/names) PENDING with the LEAD.

Previous activity: 2026-06-14 — SHARE-01 (per-recipe sharing visibility) CODE-COMPLETE: built ON norish's existing tokenized recipe_shares feature (table + repo + recipes.shares router + sharedRecipeProcedure + the public /share/[token] page + media routes + the recipe Share panel + admin/user share-link cards — all reused, not reinvented). Added `recipe_visibility` pgEnum + `recipes.visibility` (NOT NULL default 'private') + migration `0038_lean_sauron` (CREATE TYPE + ADD COLUMN, existing rows backfill to private). The public surface is now gated on the recipe's EXPLICIT visibility: `sharedRecipeProcedure` resolves token->active-share->getRecipeFull then throws the SAME opaque NOT_FOUND when `visibility !== 'public'` (private/household unreachable via /share/<token> even with a valid token; no enumeration) — the single public choke point; `getPublicRecipeView` has a matching repo-level gate (defense in depth). The PUBLIC payload is UNCHANGED + single-recipe-only (PublicRecipeViewSchema: name/description/notes/url/image/servings/times/systemUsed/nutrition/categories/tags/recipeIngredients/steps/author{name,image}/images/videos — NEVER ids/owner/household/tokenHash/member-lists/cookbook-listing; media URLs rewritten to /share/<token>/...). `shareCreate` promotes the recipe to public; `shareRevoke`/`shareDelete` revert to private when no ACTIVE share remains (countActiveRecipeShares = not revoked + not expired). New `recipes.shareSetVisibility` authedProcedure gated on `assertRecipeAccess(edit)` (owner OR cookbook admin per POLICY-01, resolved against the recipe's OWN cookbook) emits the existing recipe `updated` event. Repo helpers getRecipeVisibility/setRecipeVisibility(optimistic version)/countActiveRecipeShares. Share token standardized to crypto.randomBytes(32).toString('base64url') (was 24). Recipe Share panel gained a Visibility select + helper wired to shareSetVisibility (invalidates the recipe query); i18n recipes.sharePanel.visibility.* in ALL 11 locales (nl+en real, 9 EN-fallback) — i18n:check exits 0. REGRESSION-PROOFED the new column (the 02-06 createSelectSchema class): visibility carried through FullRecipeSchema + getRecipeFull (columns + dto) and OMITTED from RecipeDashboardSchema, so getRecipeFull/getRecipeByUrl/dashboardRecipe/listRecipes all keep parsing real rows — verified by a real-Postgres parse test. ADVERSARIAL: weaken the middleware gate -> the two 'not serve private/household publicly' trpc tests RED; weaken the getPublicRecipeView gate -> the db 'not build a public view for private/household' test RED; both reverted byte-identical, never committed. Static verify GREEN: typecheck db/shared/auth/trpc/shared-react/web all EXIT 0; i18n:check EXIT 0; lint db/shared/trpc/web(panel) clean (0 errors); real-tsc on trpc has ZERO errors in touched files (the 5 pre-existing shared-server+auth errors are unrelated). Tests: trpc recipes+households 96/96 (incl. shares 17/17 with the new visibility-gate + payload-keys + edit-gate tests), db recipe+share+isolation+zod 34/34 (recipe-shares 8/8 incl. real-parse + gate, recipes 12/12, households.isolation 6/6 — HOUSE-06 intact, recipe zod 8/8), auth 99/99. 3 deviations (Rule 1/3): re-linked 4 broken node_modules hardlink twins (zod/recipe.ts, schema/recipes.ts, repositories/recipes.ts, middleware.ts); added visibility:'public' default to createMockFullRecipe + set the existing db public-view test recipe public (gate precondition); the FORBIDDEN test now throws a real TRPCError. 5 (SHARE-01) commits (4a8349f3, 5fdfcfa7, a35a909c, 07fd4e85, bfb679e3) on feat/phase-2-multi-household; nothing pushed; live containers (norish-app/db/redis) untouched; no docker/dev build run. Planning artifacts under .planning/phases/04-sharing/; AssemblyAI renumbered Phase 4->5. HUMAN-VERIFY (Chrome: set a recipe public -> open /share/<token> logged-out -> ONLY that recipe shows; set it private/revoke -> the link is NOT viewable; AND migration-0038-at-boot) PENDING with the LEAD.

Previous activity: 2026-06-14 — 03-01 (per-cookbook permission policies, POLICY-01) CODE-COMPLETE: 3 `permission_level` pgEnum columns (view_policy/edit_policy/delete_policy) on `households` (reusing the config PermissionLevelSchema values) + migration `0037_curious_bloodscream` adding the enum/columns and backfilling existing households from the current server-wide `recipe_permission_policy` (enum-cast inside COALESCE; no-op at 0 households). `canAccessResource` is now SYNC and takes a resolved cookbook policy + admin id; `resolveRecipeCookbookPolicy(resourceHouseholdId)` keys policy+admin off the recipe's OWN cookbook (global-default + null-admin fallback for personal/missing); `edit`/`delete = household` => recipe owner OR cookbook admin (admin-edits-any / members-edit-own). `buildViewPolicyCondition` reads the ACTIVE cookbook's `view_policy`; an active cookbook is hard-scoped and NEVER widens cross-cookbook (a seeded `everyone` is clamped) — HOUSE-06 invariant. households repo `getHouseholdPolicy`/`setHouseholdPolicy` (admin-only, optimistic version, rejects view=everyone) + policy seeded on create; `households.setPolicy` admin mutation (cache invalidation + policyUpdated emit); admin-gated policy in the settings DTO (member DTO + member resolver stay policy-free, like inviteToken). Shared zod `SetHouseholdPolicyInputSchema` enforces decision #5 (view ∈ household|owner). Admin-only Recipe Permissions card on the Household settings page (mirrors the server-admin card; view Select offers only Household/Owner) wired via the settings context + a `setPolicy` shared-react mutation; i18n `settings.household.permissions.*` in ALL 11 locales (nl+en real, 9 EN-fallback) — i18n:check exits 0. Emit-scope readers (~12 emitByPolicy sites) intentionally LEFT on the global default (broadcast scope only, not a gate). Tests: auth permissions rewritten (sync per-cookbook; admin-vs-member matrix + a 27-combo cross-cookbook isolation grid), new resolve-cookbook-policy test, trpc permissions-integration rewritten onto getHouseholdPolicy (+admin matrix), new db households.policy real-Postgres test (getHouseholdPolicy/setHouseholdPolicy, the HouseholdSelectBaseSchema real-parse of the enum columns, + the view-policy source-swap proof). ADVERSARIAL weaken→RED→revert verified at BOTH seams (drop admin check → auth admin-vs-member RED; read global view policy → db source-swap RED; never committed; reverts byte-identical). 3 deviations (Rule 1/3): migration COALESCE enum-cast fix found via the real-PG test infra; getHouseholdPolicy/setPolicy added to pre-existing test mocks (households-stale + web hooks); re-synced 4 stale node_modules hardlink twins. Static verify GREEN: typecheck db/shared/auth/trpc/shared-react/web, i18n:check, lint (auth/trpc/shared-react/web 0 problems) all clean; auth 99/99, trpc households+permissions+recipes 88/88, db household suites 18/18 (real PG), web household hooks 26/26. 4 (03-01) commits (4197e164, efd9e486, dfcafed3, e4d87bb2) + the SUMMARY commit (a3ab337f) on feat/phase-2-multi-household; nothing pushed; live containers (norish-app/db/redis) untouched; no docker/dev build run. HUMAN-VERIFY (Chrome: the admin Recipe Permissions card + view∈Household/Owner, the admin-vs-member edit behavior, per-cookbook view=owner isolation, the member-hidden card, AND migration-0037-at-boot) PENDING with the LEAD.

Previous activity: 2026-06-13 — 02-06 (shareable invite link, INVITE-01) CODE-COMPLETE: added `households.invite_token` (nullable text + `uq_households_invite_token` UNIQUE; NULLs distinct) + migration `0036_perpetual_karma`. Repo `generateInviteToken` (admin-only FORBIDDEN guard; `crypto.randomBytes(32)` base64url ~43 chars; uniqueness loop; version-bumped; revokes the old link), `getHouseholdByInviteToken` ({id,name} or null), `joinHouseholdByInviteToken` (reuses the multi-membership `addUserToHousehold` path; NOT_FOUND on invalid). Router `households.generateInviteToken` + `households.joinByInviteToken` (authedProcedure) + a PUBLIC `households.getByInviteToken` (publicProcedure) that returns `{ name }` ONLY for a valid token, null otherwise (NEVER members/recipes/ids/other households — per-cookbook isolation HOUSE-06 intact); InviteTokenSchema (32-128 url-safe) guards both the generate input + the public lookup. join-by-token mirrors join-by-code (setActiveHousehold + emit + cache/connection invalidation). shared-react `generateInviteToken`/`joinByInviteToken` mutations (mutateAsync) on the global HouseholdContextValue + the web context. Admin Invite-link section in the settings join-code card (`${origin}/join/${inviteToken}` + Copy + Generate/Regenerate). New PUBLIC `/join/[token]` page (mirrors the share/ route group + BaseProviders layout; calls the public lookup + the authed join DIRECTLY via useTRPC + better-auth useSession): valid → cookbook name + Join → joined+active → dashboard; logged-out → `/login?callbackUrl=/join/<token>` → return (existing signup flow, NO registration bypass — that's deferred as INVITE-02); invalid/revoked → a friendly invalid state. i18n: settings.household.invite.* (8) + a settings.join.* namespace (9, description with {name}) in ALL 11 locales — nl+en real, 9 EN-fallback — i18n:check exits 0. Tests: trpc households-stale +6 (name-only/invalid public lookup, multi-membership join + active-switch, NOT_FOUND, admin/non-admin generate; the name-only guard adversarially verified RED-when-weakened then reverted/never-committed), web mutations +2. Static verify GREEN: typecheck db/shared/trpc/shared-react/web, i18n:check, @norish/web lint all exit 0; household tests green (trpc 7/7, web hooks 26/26, db households.isolation 6/6 — HOUSE-06 intact). 3 deviations: /join page path adapted to app/join/[token] (no (public) group; share/ is the public pattern), i18n keys scoped under the existing settings namespace (avoid a new join.json + messages.ts registration), and re-linked household.d.ts into the node_modules hardlink farm (broken twin from an earlier checkout). 5 feat(02-06) commits (b668f599, ca36cd21, ddbc2cd6, 7957913e, 44d079fa) + the SUMMARY commit (459670cc) on feat/phase-2-multi-household; nothing pushed; live DB/containers untouched; no docker/dev-server build run. HUMAN-VERIFY (Chrome re-verify of the invite-link generate/copy → /join/<token> name → Join → member+active, the logged-out→login→return flow, the invalid-token state, AND migration-0036-at-boot) PENDING with the LEAD.

Previous activity: 2026-06-13 — 02-05 (multi-household UI completion) CODE-COMPLETE: added a `households.rename` mutation (admin-only, optimistic-version `version+1`, members' household cache invalidated) + `renameHousehold` repo fn (mirrors transferHouseholdAdmin) + `RenameHouseholdInputSchema` (shared zod). Promoted createHousehold/joinHousehold/rename onto the GLOBAL HouseholdContextValue (factory gained useCreateHousehold/useJoinHousehold/useRename adapters; web wired) so the navbar can call them; the settings context now inherits create/join from `...base` (removed from its explicit list). New reusable `create-or-join-cookbook-modal.tsx` (CreateOrJoinCookbookForms — the exact Create-name + Join-6-digit forms sourced from useHouseholdContext() — + a heroui Modal); the navbar 'Create or join a cookbook' entry opens this modal instead of routing to /settings (mobile gets it free via the shared NavbarUserMenu); no-household-view.tsx refactored to reuse the same forms (−~90 lines). Admin-only inline cookbook rename on the Household settings page (household-info-card: pencil → Input, Enter saves / Esc cancels, passes household.version; addToast/showSafeErrorToast). i18n: settings.household.createOrJoin.title + settings.household.rename.* (5 keys) in ALL 11 locales — nl+en real, 9 EN-fallback — i18n:check exits 0. Resolves CKBK-UI-01 + RENAME-01 and the HOUSE-02 UI gap. Static verify GREEN: typecheck db/shared/trpc/shared-react/web, i18n:check, @norish/web lint all exit 0; household tests green (web hooks 24/24 incl. a fixed-up pre-existing 02-04-stale mock + a new rename test, trpc households-stale 1/1, db households.isolation 6/6). Two deviations beyond the test-mock fixup: re-linked 3 package-src files into the node_modules hardlink farm (broken/missing twins from earlier checkouts; @norish/shared zod + @norish/trpc router + the 02-04 use-households-list-query.ts), and removed an `autoFocus` that tripped jsx-a11y/no-autofocus. 5 feat(02-05) commits (538fa09d, 2bfc8c1c, 8933c0a8, edb19b27, dd8dc294) on feat/phase-2-multi-household; nothing pushed; live DB/containers untouched; no docker/dev-server build run. HUMAN-VERIFY (Chrome re-verify of the modal create/join + admin rename) PENDING with the LEAD.

Previous activity: 2026-06-13 — 02-04 (frontend cookbook switcher + assign-to-cookbook UX + i18n) CODE-COMPLETE: extended shared-react household hooks (useHouseholdsListQuery -> households.list; switchActive mutation that invalidates the household-list AND recipes.list query path) + the household context (households/activeHouseholdId/switchActive); a Cookbook DropdownSection in the shared NavbarUserMenu (rendered by desktop navbar + mobile bottom-bar) lists every household + a Personal option, marks the active one, calls switchActive — fully localized; the import-from-URL modal indicates the active-cookbook assign target; recipes-context refetches the list on activeHouseholdId change (server-event safety net beyond the local switchActive). i18n: navbar.cookbook.* (6 keys) + common.import.url.targetCookbook added to ALL 11 locales — nl+en REAL, the other 9 use the EN string value (user-locked EN-fallback) — pnpm i18n:check exits 0. Create/join uses the EXISTING join-by-code flow (entry -> /settings?tab=household); NO invite-link system built (out of scope). Static verification GREEN: shared-react typecheck, web typecheck, i18n:check, web lint all exit 0 (real tsc filtered to all touched shared-react files = 0 errors). 5 feat(02-04) commits on feat/phase-2-multi-household; nothing pushed; live DB/containers untouched; no docker/dev-server build run. HUMAN-VERIFY (docker build + visual smoke + the signup own-cookbook/OIDC UX confirmation) is PENDING with the LEAD.

Previous activity: 2026-06-13 — 02-03 (permissions + per-cookbook isolation tests, SECURITY-CRITICAL, HOUSE-06) shipped: canAccessResource is now cookbook-aware (resourceHouseholdId + requesterMemberHouseholdIds; household level = requester must be a member of the recipe's own cookbook; NULL household = owner-only); recipe helpers + the convert mutation pass the recipe's householdId + ctx.memberHouseholdIds; added getRecipeOwnerAndHousehold; getRecipeFull/dashboardRecipe/listRecipes now select+map householdId (the real cause of the 49 db failures — a 02-01 prod omission). Two dedicated isolation suites prove HOUSE-06 at the DB-scoping layer (households.isolation.test.ts, 6 tests) and the tRPC permission layer (permissions-integration.test.ts rewritten to a REAL-boundary test, 16 tests incl. a cross-cookbook block). Adversarial sanity check performed on BOTH layers (weaken canAccessResource → tRPC RED; weaken buildViewPolicyCondition → DB RED; reverted → both GREEN; weakenings never committed). Also fixed a latent per-cookbook dedup bug (recipeExistsByUrlForPolicy used `!== null` vs drizzle's undefined → exists always true). Cleared all 49 db + 32 trpc + 4 auth contract failures 02-02 deferred. Final: @norish/auth 61/61, @norish/trpc 223/223 green; @norish/db all cookbook/contract failures gone (3 pre-existing updateRecipeWithRefs unit-normalization failures remain — out of scope, follow-up task spawned). typecheck+lint clean across auth/db/trpc/shared. HOUSE-06 complete.

Progress: [█████████░] 93%

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table + Phase 2 CONTEXT.md (D-01..D-15). Recent:

- Phase 0: Dev + build on LXC 110 via SSH; fork under gh account 23492; gsd-core minimal profile. (done)
- Phase 1: native Camoufox REST client replaced chromium.connectOverCDP; vendored camofox-browser v1.4.1 bundled by default; CAMOFOX_URL config; cloud keys via admin UI not env. (done)
- Phase 2 (planned): `recipes.household_id` (nullable, ON DELETE SET NULL; NULL=personal) scopes recipe visibility; `user.active_household_id` is the active-cookbook pointer; unique constraint swaps `(url,userId)`→`(url,household_id)`.
- Phase 2 (planned): `getHouseholdForUser` → `getActiveHouseholdForUser` is THE central seam — member-scoped secondary repos (groceries/calendar/allergies/caldav) follow the active cookbook for free via `ctx.userIds`.
- Phase 2 (planned): per-cookbook isolation enforced in `permissions.ts` (`canAccessResource` takes recipe household_id + requester member household ids); dedicated DB + tRPC isolation tests (security-critical, HOUSE-06).
- Phase 2 (planned): `recipe_permission_policy` shape unchanged; `household` level reinterpreted as "recipe's own cookbook + requester is a member"; per-cookbook policy override deferred to v2 (HOUSE-08).
- Phase 2 (planned): i18n gate `i18n:check` requires new keys in ALL 11 locales (not just nl+en).
- Phase 2 (02-01 done): schema primitives + migration 0035 landed; activeHouseholdId FK callback annotated with `AnyPgColumn` (drizzle-orm/pg-core) to break the user↔households mutual-reference type cycle. (done)
- Phase 2 (02-02 done): active-household seam wired (context/middleware/withAuth/scheduler use getActiveHouseholdForUser; cached-household re-keyed to active; ctx.memberHouseholdIds added for 02-03 isolation); multi-membership (guards gone, households.list + switchActive, active set on create/join, reset on leave/kick); recipe scoping by recipes.household_id (buildViewPolicyCondition + dedup + createRecipeWithRefs onConflict [url, household_id]); import queue + archive carry householdId; signup auto-creates the user's own cookbook (better-auth user.create.after) which coexists with the OIDC claim path (OIDC org claim then sets the org cookbook active). (done)
- Phase 2 (02-04 code-complete): frontend cookbook switcher built on the shared-react factory — useHouseholdsListQuery (households.list) + a switchActive mutation that invalidates the household-list AND recipes.list query path; HouseholdContextValue gained households/activeHouseholdId/switchActive. One shared Cookbook DropdownSection in NavbarUserMenu serves desktop navbar + mobile bottom-bar (lists households + a Personal option, checks the active one, onPress->switchActive); mobile-nav also shows the active cookbook name. Import modal indicates the active-cookbook target (no v1 picker; backend assigns to active). recipes-context refetches on activeHouseholdId change too. i18n: nl+en real, other 9 EN-fallback (user-locked), i18n:check green. Create/join = existing join-by-code (no invite-link). Human-verify (docker build + visual) owned by the lead, PENDING. (code-complete)
- Phase 2 (02-05 code-complete): multi-household UI completed — `households.rename` (admin-only, optimistic-version; mirrors transferHouseholdAdmin; members' cache invalidated; NO new realtime event — the renamer's switcher/settings refresh via the client optimistic update + list invalidation, others on next read); create/join/rename promoted onto the GLOBAL household context (settings context inherits create/join via ...base); a reusable Create/Join cookbook modal (CreateOrJoinCookbookForms sourced from useHouseholdContext()) opened from the navbar 'Create or join a cookbook' entry (mobile via the shared NavbarUserMenu); no-household-view reuses the same forms; admin inline-rename on the settings page. i18n createOrJoin+rename in all 11 locales (nl+en real, 9 EN-fallback). Resolves CKBK-UI-01 + RENAME-01 + the HOUSE-02 UI gap. (code-complete; human-verify pending with the lead)
- Phase 2 (02-06 code-complete): shareable invite link (INVITE-01) — households.invite_token (nullable, uq_households_invite_token UNIQUE, NULLs distinct) + migration 0036; repo generateInviteToken (admin-only, crypto.randomBytes(32) base64url, version-bumped, revokes old) / getHouseholdByInviteToken (id+name) / joinHouseholdByInviteToken (reuses the multi-membership addUserToHousehold path). Router generateInviteToken + joinByInviteToken (authed) + a PUBLIC getByInviteToken (publicProcedure) returning { name } ONLY for a valid token (null otherwise; NEVER members/recipes/ids — HOUSE-06 intact; adversarially verified RED-when-weakened); InviteTokenSchema (32-128 url-safe) guards both. shared-react generateInviteToken/joinByInviteToken on the global context + web context. Admin invite-link UI in the settings join-code card + a PUBLIC /join/[token] page (mirrors share/; useTRPC + useSession; logged-out → login?callbackUrl → return, NO registration bypass). i18n settings.household.invite.* + settings.join.* in all 11 locales (nl+en real, 9 EN-fallback). SAME security as the join code; registration-bypass DEFERRED as INVITE-02. (code-complete; human-verify pending with the lead)
- Phase 2 (02-03 done): per-cookbook isolation enforced server-side in TWO layers — canAccessResource(action,userId,ownerId,resourceHouseholdId,requesterMemberHouseholdIds,isServerAdmin) for item access (household level = member-of-the-recipe's-cookbook; NULL household = owner-only) AND buildViewPolicyCondition for list scoping. canAccessHouseholdResource kept unchanged (groceries/calendar/stores items lack household_id; member-overlap is correct). Dedicated DB + tRPC isolation suites, adversarially verified RED-when-weakened on both layers. Found+fixed a latent dedup bug (recipeExistsByUrlForPolicy `!== null` vs drizzle undefined). getRecipeFull/dashboardRecipe/listRecipes select+map householdId (fixed the deferred 49 db failures, a 02-01 prod omission). HOUSE-06 complete. (done)
- Phase 4 (SHARE-01 code-complete): per-recipe sharing visibility built ON the existing recipe_shares feature. `recipes.visibility` pgEnum (private default | household | public) + migration 0038 (backfill private). The no-auth /share/<token> route is gated on visibility=public — `sharedRecipeProcedure` throws an OPAQUE NOT_FOUND for private/household (the single public choke point; no enumeration) and `getPublicRecipeView` mirrors it (defense in depth). The PUBLIC payload is single-recipe-only (PublicRecipeViewSchema; never ids/owner/household/tokenHash/member-lists/other-recipes). shareCreate->public, revoke/delete-last->private (countActiveRecipeShares). shareSetVisibility authedProcedure behind assertRecipeAccess(edit) = owner OR cookbook admin (POLICY-01) — sharing sits ON TOP of the boundary, no cross-cookbook widening (HOUSE-06 intact). Token = randomBytes(32). i18n 11 locales. The new column carried through FullRecipeSchema/getRecipeFull + omitted from RecipeDashboardSchema (no createSelectSchema regression; real-parse tested). Both gates adversarially RED-when-weakened then reverted. SHARE-02 deferred. (code-complete; human-verify pending with the lead)
- Phase 9 (09-01 code-complete): WorkOS auth provider moved from admin-runtime config (phase 08) to ENV / config-as-code. norish's env->provider pattern SEEDS THE DB AT BOOT (seed-config.ts syncOIDC/GitHub/Google read SERVER_CONFIG env -> insert/update-if-differ/delete a non-overridden auth_provider_* row; never touch isOverridden=true), then loadAuthProvidersIntoCache loads DB rows -> the provider cache auth.ts consumes. Mirrored for WorkOS: WORKOS_CLIENT_ID + WORKOS_API_KEY (env-config-server.ts) + a syncWorkOSProvider() (seed-config.ts). PRECEDENCE env-over-DB (isOverridden:false; the UI that could set true is removed). The phase-08 genericOAuth provider/schema/cache/tRPC plumbing UNCHANGED — only the config SOURCE. The WorkOS admin-UI card (auth-providers-card.tsx) was REMOVED (card + tWorkos + authProviderWorkOS + dirtySections.workos); google/github/oidc untouched. WORKOS_API_KEY is sensitive=true (encrypted at rest). (code-complete; HUMAN-VERIFY: lead docker:build + set WORKOS_CLIENT_ID in the live compose + redeploy; owner sets WORKOS_API_KEY)
- Phase 3 (03-01 code-complete): per-cookbook policy stored as 3 `permission_level` pgEnum columns on `households` (view/edit/delete), defaulting to DEFAULT_RECIPE_PERMISSION_POLICY + seeded from the server-wide policy on create (migration 0037, backfilled). The server-wide `recipe_permission_policy` is DEMOTED to default-for-new-cookbooks + the personal-recipe (household_id NULL) fallback — no longer the live gate for cookbook recipes. `canAccessResource` made SYNC + takes a resolved `cookbookPolicy`+`cookbookAdminId` (resolved from the recipe's OWN cookbook via `resolveRecipeCookbookPolicy`); `edit`/`delete = household` => owner OR cookbook admin (admin-edits-any/members-edit-own; no new role system — reuses households.adminUserId). `buildViewPolicyCondition` reads the ACTIVE cookbook's view_policy and NEVER widens cross-cookbook (active cookbook hard-scoped; a seeded `everyone` clamped) — HOUSE-06. Per-cookbook `view = everyone` is DISALLOWED in v1 (SetHouseholdPolicyInputSchema + the repo enforce view ∈ household|owner); only the global default may be everyone. The two getRecipePermissionPolicy impls are reconciled at the GATE (per-cookbook for cookbook recipes); the ~12 emitByPolicy realtime-scope readers stay on the global default (broadcast scope, not a gate). Admin-only Recipe Permissions card on Household settings (members don't see it). Adversarially verified RED-when-weakened at both seams. (code-complete; human-verify pending with the lead)

### Pending Todos

- HUMAN-VERIFY 02-04 + 02-05 + 02-06 (LEAD-owned, PENDING): rebuild norish:local on LXC 110 (docker build), recreate the norishp2 verify stack (applies migration 0036 at boot), and re-verify in Chrome. 02-04: the Cookbook switcher (households + Personal, active marked) on desktop + mobile, per-cookbook recipe-list isolation when switching, the import active-cookbook target, Dutch switcher strings, AND the 02-02 signup own-cookbook + OIDC interaction UX. 02-05: the avatar switcher's "Create or join a cookbook" opens a MODAL (not a settings redirect) → create a 2nd cookbook (appears + becomes active) → join-by-code field present → admin rename in Settings → Household reflects in the switcher. 02-06: Settings → Household → generate an invite link → copy the URL → open /join/<token> → shows the cookbook name → Join → becomes member + active; invalid/revoked token → friendly invalid state; logged-out → /join/<token> → login → returns to the join action; AND migration 0036 applies at boot. Resume signal: "approved" or issues. (see 02-04/02-05/02-06-SUMMARY.md for the exact how-to-verify steps)
- CKBK-UI-01 + RENAME-01: DONE 2026-06-13 (02-05) — create-another/join-by-code reachable from the navbar modal; admin cookbook rename shipped (code-complete; folded into the 02-05 human-verify above).
- INVITE-01: DONE 2026-06-13 (02-06) — shareable regenerable invite link + the public name-only getByInviteToken + the /join/<token> page reusing the multi-membership join path; same security as the join code (code-complete; folded into the 02-06 human-verify above). Registration-bypass spun off as INVITE-02 (Backlog/v2).
- Follow-up (non-blocking, spawned task): fix syncRecipeIngredientsTx unit normalization — 3 pre-existing @norish/db updateRecipeWithRefs tests (create path normalizes units, update path does not; predates Phase 2).
- SHARE-01: DONE 2026-06-14 (Phase 4) — per-recipe visibility private/household/public on the existing recipe_shares; /share/<token> gated on visibility=public (private/household unreachable, adversarially verified); create->public / revoke-last->private; shareSetVisibility behind edit access; randomBytes(32) token; 11-locale i18n; migration 0038; real-parse + isolation tests. Code-complete; HUMAN-VERIFY (Chrome + migration-0038-at-boot) PENDING with the lead. SHARE-02 (save-to-account) deferred to next cycle.
- POLICY-01: DONE 2026-06-14 (03-01) — per-cookbook view/edit/delete policies (permission_level columns + migration 0037; canAccessResource per-cookbook + admin-or-owner; buildViewPolicyCondition source-swap; admin Recipe Permissions card; 11-locale i18n; adversarial isolation + real-parse). Code-complete; HUMAN-VERIFY (Chrome + migration-0037-at-boot) PENDING with the lead.
- UI-FIXES-2 (2026-06-14): green'd the web suite — added the missing `useRecipeRatersQuery` stub to the `@/hooks/ratings` vi.mock in recipe-favorites-doubletap.test.tsx (RATE-01 gap, 2 tests) AND a `@/context/household-context` mock to import-recipe-modal.test.tsx (pre-existing gap from b33327c7 feat-02-04, 4 tests); 379/379 pass, typecheck:web clean. Bug #3 ("cannot press add-recipe") root-caused to connection-status-overlay.tsx (`fixed inset-0 z-[99999]`, NO pointer-events-none) which intercepts all clicks while `status==="disconnected"` (abnormal WS close + 1s retry loop; note the overlay's `"connecting"` branch is dead code — the tRPC provider never sets it). NOT a verify-stack bug: proved the norishp2 WS infra is healthy (direct :3010->:3000 carries the /trpc upgrade, AUTH_URL matches origin, authed cookie => socket opens => overlay dismisses). Real risk is an external self-host proxy that drops the WS upgrade => stuck overlay => unusable UI. Left behavior UNCHANGED per DECISION GUARD (soft-degraded-non-blocking vs fix-WS-reachability is a lead/user call); see /tmp/UI-FIXES-2-SUMMARY.md for the recommendation.
- Phase 5 (AssemblyAI, renumbered from Phase 4) remains unplanned; independent of Phases 2-4.
