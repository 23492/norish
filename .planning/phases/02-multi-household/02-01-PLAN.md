---
phase: 02-multi-household
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema/recipes.ts
  - packages/db/src/schema/auth.ts
  - packages/db/src/schema/relations.ts
  - packages/shared/src/contracts/zod/recipe.ts
  - packages/db/src/migrations/0035_*.sql
  - packages/db/src/migrations/meta/_journal.json
autonomous: true
requirements: [HOUSE-04, HOUSE-05]
user_setup: []

must_haves:
  truths:
    - "recipes table has a nullable household_id column referencing households(id) ON DELETE SET NULL"
    - "user table has a nullable active_household_id column referencing households(id) ON DELETE SET NULL"
    - "The unique constraint on recipes is (url, household_id), not (url, userId)"
    - "A new numbered Drizzle migration exists and the journal references it"
    - "Existing recipes backfill to household_id = NULL (personal)"
  artifacts:
    - path: "packages/db/src/schema/recipes.ts"
      provides: "recipes.household_id column + idx_recipes_household_id + uq_recipes_url_household"
      contains: "household_id"
    - path: "packages/db/src/schema/auth.ts"
      provides: "user.active_household_id column"
      contains: "active_household_id"
    - path: "packages/db/src/schema/relations.ts"
      provides: "recipes.household relation"
      contains: "household: one(households"
    - path: "packages/shared/src/contracts/zod/recipe.ts"
      provides: "householdId in recipe select/full schemas; omitted from insert"
      contains: "householdId"
  key_links:
    - from: "packages/db/src/schema/recipes.ts"
      to: "packages/db/src/schema/households.ts"
      via: "household_id FK references households(id)"
      pattern: "references\\(\\(\\) => households"
    - from: "packages/db/src/migrations/meta/_journal.json"
      to: "packages/db/src/migrations/0035"
      via: "journal entry for the new migration"
      pattern: "0035"
---

<objective>
Add the schema primitives for per-cookbook recipe scoping and active-household selection, then generate the Drizzle migration that auto-applies at boot.

Purpose: Establishes the data model (HOUSE-04 recipe→cookbook scope, HOUSE-05 personal coexists) that every later plan builds on. No behavior change yet — columns are additive and default to the personal (NULL) case.
Output: Updated schema + relations + recipe zod, and a generated `0035_*` migration (+ journal/snapshot) verified to apply cleanly at boot.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-household/02-CONTEXT.md

@packages/db/src/schema/recipes.ts
@packages/db/src/schema/auth.ts
@packages/db/src/schema/households.ts
@packages/db/src/schema/relations.ts
@packages/shared/src/contracts/zod/recipe.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recipes.household_id column, index, and swap the unique constraint</name>
  <files>packages/db/src/schema/recipes.ts</files>
  <read_first>packages/db/src/schema/recipes.ts, packages/db/src/schema/households.ts, packages/db/src/schema/household-users.ts</read_first>
  <action>In the `recipes` pgTable column block, add `householdId: uuid("household_id").references(() => households.id, { onDelete: "set null" })` (nullable, mirroring the style of `userId`). Import `households` from `./households` at the top (match existing import ordering). In the table constraints array `(t) => [...]`: ADD `index("idx_recipes_household_id").on(t.householdId)`; REMOVE `unique("uq_recipes_url_user").on(t.url, t.userId)`; ADD `unique("uq_recipes_url_household").on(t.url, t.householdId)`. Keep all other indexes unchanged. Do NOT remove `idx_recipes_user_id` (userId stays for ownership/orphan logic). NULL household_id rows = personal (Postgres treats NULLs as distinct under the unique constraint, so personal recipes never collide).</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/db typecheck'</verify>
  <acceptance_criteria>
    - recipes.ts contains `household_id` and `references(() => households.id`
    - recipes.ts contains `idx_recipes_household_id` and `uq_recipes_url_household`
    - recipes.ts does NOT contain `uq_recipes_url_user`
    - `import { households } from "./households"` present
  </acceptance_criteria>
  <done>recipes schema has household_id + index + swapped unique constraint, typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: Add user.active_household_id column</name>
  <files>packages/db/src/schema/auth.ts</files>
  <read_first>packages/db/src/schema/auth.ts, packages/db/src/schema/households.ts</read_first>
  <action>In the `users` pgTable, add `activeHouseholdId: text("active_household_id")`-style column BUT typed as uuid referencing households: add `activeHouseholdId: uuid("active_household_id").references(() => households.id, { onDelete: "set null" })` in the Norish-specific fields region (near `isServerAdmin`/`preferences`). Import `uuid` from "drizzle-orm/pg-core" (already imported? verify the import list — add `uuid` if missing) and import `households` from "./households". NOTE: `households.adminUserId` already references `users.id`, creating a mutual reference between `user` and `households` tables; Drizzle handles this with deferred FK resolution at the SQL level — verify `pnpm db:generate` does not error on the cycle (if drizzle-kit complains, keep the column but rely on the generated SQL ALTER TABLE ... ADD CONSTRAINT ordering). Do NOT make it notNull (NULL = personal/active-none).</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/db typecheck'</verify>
  <acceptance_criteria>
    - auth.ts contains `active_household_id` and `references(() => households.id`
    - `uuid` is imported in auth.ts
    - column is nullable (no `.notNull()`)
  </acceptance_criteria>
  <done>user.active_household_id present and typechecks; import cycle resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Add the recipes→household relation and update recipe zod schemas</name>
  <files>packages/db/src/schema/relations.ts, packages/shared/src/contracts/zod/recipe.ts</files>
  <read_first>packages/db/src/schema/relations.ts, packages/shared/src/contracts/zod/recipe.ts</read_first>
  <action>In `relations.ts`, extend `recipesRelations` (currently `relations(recipes, ({ many }) => ({...}))`) to `({ one, many })` and add `household: one(households, { fields: [recipes.householdId], references: [households.id] })`. Optionally extend `householdsRelations` with `recipes: many(recipes)` (only if it reads cleanly — not required). In `packages/shared/src/contracts/zod/recipe.ts`: in `RecipeSelectBaseSchema.extend({...})` (which already re-declares `userId: z.string().nullable()`), add `householdId: z.string().nullable()`. In `RecipeInsertBaseSchema` `.omit({...})` (which omits `userId: true`), also omit `householdId: true` — household is set server-side from the active cookbook, never trusted from client input. `FullRecipeSchema`/`RecipeDashboardSchema` derive from the select base so they inherit `householdId`; verify `RecipeDashboardSchema.omit(...)` does not need it (leave dashboard as-is unless a later plan needs household on the card — it does not for v1).</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/db typecheck && pnpm --filter @norish/shared typecheck'</verify>
  <acceptance_criteria>
    - relations.ts contains `household: one(households`
    - recipe.ts RecipeSelectBaseSchema contains `householdId: z.string().nullable()`
    - recipe.ts RecipeInsertBaseSchema omit block contains `householdId: true`
  </acceptance_criteria>
  <done>relation + zod updated; db and shared typecheck.</done>
</task>

<task type="auto">
  <name>Task 4: Generate the Drizzle migration and verify it applies at boot</name>
  <files>packages/db/src/migrations/0035_*.sql, packages/db/src/migrations/meta/_journal.json, packages/db/src/migrations/meta/0035_snapshot.json</files>
  <read_first>packages/db/src/drizzle.config.ts, packages/db/src/migrations/meta/_journal.json, packages/api/src/startup/migrations.ts</read_first>
  <action>Run `pnpm --filter @norish/db db:generate` (the package's drizzle-kit generate script — confirm the exact script name in packages/db/package.json; it maps to `drizzle-kit generate`). This produces `0035_<name>.sql` containing: ALTER TABLE recipes ADD COLUMN household_id uuid + FK; CREATE INDEX idx_recipes_household_id; DROP CONSTRAINT uq_recipes_url_user; ADD CONSTRAINT uq_recipes_url_household; ALTER TABLE "user" ADD COLUMN active_household_id uuid + FK. Inspect the generated SQL: ensure the DROP of uq_recipes_url_user and ADD of uq_recipes_url_household are present and that ADD COLUMN statements are nullable (no NOT NULL/DEFAULT that would fail). Existing rows get NULL household_id automatically (no backfill statement needed — 0 households, 9 personal recipes). Do NOT hand-edit beyond confirming correctness; if drizzle-kit emits a `--> statement-breakpoint` ordering issue for the mutual FK, that is expected and fine. Verify the journal (`meta/_journal.json`) has the new entry and a `0035_snapshot.json` exists.</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && ls packages/db/src/migrations/0035_*.sql && grep -c "uq_recipes_url_household" packages/db/src/migrations/0035_*.sql && grep -c "active_household_id" packages/db/src/migrations/0035_*.sql'  — then build the image OR run the app once so startup `runMigrations()` applies it against the live DB; confirm dbLogger logs "Migrations complete" and no error.</verify>
  <acceptance_criteria>
    - A file matching `packages/db/src/migrations/0035_*.sql` exists
    - That SQL contains `uq_recipes_url_household`, `idx_recipes_household_id`, `active_household_id`, and `DROP CONSTRAINT` for `uq_recipes_url_user`
    - `meta/_journal.json` contains a `0035` entry; `meta/0035_snapshot.json` exists
    - Boot migration applies with no error (startup logs "Migrations complete")
  </acceptance_criteria>
  <done>Migration generated, journal updated, applies cleanly at boot with all 9 existing recipes at household_id NULL.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @norish/db typecheck` and `pnpm --filter @norish/shared typecheck` pass
- [ ] `pnpm db:generate` produced exactly one new `0035_*` migration + snapshot + journal entry (no uncommitted drift on re-run)
- [ ] Generated SQL swaps the unique constraint and adds both nullable columns
- [ ] Boot `runMigrations()` applies the migration without error against the live DB
- [ ] `pnpm lint` clean on the touched packages
</verification>

<success_criteria>
- recipes.household_id (nullable, ON DELETE SET NULL) + idx_recipes_household_id + uq_recipes_url_household present
- user.active_household_id (nullable, ON DELETE SET NULL) present
- recipes→household relation + recipe zod (householdId in select/full, omitted from insert)
- One generated migration that auto-applies at boot; existing recipes default to personal (NULL)
- No behavior change to running app beyond the additive columns
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-household/02-01-SUMMARY.md`
</output>
