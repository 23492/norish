---
phase: 02-multi-household
plan: 04
type: execute
wave: 4
depends_on: ["02-02"]
files_modified:
  - packages/shared-react/src/contexts/households/household-context.tsx
  - packages/shared-react/src/hooks/households/types.ts
  - packages/shared-react/src/hooks/households/index.ts
  - packages/shared-react/src/hooks/households/use-household-query.ts
  - packages/shared-react/src/hooks/households/use-household-mutations.ts
  - packages/shared-react/src/hooks/households/use-household-subscription.ts
  - apps/web/context/household-context.tsx
  - apps/web/hooks/households/use-household-mutations.ts
  - apps/web/components/navbar/navbar-user-menu.tsx
  - apps/web/components/navbar/mobile-nav.tsx
  - apps/web/components/shared/import-recipe-modal.tsx
  - apps/web/context/recipes-context.tsx
  - packages/i18n/src/messages/en/navbar.json
  - packages/i18n/src/messages/en/settings.json
  - "packages/i18n/src/messages/(all 11 locales)/navbar.json + settings.json"
autonomous: false
requirements: [HOUSE-01, HOUSE-02, HOUSE-03, HOUSE-07]
user_setup: []

must_haves:
  truths:
    - "The navbar shows a cookbook switcher listing all the user's households plus a Personal option"
    - "Selecting a different cookbook calls households.switchActive and the recipe list refetches to that cookbook"
    - "A user can create and join additional households from the UI without an 'already in a household' error"
    - "The import flow assigns the imported recipe to the active cookbook (and shows which cookbook)"
    - "pnpm i18n:check passes — every new key exists in all 11 locales"
  artifacts:
    - path: "packages/shared-react/src/contexts/households/household-context.tsx"
      provides: "households list + activeHouseholdId + switchActive in context value"
      contains: "switchActive"
    - path: "apps/web/components/navbar/navbar-user-menu.tsx"
      provides: "cookbook switcher UI"
      contains: "switchActive"
    - path: "packages/i18n/src/messages/nl/navbar.json"
      provides: "Dutch switcher strings"
      contains: "cookbook"
  key_links:
    - from: "apps/web/components/navbar/navbar-user-menu.tsx"
      to: "packages/trpc/src/routers/households/households.ts"
      via: "switcher calls households.list + households.switchActive via hooks"
      pattern: "switchActive|list"
    - from: "apps/web/context/recipes-context.tsx"
      to: "packages/trpc/src/routers/households/households.ts"
      via: "recipe list invalidated on active-household switch"
      pattern: "invalidate|refetch"
---

<objective>
Build the household/cookbook switcher and assign-to-cookbook UX on the web app by extending the existing shared-react factory hooks/contexts, and add the i18n keys across all 11 locales so the CI gate passes.

Purpose: Delivers the user-facing side of HOUSE-01/02/03/07 — switch active cookbook, create/join more, import into the active one.
Output: Extended shared-react household context + hooks (list/active/switch), a navbar switcher (desktop + mobile) with a Personal option, import-modal cookbook indication, recipe-list refetch on switch, and complete navbar/settings i18n in da, de-formal, de-informal, en, es, fr, it, ko, nl, pl, ru.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
@/opt/norish-src/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-household/02-CONTEXT.md
@.planning/phases/02-multi-household/02-02-SUMMARY.md

@packages/shared-react/src/contexts/households/household-context.tsx
@packages/shared-react/src/hooks/households/types.ts
@packages/shared-react/src/hooks/households/index.ts
@apps/web/context/household-context.tsx
@apps/web/components/navbar/navbar-user-menu.tsx
@apps/web/components/shared/import-recipe-modal.tsx
@packages/i18n/scripts/check-locale-keys.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared-react household hooks for list + active + switchActive</name>
  <files>packages/shared-react/src/hooks/households/types.ts, packages/shared-react/src/hooks/households/use-household-query.ts, packages/shared-react/src/hooks/households/use-household-mutations.ts, packages/shared-react/src/hooks/households/index.ts, apps/web/hooks/households/use-household-mutations.ts</files>
  <read_first>packages/shared-react/src/hooks/households/types.ts, packages/shared-react/src/hooks/households/use-household-query.ts, packages/shared-react/src/hooks/households/use-household-mutations.ts, packages/trpc/src/routers/households/households.ts</read_first>
  <action>Add a `useHouseholdsListQuery` (or extend the query factory) that calls the new `households.list` tRPC procedure and returns `{ households: HouseholdSummaryDto[], activeHouseholdId: string | null, currentUserId, isLoading, invalidate }`. Add a `switchActive(householdId: string | null)` mutation to `createUseHouseholdMutations` that calls `households.switchActive` and on success invalidates the household list query AND the recipe list query (so the dashboard refetches). Update `HouseholdMutationsResult` type (and `HouseholdQueryResult`/new list result type) in types.ts. Keep the existing `createHousehold`/`joinHousehold`/`leaveHousehold`/`kickUser`/`regenerateJoinCode`/`transferAdmin` intact. Export the new hook/types from index.ts. Wire the web `use-household-mutations.ts` wrapper to expose `switchActive`.</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/shared-react typecheck && pnpm --filter @norish/web typecheck'</verify>
  <acceptance_criteria>
    - shared-react households hooks export a list query (households.list) and a switchActive mutation
    - HouseholdMutationsResult includes switchActive
    - web use-household-mutations re-exports switchActive
  </acceptance_criteria>
  <done>Hooks expose households list + active id + switchActive, refetching recipes on switch.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the household context value with households/activeHouseholdId/switchActive</name>
  <files>packages/shared-react/src/contexts/households/household-context.tsx, apps/web/context/household-context.tsx</files>
  <read_first>packages/shared-react/src/contexts/households/household-context.tsx, apps/web/context/household-context.tsx, apps/web/hooks/households/index.ts</read_first>
  <action>Extend `HouseholdContextValue` with `households: HouseholdSummaryDto[]`, `activeHouseholdId: string | null`, and `switchActive: (householdId: string | null) => void`. In `createHouseholdContext`, consume the list query (add a `useHouseholdsListQuery` option to the factory) and the switchActive mutation; populate the new fields in the memoized value. Keep the existing `household`/`currentUserId`/`isLoading` (active household) fields for backward compatibility. Update the web `createHouseholdContext({...})` call to pass the new hooks. Ensure the `HouseholdSettingsContextValue` still works (it composes the base context).</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/shared-react typecheck && pnpm --filter @norish/web typecheck'</verify>
  <acceptance_criteria>
    - HouseholdContextValue contains `households`, `activeHouseholdId`, `switchActive`
    - web household-context passes the list + switch hooks into the factory
  </acceptance_criteria>
  <done>Context exposes the full multi-household surface to components.</done>
</task>

<task type="auto">
  <name>Task 3: Cookbook switcher UI in navbar (desktop + mobile) with a Personal option</name>
  <files>apps/web/components/navbar/navbar-user-menu.tsx, apps/web/components/navbar/mobile-nav.tsx</files>
  <read_first>apps/web/components/navbar/navbar-user-menu.tsx, apps/web/components/navbar/mobile-nav.tsx, apps/web/context/household-context.tsx</read_first>
  <action>Add a cookbook switcher using the heroui Dropdown pattern already present in navbar-user-menu.tsx. Render the list from `useHouseholdContext().households` plus a "Personal" entry (value null). Mark the `activeHouseholdId` item as selected (check/active styling). On select, call `switchActive(id | null)`. Place it in the user menu (a labeled section "Cookbook" with the list) and mirror in mobile-nav.tsx. Use `useTranslations("navbar")` for all visible strings (keys added in Task 5) — no hardcoded English. If the user has 0 households, still show "Personal" (selected) and the existing Create/Join entry. Keep the existing menu items (settings, import, sign out) intact.</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/web typecheck && pnpm --filter @norish/web lint'</verify>
  <acceptance_criteria>
    - navbar-user-menu.tsx renders households + a Personal option and calls switchActive
    - mobile-nav.tsx has the same switcher
    - all switcher strings use useTranslations (no hardcoded English literals in the switcher JSX)
  </acceptance_criteria>
  <done>Switcher works on desktop + mobile, including the Personal option, fully localized.</done>
</task>

<task type="auto">
  <name>Task 4: Assign-to-cookbook indication in the import flow + recipe-list refetch on switch</name>
  <files>apps/web/components/shared/import-recipe-modal.tsx, apps/web/context/recipes-context.tsx</files>
  <read_first>apps/web/components/shared/import-recipe-modal.tsx, apps/web/context/recipes-context.tsx, apps/web/context/household-context.tsx</read_first>
  <action>Since the backend assigns imports to the ACTIVE cookbook automatically (02-02), the import modal does not need a picker for v1 — instead surface WHICH cookbook the recipe will be added to: show the active cookbook name (from `useHouseholdContext().activeHouseholdId` → resolve name from `households`, or "Personal" when null) as helper text near the import button (localized). In `recipes-context.tsx`: ensure the recipe list query is invalidated/refetched when the active household changes — the cleanest hook is to invalidate the recipe list inside `switchActive` (done in Task 1); ALSO confirm the recipes-context query key does not need the active household appended (if the server scopes by session/active, the same key is fine and an invalidate suffices). If a per-cookbook picker is trivially supported by the existing modal, it MAY be added, but the v1 requirement is only assign-to-active + visible indication.</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm --filter @norish/web typecheck'</verify>
  <acceptance_criteria>
    - import-recipe-modal.tsx shows the active cookbook name/Personal (localized) as the import target
    - switching active household invalidates/refetches the recipe list (grep invalidate/refetch tied to switchActive or active-household change)
  </acceptance_criteria>
  <done>Imports clearly target the active cookbook and the dashboard refetches on switch.</done>
</task>

<task type="auto">
  <name>Task 5: i18n keys for the switcher/cookbook UX across ALL 11 locales</name>
  <files>packages/i18n/src/messages/en/navbar.json, packages/i18n/src/messages/en/settings.json, and the matching navbar.json/settings.json in da, de-formal, de-informal, es, fr, it, ko, nl, pl, ru</files>
  <read_first>packages/i18n/src/messages/en/navbar.json, packages/i18n/src/messages/en/settings.json, packages/i18n/scripts/check-locale-keys.js</read_first>
  <action>Add the new keys to the `en` source first (e.g. under navbar: `cookbook.label`, `cookbook.personal`, `cookbook.switchTo`, `cookbook.active`, `cookbook.createNew`, `cookbook.join`; reuse existing settings household keys where possible). Then add the SAME key paths to ALL 10 other locales (da, de-formal, de-informal, es, fr, it, ko, nl, pl, ru) — the gate `check-locale-keys.js` uses `en` as source of truth and FAILS on any missing key in any locale. Provide real translations for nl (Dutch) and en (canonical); for the other 9 locales provide best-effort translations consistent with the existing tone in those files (do NOT leave English placeholders that would read wrong, but a sensible localized string is required for parity — match how existing household keys are translated in each file). Do NOT introduce keys in only some locales (that triggers "extra"/"missing" mismatches). Keep JSON valid + 2-space indented to match the repo.</action>
  <verify>ssh ... 'cd /opt/norish-src && export PATH=/usr/local/bin:$PATH && pnpm i18n:check'</verify>
  <acceptance_criteria>
    - `pnpm i18n:check` exits 0 (no missing keys in any locale)
    - en + nl navbar.json contain the new cookbook.* keys with real translations
    - all 11 locales have identical key sets for the touched namespaces
  </acceptance_criteria>
  <done>i18n parity gate passes with the new cookbook strings in all 11 locales.</done>
</task>

<task type="auto">
  <name>Start dev server for visual verification</name>
  <action>Build/run the web app (or the docker stack) so the navbar switcher is reachable in a browser against the live DB on LXC 110. Confirm the app boots (migration applied from 02-01) and the dashboard loads.</action>
  <verify>The app responds on its configured port and the dashboard renders without console errors.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Multi-household cookbook switcher in the navbar (desktop + mobile), assign-to-active-cookbook import, all 11 locales updated.</what-built>
  <how-to-verify>Open the app. In the user menu, confirm a Cookbook switcher listing your households + a "Personal" option, with the active one marked. Create a second household, then join/switch between them and the Personal cookbook — the recipe list should change per cookbook and you must NOT see another cookbook's recipes. Import a recipe and confirm it shows it will be added to the active cookbook and appears only there. Switch the app language to Dutch and confirm the switcher strings are translated.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter @norish/web typecheck` + `pnpm --filter @norish/shared-react typecheck` pass
- [ ] `pnpm i18n:check` exits 0 (all 11 locales complete)
- [ ] `pnpm --filter @norish/web lint` clean
- [ ] Human visual verification approved (switcher + per-cookbook isolation visible + Dutch strings)
</verification>

<success_criteria>
- Cookbook switcher (desktop + mobile) lists households + Personal, marks active, and switches (HOUSE-03)
- Create/join additional households from the UI with no single-household error (HOUSE-01/02)
- Import assigns to + indicates the active cookbook; dashboard refetches on switch (HOUSE-07)
- i18n:check passes with new keys present in all 11 locales (nl + en real, others localized)
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-household/02-04-SUMMARY.md`
</output>
