---
phase: 10-workos-only-auth
plan: 10-01
subsystem: auth
tags: [auth, better-auth, workos, oauth2, sso-only, auto-redirect, login, signup, password-auth, i18n, vitest]
requirements: [WORKOS-ONLY-01]
---

# Phase 10 Plan 10-01: WorkOS as the sole sign-in path — disable norish-only auth + auto-redirect to AuthKit

<context>
Phase 08 shipped WorkOS AuthKit (better-auth genericOAuth, providerId `workos`, `api.workos.com/user_management`, `authorizationUrlParams:{provider:'authkit'}`). Phase 09 moved its config to env (config-as-code). Password auth is currently ON (`PASSWORD_AUTH_ENABLED=true`). Owner now wants WorkOS to be the ONLY sign-in/sign-up path: (a) no norish email/password accounts, and (b) the unauthenticated entry should jump STRAIGHT to the WorkOS hosted login (Google etc.), never showing norish's own login UI.

KEY INVESTIGATION FINDING — most of this already exists. `apps/web/app/(auth)/login/page.tsx` already computed a sole-OAuth-provider auto-redirect (`oauthProviders.length === 1 && !hasCredential && !justLoggedOut`) and `login-client.tsx` already renders `<AutoSignIn>` which already calls `signIn.oauth2({providerId})` for non-google/github providers (i.e. WorkOS). `signup/page.tsx` already `redirect("/login")` when `!passwordEnabled`, so disabling password auth already removes the norish-only signup. Loop-safety already exists: `apps/web/proxy.ts` excludes `/login`, `/signup`, `/auth-error`, `/api/auth` from the auth matcher, and the OAuth flow uses `errorCallbackURL:"/auth-error"`. `PASSWORD_AUTH_ENABLED` already defaults to `!hasOAuthEnvConfigured()` (so it defaults OFF when WorkOS env is set).

SCOPE: (1) make the sole-provider auto-redirect decision a TESTABLE pure helper; (2) add the explicit `?sso=0` recovery escape the task requires (the existing escape was only `?logout=true`); (3) surface a visible escape link on the redirect spinner; (4) tests. The actual FLIP is a config change (`PASSWORD_AUTH_ENABLED=false`, owned by the lead at deploy). Do NOT change the genericOAuth provider, the proxy matcher, the better-auth callback, or any live container. Do NOT `pnpm docker:build`, merge to main, or deploy.
</context>

<interfaces>
- packages/auth/src/providers.ts: NEW pure `shouldAutoRedirectToSso(providers: ProviderInfo[], escapeRequested = false): boolean` — returns `oauthProviders.length === 1 && !hasCredential && !escapeRequested`. Conservative + self-recovering: false when password on, when >1 provider, when 0 providers, or when escape requested.
- apps/web/app/(auth)/login/page.tsx: import + call the helper; add `sso?: string` to searchParams; `escapeAutoRedirect = sso === "0" || logout === "true"`.
- apps/web/app/(auth)/signup/page.tsx: add `sso?: string`; forward `?sso=0` through the `redirect("/login")` so the escape survives.
- apps/web/app/(auth)/login/components/auto-sign-in.tsx: add a `next/link` escape to `/login?sso=0` using a new i18n key.
- packages/i18n/src/messages/*/auth.json (all 11 locales): NEW `login.useAnotherMethod` (key parity required by check:locale-keys; en/nl/de/fr/es/it translated, da/ko/pl/ru English fallback).
- packages/auth/__tests__/auth/sso-auto-redirect.test.ts: NEW — sole-provider→redirect; password-on→no; no-provider→no; >1-provider→no; `?sso=0`/escape→no.

NO new dependency, NO schema/migration, NO provider-logic change, NO proxy/middleware change.
</interfaces>

<task type="auto" tdd="false">
**Task 1: testable sole-provider auto-redirect helper + ?sso=0 escape** — providers.ts (shouldAutoRedirectToSso) + login/page.tsx + signup/page.tsx. redit.py; confirm with git diff.
<acceptance_criteria>
- `pnpm --filter @norish/auth typecheck` + `@norish/web typecheck` EXIT 0.
- `grep -n shouldAutoRedirectToSso packages/auth/src/providers.ts apps/web/app/(auth)/login/page.tsx` shows the helper + its use.
</acceptance_criteria>
</task>

<task type="auto" tdd="false">
**Task 2: visible recovery escape link** — auto-sign-in.tsx (Link to /login?sso=0) + `login.useAnotherMethod` in all 11 locales.
<acceptance_criteria>
- `pnpm i18n:check` EXIT 0.
- `pnpm --filter @norish/web typecheck` EXIT 0.
</acceptance_criteria>
</task>

<task type="auto" tdd="false">
**Task 3: redirect-condition test** — sso-auto-redirect.test.ts.
<acceptance_criteria>
- `pnpm --filter @norish/auth exec vitest run __tests__/auth/sso-auto-redirect.test.ts` all pass.
- Full @norish/auth suite no NEW failures.
</acceptance_criteria>
</task>

<verification>
## Plan-level verification (static + unit only — LEAD owns docker:build + Chrome e2e)
- typecheck auth + web EXIT 0; i18n:check EXIT 0; lint clean on touched files.
- @norish/auth full suite green incl. the new sso-auto-redirect (8 tests).
- Do NOT run `pnpm docker:build`. Do NOT enter any real WorkOS key.

## Success criteria (WORKOS-ONLY-01)
- With WorkOS as the sole provider (password off + no other social/OIDC), the unauthenticated login page auto-redirects to the WorkOS AuthKit page; the norish login UI is never shown.
- No norish email/password login or signup is reachable when password is off (signup redirects to login; no credential form).
- CONDITIONAL + recoverable: if password is re-enabled OR WorkOS is unset, the normal login page returns (no lockout). `?sso=0` always shows the normal login page. No redirect loop (proxy excludes login/signup/auth-error/api-auth; errors land on /auth-error with a back link).
</verification>
