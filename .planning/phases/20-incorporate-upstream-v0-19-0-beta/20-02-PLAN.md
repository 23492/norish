---
phase: 20-incorporate-upstream-v0-19-0-beta
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - packages/api/src/ai/image-recipe-parser.ts
  - packages/api/src/ai/recipe-parser.ts
  - packages/api/src/video/normalizer.ts
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "The api/parser conflicts are resolved keeping upstream's module-path updates AND the fork's locale/language threading"
    - "Scraping still goes through native Camoufox — no playwright/chrome-headless reference anywhere in packages/api/src"
    - "The @norish/api test suite passes (locale/language threading tests intact)"
    - "api typecheck is clean against the new @norish/db-schema shims and the moved server-config-loader path"
  artifacts:
    - path: "packages/api/src/ai/recipe-parser.ts"
      provides: "HTML recipe parser with upstream import paths + fork locale threading"
      contains: "getDefaultLocale"
    - path: "packages/api/src/video/normalizer.ts"
      provides: "Video normalizer with fork targetLanguage threading preserved"
      contains: "targetLanguage"
  key_links:
    - from: "packages/api/src/parser/fetch.ts"
      to: "packages/api/src/camofox.ts"
      via: "camofox import (asserted in 20-01, re-checked here)"
      pattern: "camofox"
    - from: "packages/api/src/ai/recipe-parser.ts"
      to: "@norish/shared-server/config/server-config-loader"
      via: "moved server-config-loader import"
      pattern: "shared-server/config/server-config-loader"
---

<objective>
Resolve the api/parser subsystem conflicts (D-03 second in order, depends on the db-schema shims from 20-01). Three content conflicts in `packages/api/src/ai` + `packages/api/src/video` follow the same pattern: upstream moved the `server-config-loader` import to `@norish/shared-server/config/server-config-loader` and made refactors, while the fork added locale-aware extraction (Phase 7 LOCALE-01 — `targetLanguage`/`getDefaultLocale` threading). Both sides must win. (Per Strategy B, the 20-01 merge already committed these files on the UPSTREAM side — the fork's locale delta was dropped pending this plan, so there are NO live conflict markers; this plan RE-APPLIES the fork delta on top.) The Camoufox hard constraint (re-asserted in 20-01) is re-verified here as the api subsystem's blast-radius check.

Purpose: Adopt upstream's 0.19.0 api refactor + module-boundary import moves while preserving the fork's locale-aware recipe extraction, keeping scraping firmly on the native Camoufox path.
Output: The three api/parser conflicts resolved and committed; `@norish/api` typecheck + test green; Camoufox proof re-confirmed.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-CONTEXT.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-01-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
NEW symbols this plan creates: none (this is conflict resolution only). It RESOLVES the three api/parser content conflicts (`ai/image-recipe-parser.ts`, `ai/recipe-parser.ts`, `video/normalizer.ts`) on top of the merged tree, preserving the fork's `buildLanguageInstruction`/`targetLanguage`/`getDefaultLocale` locale-threading and adopting upstream's moved `@norish/shared-server/config/server-config-loader` import path.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the three api/parser content conflicts (upstream import paths + fork locale threading)</name>
  <files>packages/api/src/ai/image-recipe-parser.ts, packages/api/src/ai/recipe-parser.ts, packages/api/src/video/normalizer.ts</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§1 Subsystem 2 api/parser table; §6 module-boundary refactor; §4a Camoufox)
    - packages/api/src/ai/image-recipe-parser.ts (conflict: upstream server-config-loader move + fork locale/language threading)
    - packages/api/src/ai/recipe-parser.ts (conflict: same pattern; fork getDefaultLocale threading)
    - packages/api/src/video/normalizer.ts (conflict: upstream changes + fork targetLanguage threading)
    - packages/api/src/ai/prompts/fragments/language.ts (fork's buildLanguageInstruction/localeToLanguageName — the locale logic that must survive)
    - packages/api/src/parser/fetch.ts (already resolved in 20-01; re-check no playwright reference)
  </read_first>
  <action>
    Per Strategy B, the 20-01 merge already committed these three api/parser files on the UPSTREAM side (the fork's locale delta was dropped pending this plan) — there are NO live conflict markers. RE-APPLY the fork's locale delta onto that committed-upstream base, reconstructing the both-sides-win result: recover the fork delta from the tagged pre-merge tip (`git show pre-0.19.0-integration:packages/api/src/ai/recipe-parser.ts`, …, or `git diff 6af3670a..pre-0.19.0-integration -- packages/api/src/ai packages/api/src/video`) and layer it onto the current (upstream) files. Concretely (per D-08 favor-upstream for the refactor surface, re-apply the fork delta where it actually changed): for `packages/api/src/ai/image-recipe-parser.ts` and `packages/api/src/ai/recipe-parser.ts`, take upstream's moved import path `@norish/shared-server/config/server-config-loader` (not the deleted `@norish/config/server-config-loader`) and upstream's refactor, then re-apply the fork's locale threading — the `targetLanguage`/`await getDefaultLocale()` call sites and the `buildLanguageInstruction(targetLanguage)` fragment usage from Phase 7. For `packages/api/src/video/normalizer.ts`, perform a 3-way merge taking BOTH upstream's changes and the fork's `targetLanguage = metadata.language || (await getDefaultLocale())` threading. Do NOT alter `packages/api/src/ai/prompts/fragments/language.ts` (it is fork-only and not in conflict). After resolving, re-sync the hardlink twin: `cp -a packages/api/src/. node_modules/@norish/api/src/`. Re-verify the Camoufox constraint is still intact on this tree (no playwright reintroduced by these edits). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "packages/api/src/(ai|video)/" | wc -l` returns `0` (all three api/parser conflicts resolved).
    - `grep -c "getDefaultLocale" packages/api/src/ai/recipe-parser.ts` returns >= 1 (fork locale threading preserved).
    - `grep -c "targetLanguage" packages/api/src/video/normalizer.ts` returns >= 1 (fork video locale threading preserved).
    - `grep -c "@norish/config/server-config-loader" packages/api/src/ai/recipe-parser.ts packages/api/src/ai/image-recipe-parser.ts` returns `0` (old deleted path gone).
    - Camoufox re-confirmed: `grep -rE "playwright|chrome-headless|CHROME_WS_ENDPOINT" packages/api/src --include="*.ts" | wc -l` returns `0`; `ls packages/api/src/camofox.ts` succeeds; `ls packages/api/src/playwright.ts 2>/dev/null && echo FAIL || echo PASS` prints `PASS`.
  </acceptance_criteria>
  <verify>
    <automated>! ( git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -qE "packages/api/src/(ai|video)/" ) && grep -q "getDefaultLocale" packages/api/src/ai/recipe-parser.ts && grep -q "targetLanguage" packages/api/src/video/normalizer.ts && ( grep -rE "playwright|chrome-headless" packages/api/src --include="*.ts" | grep -q . && exit 1 || echo OK )</automated>
  </verify>
  <done>All three api/parser conflicts carry no unresolved markers; the fork's locale/language threading and Camoufox path survive; upstream's moved server-config-loader import is adopted.</done>
</task>

<task type="auto">
  <name>Task 2: Typecheck + test @norish/api on the resolved tree and commit the api subsystem resolution</name>
  <files>packages/api/ (typecheck/test only; no source edits beyond any fork-only test import fixup noted in the SUMMARY)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (Validation Architecture per-plan gate for 20-02; §6 module-boundary; baseline api test count)
    - packages/api/__tests__/server/ai/ (the locale/language builder tests that must stay green)
    - CLAUDE.md (do NOT run docker:build — director-owned)
  </read_first>
  <action>
    Run `pnpm --filter @norish/api typecheck` against the resolved tree (it now consumes the `@norish/db-schema` shims from 20-01 and the moved `@norish/shared-server/config/server-config-loader` path) and fix any import-resolution errors introduced by the boundary move — but ONLY in the three resolved api files plus any fork-only api test using the old `@norish/config/server-config-loader` path that surfaces here (e.g. `packages/api/__tests__/server/ai/transcriber-assemblyai.test.ts` import fixup if it fails typecheck; if it is cleaner to defer that single test fixup to plan 20-05's import-fixup task, note it in the SUMMARY rather than touching unrelated files). Run `pnpm --filter @norish/api test` and confirm the suite passes (locale/language builder tests + AssemblyAI provider tests green). Re-run the Camoufox grep proof as a regression gate. Commit the api/parser subsystem resolution (`feat(20-02): ...`). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/api typecheck` exits 0.
    - `pnpm --filter @norish/api test` exits 0 with the suite passing (no regression in the locale/language builder tests).
    - `grep -rE "playwright|chrome-headless|CHROME_WS_ENDPOINT" packages/api/src packages/api/package.json --include="*.ts" --include="*.json" | wc -l` returns `0`.
    - `git status --porcelain` shows no remaining conflict markers in `packages/api/` and the api subsystem resolution is committed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/api typecheck && pnpm --filter @norish/api test && ( grep -rE "playwright|chrome-headless" packages/api/src packages/api/package.json --include="*.ts" --include="*.json" | grep -q . && exit 1 || echo OK )</automated>
  </verify>
  <done>`@norish/api` typecheck + test are green on the merged+resolved tree, the Camoufox constraint holds, and the api/parser subsystem resolution is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream api refactor → fork parser | Upstream's reintroduced playwright path + module-boundary moves cross into the fork's Camoufox-based parser. |
| external recipe source → scraper | Untrusted remote HTML/video fetched through the scraper; must stay on the audited Camoufox REST client, not a reintroduced headless Chrome. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-PW | Tampering (supply chain) | playwright/chrome-headless reintroduction via the api refactor | mitigate | Re-run the grep proof (`grep -rE "playwright\|chrome-headless" packages/api/src` → zero) in both tasks; fetch.ts stays on camofox.ts (asserted 20-01). |
| T-20-02-CFG | Tampering | moved server-config-loader import resolving to a stale/deleted module | mitigate | Adopt upstream `@norish/shared-server/config/server-config-loader`; gate via `pnpm --filter @norish/api typecheck` (Task 2). |
| T-20-02-LOC | Information Disclosure (minor) | locale threading dropped → wrong-language extraction | accept | Low security risk; preserved for correctness via `grep getDefaultLocale/targetLanguage` + api test suite, not a security gate. |
</threat_model>

<verification>
- All three api/parser conflicts resolved with no markers; fork locale threading + Camoufox path preserved.
- `pnpm --filter @norish/api typecheck` + `pnpm --filter @norish/api test` green.
- Camoufox grep proof returns zero playwright/chrome references.
- DIRECTOR NOTE (not an executor gate): the director may run `pnpm docker:build` after this plan's gates pass; the build is never a task acceptance criterion.
</verification>

<success_criteria>
- api/parser subsystem conflicts resolved keeping upstream import moves + fork locale threading.
- Scraping stays native Camoufox; no playwright/chrome reference in packages/api/src.
- `@norish/api` typecheck + test green; resolution committed.
</success_criteria>

<output>
Create `.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-02-SUMMARY.md` when done.
</output>
