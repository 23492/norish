---
phase: 20-incorporate-upstream-v0-19-0-beta
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - packages/trpc/src/middleware.ts
  - packages/trpc/src/routers/households/households.ts
  - packages/trpc/src/routers/recipes/recipes.ts
  - packages/trpc/src/routers/recipes/shares.ts
  - packages/trpc/src/routers/recipes/helpers.ts
  - packages/shared-react/src/hooks/households/types.ts
  - packages/shared-react/src/hooks/households/use-household-mutations.ts
  - packages/shared-react/src/hooks/ratings/index.ts
  - packages/shared-react/src/contexts/households/household-context.tsx
  - packages/shared-server/src/cache/household.ts
  - packages/shared/src/contracts/zod/household.ts
  - packages/shared/src/contracts/zod/recipe.ts
  - packages/trpc/__tests__/archive/archive-import-validation.test.ts
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "auth.ts / permissions.ts / claim-processor.ts auto-merged result preserves WorkOS + multi-household + per-cookbook permissions"
    - "The 13 auth/trpc/shared-react/shared/shared-server conflicts are resolved keeping upstream module-path moves AND fork multi-household/sharing/ratings features"
    - "@norish/auth tests pass; @norish/trpc tests (incl. permissions-integration + raters isolation) pass under sg docker"
    - "The per-cookbook access boundary at the tRPC layer fails the isolation suites when adversarially weakened, then is reverted byte-identical (weakening never committed)"
  artifacts:
    - path: "packages/trpc/src/middleware.ts"
      provides: "withAuth ctx with memberHouseholdIds + getHouseholdsForUser, on upstream's moved cache/redis imports"
      contains: "memberHouseholdIds"
    - path: "packages/shared-server/src/cache/household.ts"
      provides: "moved cached-household helper (upstream location accepted)"
      contains: "getCachedHouseholdForUser"
  key_links:
    - from: "packages/trpc/src/middleware.ts"
      to: "@norish/shared-server/cache/household"
      via: "moved getCachedHouseholdForUser import"
      pattern: "shared-server/cache/household"
    - from: "packages/trpc/src/routers/recipes/shares.ts"
      to: "assertRecipeAccess"
      via: "edit-access gate on shareSetVisibility"
      pattern: "assertRecipeAccess"
---

<objective>
Resolve the auth / permissions / trpc subsystem (D-03 third in order). The three core auth files (`auth.ts`, `permissions.ts`, `claim-processor.ts`) AUTO-MERGE cleanly (research §4d) and already preserve WorkOS + multi-household + per-cookbook permissions — this plan verifies that and resolves the 13 surrounding conflicts in the packages that wrap/consume auth (`trpc` routers/middleware, `shared-react` hooks/contexts, `shared` zod contracts, `shared-server/cache/household`). Every conflict follows one pattern: take upstream's refactored base + moved module-boundary import paths, re-apply the fork's multi-household / sharing / ratings additions. This is the second security-critical plan: the per-cookbook access boundary at the tRPC layer is adversarially verified here.

Purpose: Adopt upstream's 0.19.0 auth-adjacent refactor + the `@norish/shared-server` module-boundary consolidation while preserving HOUSE-06 isolation, WorkOS, multi-household, and per-recipe sharing/visibility — proven green and adversarially red-when-weakened.
Output: All 13 conflicts resolved; auth + trpc suites (incl. permissions-integration + raters isolation) green under `sg docker`; adversarial check reverted; subsystem resolution committed.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-CONTEXT.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
NEW symbols this plan creates: none. It RESOLVES 13 conflicts and accepts the moved file `packages/shared-server/src/cache/household.ts` (upstream's new home for the old `@norish/db/cached-household`). It preserves: `withAuth` ctx fields `memberHouseholdIds` + `getHouseholdsForUser` (trpc middleware); the fork's rename/invite household mutations + visibility/ratings router additions; `useRecipeRatersQuery` (shared-react ratings); the inviteToken/policy zod types (shared/household) and visibility zod types (shared/recipe); and the auto-merged WorkOS/multi-household/permissions logic in the three core auth files.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Verify the auto-merged core auth files preserve fork features (WorkOS / multi-household / permissions)</name>
  <files>packages/auth/src/auth.ts, packages/auth/src/permissions.ts, packages/auth/src/claim-processor.ts (verify-only unless an auto-merge dropped a fork fn)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§4d WorkOS+multi-household+permissions; §6 module-boundary moves; §4b isolation suites)
    - packages/auth/src/auth.ts (auto-merged; must still contain buildWorkOSProviders/addUserToHousehold/setActiveHousehold)
    - packages/auth/src/permissions.ts (auto-merged; must still contain canAccessResource/resolveRecipeCookbookPolicy/buildViewPolicyCondition)
    - packages/auth/src/claim-processor.ts (auto-merged; now imports from @norish/shared-server/realtime + cache)
  </read_first>
  <action>
    These three files auto-merged in the 20-01 `git merge` (no conflict markers). Verify — do NOT rewrite — that the auto-merged result preserved every fork addition: `buildWorkOSProviders`, `addUserToHousehold`, `setActiveHousehold` in `auth.ts`; `canAccessResource`, `resolveRecipeCookbookPolicy`, `buildViewPolicyCondition` in `permissions.ts`; and that `claim-processor.ts` now imports `HouseholdUserInfo`/`invalidateHouseholdCacheForUsers` from the new `@norish/shared-server/realtime/households` + `@norish/shared-server/cache/household` paths (upstream's module-boundary move). If any fork function is missing from the auto-merge (it should not be, per research), restore it from the fork side via the merge's stage-2 (`git show :2:<file>`) rather than rewriting. Run `pnpm --filter @norish/auth test`; the auth suite (incl. workos-provider + permissions) must pass. Note the 4 fork-only files that still import the deleted `@norish/config/server-config-loader` (research §4d) but do NOT fix them here — they are owned by plan 20-05's import-fixup task; record them in the SUMMARY. Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `grep -E "buildWorkOSProviders|addUserToHousehold|setActiveHousehold" packages/auth/src/auth.ts | wc -l` returns >= 3 (all present).
    - `grep -E "canAccessResource|resolveRecipeCookbookPolicy|buildViewPolicyCondition" packages/auth/src/permissions.ts | wc -l` returns >= 3.
    - `grep -c "shared-server/realtime\|shared-server/cache" packages/auth/src/claim-processor.ts` returns >= 1 (upstream module-boundary path adopted).
    - `pnpm --filter @norish/auth test` exits 0.
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "packages/auth/src/" | wc -l` returns `0` (the three core auth files were not in conflict and remain clean).
  </acceptance_criteria>
  <verify>
    <automated>grep -qE "buildWorkOSProviders" packages/auth/src/auth.ts && grep -qE "canAccessResource" packages/auth/src/permissions.ts && pnpm --filter @norish/auth test</automated>
  </verify>
  <done>The auto-merged core auth files retain WorkOS + multi-household + per-cookbook permissions and use upstream's moved module paths; the auth suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 13 trpc / shared-react / shared / shared-server conflicts (upstream base + fork feature re-apply)</name>
  <files>packages/trpc/src/middleware.ts, packages/trpc/src/routers/households/households.ts, packages/trpc/src/routers/recipes/{recipes,shares,helpers}.ts, packages/shared-react/src/hooks/households/{types,use-household-mutations}.ts, packages/shared-react/src/hooks/ratings/index.ts, packages/shared-react/src/contexts/households/household-context.tsx, packages/shared-server/src/cache/household.ts, packages/shared/src/contracts/zod/{household,recipe}.ts, packages/trpc/__tests__/archive/archive-import-validation.test.ts</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§1 Subsystem 3 table — the per-file upstream-change vs fork-change map; §6 module-boundary moves)
    - packages/trpc/src/middleware.ts (upstream moved getCachedHouseholdForUser → @norish/shared-server/cache/household + SubscriptionMultiplexer → @norish/shared-server/redis; fork added getHouseholdsForUser + memberHouseholdIds)
    - packages/trpc/src/routers/recipes/shares.ts (upstream recipe-share-links router; fork's visibility rewrite + assertRecipeAccess(edit) gate)
    - packages/trpc/src/routers/households/households.ts (fork rename/invite mutations)
    - packages/shared-react/src/hooks/ratings/index.ts (fork useRecipeRatersQuery)
    - packages/shared/src/contracts/zod/household.ts (fork inviteToken + policy types)
    - packages/shared/src/contracts/zod/recipe.ts (fork visibility types)
    - packages/shared-server/src/cache/household.ts (NEW upstream file; accept the moved helper)
    - packages/trpc/__tests__/archive/archive-import-validation.test.ts (upstream updated mock paths; fork added getHouseholdsForUser mock)
  </read_first>
  <action>
    Resolve each of the 13 conflicts listed in research §1 Subsystem 3 per D-08: take upstream's refactored base + the moved module-boundary import paths (`@norish/shared-server/cache/household`, `@norish/shared-server/redis/subscription-multiplexer`, `@norish/shared-server/realtime/*`), then re-apply the fork's feature delta. Specifically: `trpc/middleware.ts` — keep upstream's moved cache/redis imports, re-add the fork's `getHouseholdsForUser` call + `memberHouseholdIds` into the `withAuth` ctx; `trpc/routers/recipes/shares.ts` — unify upstream's new recipe-share-links additions with the fork's visibility rewrite, keeping the `assertRecipeAccess(edit)` gate on `shareSetVisibility` (HOUSE-06); `trpc/routers/households/households.ts` — keep fork rename/invite mutations on upstream's base; `trpc/routers/recipes/recipes.ts` + `helpers.ts` — keep fork household scoping; `shared-react` hooks/types/context — re-add fork multi-household + `useRecipeRatersQuery`; `shared/contracts/zod/household.ts` + `recipe.ts` — re-add fork inviteToken/policy + visibility zod types; `shared-server/src/cache/household.ts` — accept upstream's moved file (the helper's new home); `trpc/__tests__/archive/archive-import-validation.test.ts` — take upstream's updated mock paths + the fork's `getHouseholdsForUser` mock. After resolving, re-sync the touched hardlink twins: `cp -a packages/trpc/src/. node_modules/@norish/trpc/src/`, `cp -a packages/shared-react/src/. node_modules/@norish/shared-react/src/`, `cp -a packages/shared/src/. node_modules/@norish/shared/src/`, `cp -a packages/shared-server/src/. node_modules/@norish/shared-server/src/`. Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "packages/(trpc|shared-react|shared|shared-server)/" | wc -l` returns `0` (all 13 resolved).
    - `grep -c "memberHouseholdIds" packages/trpc/src/middleware.ts` returns >= 1 (fork ctx field preserved).
    - `grep -c "@norish/shared-server/cache/household" packages/trpc/src/middleware.ts` returns >= 1 (upstream moved import adopted); `grep -c "@norish/db/cached-household" packages/trpc/src/middleware.ts` returns `0`.
    - `grep -c "assertRecipeAccess" packages/trpc/src/routers/recipes/shares.ts` returns >= 1 (sharing visibility edit-gate preserved).
    - `grep -c "useRecipeRatersQuery" packages/shared-react/src/hooks/ratings/index.ts` returns >= 1.
    - `grep -c "getCachedHouseholdForUser" packages/shared-server/src/cache/household.ts` returns >= 1 (moved helper accepted).
  </acceptance_criteria>
  <verify>
    <automated>! ( git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -qE "packages/(trpc|shared-react|shared|shared-server)/" ) && grep -q "memberHouseholdIds" packages/trpc/src/middleware.ts && grep -q "assertRecipeAccess" packages/trpc/src/routers/recipes/shares.ts && echo OK</automated>
  </verify>
  <done>All 13 conflicts resolved with no markers; upstream module-boundary moves adopted; fork multi-household/sharing/ratings features re-applied.</done>
</task>

<task type="auto">
  <name>Task 3: Typecheck + test auth/trpc under sg docker, run the tRPC-layer adversarial isolation check, and commit</name>
  <files>packages/auth/, packages/trpc/ (typecheck/test only)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (Validation Architecture per-plan gate for 20-03; §4b isolation + adversarial verification; baseline auth 129+ / trpc 256+)
    - CLAUDE.md (adversarial verification of the security boundary; do NOT run docker:build)
    - packages/trpc/__tests__/recipes/permissions-integration.test.ts (the cross-cookbook block this plan must keep green)
    - packages/trpc/__tests__/ratings/raters.test.ts (the assertRecipeAccess(view) gate)
  </read_first>
  <action>
    Run `pnpm --filter @norish/trpc typecheck` and `pnpm --filter @norish/auth typecheck`; fix any import-resolution errors that surface from the moved module boundaries IN THE RESOLVED FILES ONLY. Run the auth suite (`pnpm --filter @norish/auth test`) and the trpc testcontainer suite under `sg docker`: `sg docker -c 'pnpm --filter @norish/trpc test'` (incl. permissions-integration + raters isolation). Then run the security-critical adversarial check at the tRPC access boundary (D-09, CLAUDE.md): temporarily weaken `assertRecipeAccess` / the `canAccessResource` gate (e.g. force it to return allow), run `sg docker -c 'pnpm --filter @norish/trpc test -- --testPathPattern="permissions-integration|raters"'`, confirm the cross-cookbook block + raters access-gate tests go RED, then revert the weakening byte-identical and confirm GREEN — NEVER commit the weakening (`git diff` for the weakened file must be empty before committing). Only after all gates are green and the adversarial check is reverted, commit the auth/permissions/trpc subsystem resolution (`feat(20-03): ...`). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/auth typecheck` and `pnpm --filter @norish/trpc typecheck` both exit 0.
    - `pnpm --filter @norish/auth test` exits 0 (baseline 129+ passing).
    - `sg docker -c 'pnpm --filter @norish/trpc test'` exits 0 (baseline 256+ passing, incl. permissions-integration + raters isolation).
    - Adversarial check performed and reverted: after the run `git status --porcelain` shows no modification to the weakened access-boundary file (the weakening is neither committed nor left in the tree).
    - `git status --porcelain` shows no remaining conflict markers in `packages/(trpc|shared-react|shared|shared-server|auth)/`; the subsystem resolution is committed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/auth typecheck && pnpm --filter @norish/trpc typecheck && pnpm --filter @norish/auth test && sg docker -c 'pnpm --filter @norish/trpc test'</automated>
  </verify>
  <done>auth + trpc typecheck/test green (trpc under `sg docker`, incl. permissions-integration + raters isolation); the tRPC-layer adversarial weaken→red→revert check passed without committing the weakening; the subsystem resolution is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request → tRPC procedure | Untrusted client input crosses the tRPC middleware; `withAuth` resolves the session + member-household set that gates access. |
| requester household scope → recipe / ratings access | `assertRecipeAccess` / `canAccessResource` gate per-recipe and per-rater-name access by the recipe's own cookbook policy (HOUSE-06). |
| upstream module-boundary move → fork consumers | Moved cache/redis/realtime helpers cross from `@norish/db`/`@norish/queue`/`@norish/trpc/helpers` into `@norish/shared-server`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-LEAK | Information Disclosure | per-cookbook recipe + rater-name isolation at the tRPC layer (HOUSE-06 / POLICY-01) | mitigate | Re-apply fork ctx `memberHouseholdIds` + `assertRecipeAccess` gates on upstream's base; prove with `sg docker -c 'pnpm --filter @norish/trpc test -- --testPathPattern="permissions-integration\|raters"'`; adversarially weaken the gate → confirm RED → revert byte-identical, never commit (Task 3). |
| T-20-03-AUTH | Spoofing / Elevation | WorkOS + multi-household session resolution in the auto-merged auth.ts | mitigate | Verify (not rewrite) the auto-merge preserved buildWorkOSProviders/addUserToHousehold/setActiveHousehold; gate via `pnpm --filter @norish/auth test` (Task 1). |
| T-20-03-CFG | Tampering | moved cache/redis/realtime imports resolving to stale paths | mitigate | Adopt the `@norish/shared-server/*` paths; old `@norish/db/cached-household` reference count must be zero; gated by auth+trpc typecheck (Task 3). |
</threat_model>

<verification>
- All 13 conflicts + the 3 auto-merged core auth files leave no markers; fork features preserved; upstream module-boundary moves adopted.
- auth + trpc typecheck/test green (trpc under `sg docker`), permissions-integration + raters isolation passing.
- tRPC-layer adversarial isolation check performed and reverted (weakening never committed).
- DIRECTOR NOTE (not an executor gate): director may run `pnpm docker:build` after these gates pass; never a task acceptance criterion.
</verification>

<success_criteria>
- auth/permissions/trpc subsystem resolved: upstream module moves + fork multi-household/sharing/ratings/WorkOS all preserved.
- auth + trpc suites green under `sg docker`; HOUSE-06 isolation intact and adversarially verified at the tRPC boundary.
- Subsystem resolution committed.
</success_criteria>

<output>
Create `.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-03-SUMMARY.md` when done.
</output>
