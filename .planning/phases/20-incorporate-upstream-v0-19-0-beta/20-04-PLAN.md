---
phase: 20-incorporate-upstream-v0-19-0-beta
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx
  - apps/web/app/(app)/recipes/[id]/recipe-page-mobile.tsx
  - apps/web/app/(app)/recipes/[id]/components/recipe-share-panel.tsx
  - apps/web/app/share/[token]/page.tsx
  - apps/web/context/household-context.tsx
  - apps/web/context/recipes-context.tsx
  - apps/web/components/navbar/navbar-user-menu.tsx
  - apps/web/app/(app)/settings/household/components/household-info-card.tsx
  - apps/web/app/(app)/settings/household/components/join-code-card.tsx
  - apps/web/app/(app)/settings/household/components/no-household-view.tsx
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/login/components/auto-sign-in.tsx
  - apps/web/app/(app)/settings/admin/components/video-processing-form.tsx
  - apps/web/components/shared/import-recipe-modal.tsx
  - apps/web/components/timer-dock.tsx
  - packages/ui/src/star-rating.tsx
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "The 15 web conflicts + the shared star-rating conflict are resolved keeping upstream's HeroUI v3 API AND the fork's multi-household/sharing/ratings/WorkOS UI"
    - "Fork UI features survive: RecipeRaters on both recipe pages, the visibility share panel + public /share gate, the cookbook switcher, household rename/invite/policy cards, WorkOS auto-redirect + ?sso=0 escape, AssemblyAI provider option, timer-dock fixes"
    - "No HeroUI v2 component names remain in the resolved web files (Autocomplete/AutocompleteItem/SelectItem migrated to v3 equivalents)"
    - "@norish/web typecheck is clean and the web test suite passes"
  artifacts:
    - path: "apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx"
      provides: "Recipe detail with RecipeRaters on upstream's v3 UI"
      contains: "RecipeRaters"
    - path: "apps/web/app/share/[token]/page.tsx"
      provides: "Public share view with visibility gate on upstream v3 UI"
      contains: "visibility"
    - path: "packages/ui/src/star-rating.tsx"
      provides: "Unified star-rating: upstream v3 props + fork userValue/onClear"
      contains: "onClear"
  key_links:
    - from: "apps/web/components/navbar/navbar-user-menu.tsx"
      to: "switchActive"
      via: "cookbook switcher dropdown"
      pattern: "switchActive"
    - from: "apps/web/app/(auth)/login/page.tsx"
      to: "shouldAutoRedirectToSso"
      via: "WorkOS auto-redirect with ?sso=0 escape"
      pattern: "sso"
---

<objective>
Resolve the web UI subsystem (D-03 fourth in order, depends on the resolved trpc/shared-react contracts from 20-03). 15 conflicts in `apps/web/` + the shared `packages/ui/src/star-rating.tsx` conflict share one pattern: upstream upgraded HeroUI v2 → v3 (component API changes) while the fork added multi-household / sharing / ratings / WorkOS / AssemblyAI / timer-dock UI. Both sets must win. (Per Strategy B, the 20-01 merge already committed the 16 conflicted files on the UPSTREAM / HeroUI-v3 side — there are NO live conflict markers; this plan RE-APPLIES the fork UI delta on top, recovering it from `git show pre-0.19.0-integration:<file>`.) The HeroUI v2→v3 migration is the main hazard (Pitfall 6).

Purpose: Adopt upstream's 0.19.0 HeroUI v3 component API across the conflicting web files while preserving every fork UI feature, so the integration branch renders the full multi-household/sharing/ratings/WorkOS experience on upstream's refreshed component layer.
Output: All 16 conflicts (15 web + star-rating) resolved on the v3 API; `@norish/web` typecheck clean; web test suite green; subsystem resolution committed.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-CONTEXT.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
NEW symbols this plan creates: none. It RESOLVES 16 conflicts. It unifies `packages/ui/src/star-rating.tsx` (upstream's new `size`/`allowClear`/`showValueSuffix` props + the fork's `userValue`/`onClear` for RATE-01) and re-applies these fork UI features onto upstream's HeroUI v3 components: `RecipeRaters` on both recipe pages, the recipe-share-panel visibility control + the `/share/<token>` visibility gate, the cookbook switcher in the navbar, the household rename/invite-link/policy cards, the WorkOS auto-redirect + `?sso=0` escape on login, the AssemblyAI option in the video-processing form, the active-cookbook indicator in the import modal, and the timer-dock UI fixes.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the recipe/sharing/ratings web conflicts + star-rating on HeroUI v3</name>
  <files>apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx, apps/web/app/(app)/recipes/[id]/recipe-page-mobile.tsx, apps/web/app/(app)/recipes/[id]/components/recipe-share-panel.tsx, apps/web/app/share/[token]/page.tsx, packages/ui/src/star-rating.tsx</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§1 Subsystem 5 Web UI table + the HeroUI v2→v3 note; Pitfall 6; star-rating note in "Additional conflicts")
    - apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx (fork RecipeRaters component placement)
    - apps/web/app/(app)/recipes/[id]/recipe-page-mobile.tsx (same)
    - apps/web/app/(app)/recipes/[id]/components/recipe-share-panel.tsx (fork visibility control)
    - apps/web/app/share/[token]/page.tsx (fork public-share visibility gate)
    - packages/ui/src/star-rating.tsx (upstream v3 props vs fork userValue/onClear)
  </read_first>
  <action>
    Per Strategy B these files were committed on the upstream v3 side at 20-01 (no live conflict markers); RE-APPLY the fork delta onto that base (recover via `git show pre-0.19.0-integration:<file>`), per D-08 (upstream v3 base + fork feature re-apply). For `recipe-page-desktop.tsx` and `recipe-page-mobile.tsx`: take upstream's HeroUI v3 refactor of the recipe detail layout and re-insert the fork's `RecipeRaters` component in the existing ratings section under the StarRating input. For `recipe-share-panel.tsx`: unify upstream's new share UI with the fork's Visibility select wired to `shareSetVisibility`. For `share/[token]/page.tsx`: keep upstream's v3 UI + the fork's visibility-gated public render. For `packages/ui/src/star-rating.tsx`: perform the 3-way merge unifying upstream's new type-safe API (`size`/`allowClear`/`showValueSuffix`) with the fork's `userValue`/`onClear` props (RATE-01) — express both feature sets under upstream's v3 prop types, not the v2 ones. Audit every resolved file for HeroUI v2 component names that no longer exist in v3 (`Autocomplete`/`AutocompleteItem`/`SelectItem` → the v3 ComboBox/ListBox equivalents upstream uses) and migrate them. Re-sync the touched hardlink twin for the ui package: `cp -a packages/ui/src/. node_modules/@norish/ui/src/`. Run `pnpm --filter @norish/web typecheck` after this cluster and fix v3-API typecheck breaks in the resolved files. Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "recipe-page-(desktop|mobile)|recipe-share-panel|share/\[token\]/page|star-rating" | wc -l` returns `0`.
    - `grep -c "RecipeRaters" "apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx"` and the mobile file each return >= 1.
    - `grep -c "visibility" "apps/web/app/share/[token]/page.tsx"` returns >= 1 (fork public-share gate preserved).
    - `grep -c "onClear" packages/ui/src/star-rating.tsx` returns >= 1 (fork prop preserved).
    - `grep -rE "AutocompleteItem|\bSelectItem\b" "apps/web/app/(app)/recipes/[id]" packages/ui/src/star-rating.tsx | wc -l` returns `0` (no v2-only component names remain in these resolved files).
  </acceptance_criteria>
  <verify>
    <automated>! ( git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -qE "recipe-page-(desktop|mobile)|recipe-share-panel|share/\[token\]/page|star-rating" ) && grep -q "RecipeRaters" "apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx" && grep -q "onClear" packages/ui/src/star-rating.tsx && echo OK</automated>
  </verify>
  <done>The recipe/sharing/ratings web conflicts + star-rating are resolved on the HeroUI v3 API with the fork's RecipeRaters, visibility gate, and userValue/onClear preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the household / auth / settings / shared web conflicts on HeroUI v3</name>
  <files>apps/web/context/{household-context,recipes-context}.tsx, apps/web/components/navbar/navbar-user-menu.tsx, apps/web/app/(app)/settings/household/components/{household-info-card,join-code-card,no-household-view}.tsx, apps/web/app/(auth)/login/page.tsx, apps/web/app/(auth)/login/components/auto-sign-in.tsx, apps/web/app/(app)/settings/admin/components/video-processing-form.tsx, apps/web/components/shared/import-recipe-modal.tsx, apps/web/components/timer-dock.tsx</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§1 Subsystem 5 remaining rows; HeroUI v2→v3 note; Pitfall 6)
    - apps/web/context/household-context.tsx + apps/web/context/recipes-context.tsx (fork hooks + household-watch)
    - apps/web/components/navbar/navbar-user-menu.tsx (fork cookbook switcher)
    - apps/web/app/(app)/settings/household/components/household-info-card.tsx + join-code-card.tsx + no-household-view.tsx (fork rename/invite/modal)
    - apps/web/app/(auth)/login/page.tsx + apps/web/app/(auth)/login/components/auto-sign-in.tsx (fork WorkOS auto-redirect + ?sso=0 escape)
    - apps/web/app/(app)/settings/admin/components/video-processing-form.tsx (fork AssemblyAI option)
    - apps/web/components/shared/import-recipe-modal.tsx (fork active-cookbook indicator)
    - apps/web/components/timer-dock.tsx (fork timer-dock UI fixes)
  </read_first>
  <action>
    Per Strategy B these files were committed on the upstream v3 side at 20-01 (no live conflict markers); RE-APPLY the fork delta onto that base (recover via `git show pre-0.19.0-integration:<file>`), per D-08 (upstream v3 base + fork feature re-apply): `context/household-context.tsx` + `context/recipes-context.tsx` — keep fork hooks + the activeHouseholdId watch on upstream's import ordering; `navbar/navbar-user-menu.tsx` — re-add the fork cookbook switcher DropdownSection (`switchActive`) on upstream's v3 navbar; the three settings/household cards — re-add fork rename + invite-link section + the modal-based no-household view; `login/page.tsx` + `auto-sign-in.tsx` — keep upstream's changes + the fork's `shouldAutoRedirectToSso` auto-redirect and the `?sso=0` escape link; `video-processing-form.tsx` — re-add the AssemblyAI provider option on upstream's v3 form; `import-recipe-modal.tsx` — re-add the active-cookbook indicator; `timer-dock.tsx` — keep the fork timer-dock UI fixes on upstream's base. Migrate any HeroUI v2 component names to v3 equivalents in each resolved file. Re-run `pnpm --filter @norish/web typecheck` after this cluster and fix v3-API typecheck breaks in the resolved files. Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "apps/web/" | wc -l` returns `0` (all web conflicts resolved).
    - `grep -c "switchActive" apps/web/components/navbar/navbar-user-menu.tsx` returns >= 1 (cookbook switcher preserved).
    - `grep -ci "sso" "apps/web/app/(auth)/login/page.tsx"` returns >= 1 (WorkOS auto-redirect/escape preserved).
    - `grep -ci "assemblyai" "apps/web/app/(app)/settings/admin/components/video-processing-form.tsx"` returns >= 1 (AssemblyAI option preserved).
    - `grep -rE "AutocompleteItem|\bSelectItem\b" apps/web/components/navbar/navbar-user-menu.tsx "apps/web/app/(app)/settings/admin/components/video-processing-form.tsx" | wc -l` returns `0`.
  </acceptance_criteria>
  <verify>
    <automated>! ( git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -qE "apps/web/" ) && grep -q "switchActive" apps/web/components/navbar/navbar-user-menu.tsx && grep -qi "assemblyai" "apps/web/app/(app)/settings/admin/components/video-processing-form.tsx" && echo OK</automated>
  </verify>
  <done>All household/auth/settings/shared web conflicts are resolved on the HeroUI v3 API with the cookbook switcher, WorkOS auto-redirect+escape, AssemblyAI option, and timer-dock fixes preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Typecheck + test @norish/web on the resolved tree and commit the web subsystem resolution</name>
  <files>apps/web/ (typecheck/test only; test-only mock fixups if surfaced)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (Validation Architecture per-plan gate for 20-04; HeroUI v2→v3 note; Assumption A3 about auto-merged web files; baseline web 383+)
    - CLAUDE.md (do NOT run docker:build — director-owned)
  </read_first>
  <action>
    Run `pnpm --filter @norish/web typecheck` against the fully resolved web tree. Per Assumption A3 / Pitfall 6, the HeroUI v3 upgrade may also break AUTO-MERGED (non-conflicting) web components — typecheck surfaces them; fix any v2→v3 API breaks it reports (component/prop renames) without touching unrelated logic. Run `pnpm --filter @norish/web test` and confirm the web suite passes (the RATE-01 ratings hooks + import-modal household-context mocks noted in STATE.md must remain satisfied). If a web test fails because of a stale ratings/household mock that the merge re-introduced, fix the mock to match the resolved contracts (test-only fix). Commit the web subsystem resolution (`feat(20-04): ...`). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/web typecheck` exits 0 (clean against HeroUI v3 + the resolved trpc/shared-react contracts).
    - `pnpm --filter @norish/web test` exits 0 with the web suite passing (baseline 383+).
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) "` shows no web conflict markers remaining; the web subsystem resolution is committed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/web typecheck && pnpm --filter @norish/web test</automated>
  </verify>
  <done>`@norish/web` typecheck + test are green on the merged+resolved tree with the HeroUI v3 migration applied to both conflicting and auto-merged components; the web subsystem resolution is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public /share/<token> view → recipe data | Anonymous (no-auth) visitors hit the public share route; the visibility gate decides what renders. This UI must keep the fork's visibility-gated render. |
| upstream HeroUI v3 + UI refactor → fork UI | Upstream component-API changes cross into the fork's multi-household/sharing/ratings/WorkOS UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-SHARE | Information Disclosure | public /share/<token> visibility gate UI | mitigate | Preserve the fork's visibility-gated render in `share/[token]/page.tsx` (gate `grep visibility`); the server-side gate (sharedRecipeProcedure) is the authoritative choke point and was verified in 20-03 — the UI must not bypass it. |
| T-20-04-SSO | Spoofing / Account confusion | WorkOS auto-redirect + ?sso=0 escape on login | mitigate | Re-apply `shouldAutoRedirectToSso` + the `?sso=0` recovery escape on upstream's login page (gate `grep -i sso`); avoids a redirect loop or a stuck unauthenticated state. |
| T-20-04-V3 | Tampering (build integrity) | HeroUI v2 component names surviving into v3 build | mitigate | Audit + migrate v2-only component names; gate via `pnpm --filter @norish/web typecheck` across conflicting AND auto-merged files (Task 3). |
</threat_model>

<verification>
- All 16 web conflicts resolved on the HeroUI v3 API; every fork UI feature preserved; no v2-only component names remain in resolved files.
- `pnpm --filter @norish/web typecheck` + `pnpm --filter @norish/web test` green (incl. auto-merged components surfaced by the v3 upgrade).
- DIRECTOR NOTE (not an executor gate): the director may run `pnpm docker:build` after these gates pass; the build is never a task acceptance criterion.
</verification>

<success_criteria>
- Web UI subsystem resolved: upstream HeroUI v3 + fork multi-household/sharing/ratings/WorkOS/AssemblyAI/timer-dock UI all preserved.
- `@norish/web` typecheck + test green; subsystem resolution committed.
</success_criteria>

<output>
Create `.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-04-SUMMARY.md` when done.
</output>
