---
phase: 20-incorporate-upstream-v0-19-0-beta
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - packages/queue/src/allergy-detection/producer.ts
  - packages/queue/src/allergy-detection/worker.ts
  - packages/auth/__tests__/auth/workos-provider.test.ts
  - packages/trpc/__tests__/ratings/raters.test.ts
  - packages/api/__tests__/server/ai/transcriber-assemblyai.test.ts
  - packages/config/__tests__/config/server-config-loader.test.ts
  - .github/workflows/_node-ci.yml
  - .github/workflows/pr-quality.yml
  - pnpm-lock.yaml
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "The 4 fork-only files importing the deleted @norish/config/server-config-loader are migrated to @norish/shared-server/config/server-config-loader (or the config test moved/deleted)"
    - "The queue allergy-detection producer/worker take upstream's moved server-config-loader + realtime-emitter import paths"
    - "The CI workflows keep CAMOFOX_URL, add HEROUI_AUTH_TOKEN, and drop CHROME_WS_ENDPOINT"
    - "No conflict markers remain anywhere in the tree; the full monorepo typecheck + lint + test are green (db/queue/trpc suites under sg docker)"
  artifacts:
    - path: ".github/workflows/_node-ci.yml"
      provides: "CI with CAMOFOX_URL retained, HEROUI_AUTH_TOKEN added, no CHROME_WS_ENDPOINT"
      contains: "CAMOFOX_URL"
  key_links:
    - from: "packages/queue/src/allergy-detection/producer.ts"
      to: "@norish/shared-server/config/server-config-loader"
      via: "moved server-config-loader import"
      pattern: "shared-server/config/server-config-loader"
---

<objective>
Final conflict-resolution plan (D-03 fifth): close the remaining module-boundary import fixups, the queue allergy-detection conflicts, the CI/tooling conflicts, finalize the lockfile, and prove the WHOLE monorepo green. This plan is the "all green" gate that must pass before the director-owned `pnpm docker:build` and before any beta provisioning (20-06). It picks up the 4 fork-only files still importing the deleted `@norish/config/server-config-loader` (research §4d, Pitfall 4), which earlier subsystem plans deliberately deferred here.

Purpose: Eliminate every residual conflict + stale import from the module-boundary consolidation, keep the CI Camoufox env, and certify the integration branch with a full-monorepo typecheck/lint/test sweep (db/queue/trpc under `sg docker`).
Output: Zero conflict markers tree-wide; full `pnpm typecheck` + `pnpm lint` clean; all package test suites green; the branch is a candidate for the director build.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-CONTEXT.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md
@.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-04-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_produced>
NEW symbols this plan creates: none. It RESOLVES the queue allergy-detection conflicts (`producer.ts`, `worker.ts` — take upstream's moved import paths wholesale), migrates the 4 fork-only `@norish/config/server-config-loader` imports to `@norish/shared-server/config/server-config-loader` (and moves/deletes `packages/config/__tests__/config/server-config-loader.test.ts` since the tested module left `@norish/config`), resolves the two `.github/workflows` conflicts (keep `CAMOFOX_URL`, add `HEROUI_AUTH_TOKEN`, drop `CHROME_WS_ENDPOINT`), and finalizes `pnpm-lock.yaml`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 4 fork-only server-config-loader imports + resolve the queue allergy-detection conflicts</name>
  <files>packages/queue/src/allergy-detection/{producer,worker}.ts, packages/auth/__tests__/auth/workos-provider.test.ts, packages/trpc/__tests__/ratings/raters.test.ts, packages/api/__tests__/server/ai/transcriber-assemblyai.test.ts, packages/config/__tests__/config/server-config-loader.test.ts (move/delete)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§4d the 4 fork-only files; §6 module-boundary moves; Pitfall 4; Assumption A4 about the config test)
    - packages/queue/src/allergy-detection/producer.ts + worker.ts (upstream moved server-config-loader + realtime-emitter imports; fork logic unchanged)
    - packages/auth/__tests__/auth/workos-provider.test.ts (fork-only; old @norish/config/server-config-loader import)
    - packages/trpc/__tests__/ratings/raters.test.ts (fork-only; old import)
    - packages/api/__tests__/server/ai/transcriber-assemblyai.test.ts (fork-only; old import)
    - packages/config/__tests__/config/server-config-loader.test.ts (tests a module that moved out of @norish/config — move to shared-server or delete)
  </read_first>
  <action>
    Resolve the two queue conflicts (`packages/queue/src/allergy-detection/producer.ts`, `worker.ts`) by taking upstream's new import paths wholesale (`@norish/shared-server/config/server-config-loader`, `@norish/shared-server/realtime/*`) — fork logic is unchanged, so this is a clean upstream-take for the imports (D-08). Migrate the 3 fork-only test files that still import the deleted `@norish/config/server-config-loader` to `@norish/shared-server/config/server-config-loader`: `packages/auth/__tests__/auth/workos-provider.test.ts`, `packages/trpc/__tests__/ratings/raters.test.ts`, `packages/api/__tests__/server/ai/transcriber-assemblyai.test.ts`. For `packages/config/__tests__/config/server-config-loader.test.ts`: inspect it first (Assumption A4) — if it tests behavior still owned by the moved module, MOVE it to `packages/shared-server/__tests__/...` with the new import; if it only re-tests the deleted `@norish/config` surface, DELETE it. Re-sync the touched hardlink twins: `cp -a packages/queue/src/. node_modules/@norish/queue/src/` (and any other package whose source files changed). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -E "packages/queue/src/allergy-detection/" | wc -l` returns `0`.
    - `grep -c "@norish/shared-server/config/server-config-loader" packages/queue/src/allergy-detection/producer.ts` returns >= 1; `grep -c "@norish/config/server-config-loader" packages/queue/src/allergy-detection/producer.ts packages/queue/src/allergy-detection/worker.ts` returns `0`.
    - `grep -rl "@norish/config/server-config-loader" packages/auth/__tests__ packages/trpc/__tests__ packages/api/__tests__ | wc -l` returns `0` (all 3 fork-only test imports migrated).
    - `packages/config/__tests__/config/server-config-loader.test.ts` is either absent (deleted) OR moved under `packages/shared-server/` (verified by `ls`): `ls packages/config/__tests__/config/server-config-loader.test.ts 2>/dev/null && echo PRESENT || echo GONE` prints `GONE`.
  </acceptance_criteria>
  <verify>
    <automated>! ( git status --porcelain | grep -E "^(UU|AA|DU|UD) " | grep -qE "packages/queue/src/allergy-detection/" ) && [ "$(grep -rl '@norish/config/server-config-loader' packages/auth/__tests__ packages/trpc/__tests__ packages/api/__tests__ 2>/dev/null | wc -l)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>The queue allergy-detection conflicts take upstream's moved imports; all 4 fork-only stale `@norish/config/server-config-loader` references are migrated (config test moved or deleted).</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the CI/tooling conflicts, finalize the lockfile, and re-sync the workspace</name>
  <files>.github/workflows/_node-ci.yml, .github/workflows/pr-quality.yml, pnpm-lock.yaml</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (§1 "Additional conflicts" — .github workflows + pnpm-lock; HeroUI v2→v3 note re HEROUI_AUTH_TOKEN; Pitfall 3 lockfile)
    - .github/workflows/_node-ci.yml (upstream added HEROUI_AUTH_TOKEN; fork changed CHROME_WS_ENDPOINT → CAMOFOX_URL)
    - .github/workflows/pr-quality.yml (same pattern)
  </read_first>
  <action>
    Resolve the two `.github/workflows` conflicts (`_node-ci.yml`, `pr-quality.yml`): keep the fork's `CAMOFOX_URL` env (Camoufox constraint), ADD upstream's `HEROUI_AUTH_TOKEN` secret, and DROP any reintroduced `CHROME_WS_ENDPOINT`. If `pnpm-lock.yaml` is still conflicted (it always conflicts on merge — Pitfall 3), do NOT hand-merge it: ensure every `package.json` / `pnpm-workspace.yaml` conflict is resolved (they are, after 20-01..20-04 + Task 1), then run `pnpm install` to regenerate the lockfile deterministically from the catalog. After install, re-sync all hardlink twins that the merge replaced across every touched package (`cp -a packages/<pkg>/src/. node_modules/@norish/<pkg>/src/` for db, db-schema, api, auth, trpc, shared, shared-react, shared-server, queue, ui). Do NOT run `pnpm docker:build`.
  </action>
  <acceptance_criteria>
    - `grep -c "CAMOFOX_URL" .github/workflows/_node-ci.yml` returns >= 1; `grep -c "HEROUI_AUTH_TOKEN" .github/workflows/_node-ci.yml` returns >= 1; `grep -c "CHROME_WS_ENDPOINT" .github/workflows/_node-ci.yml .github/workflows/pr-quality.yml` returns `0`.
    - `git status --porcelain | grep -E "^(UU|AA|DU|UD) "` returns nothing (ZERO conflict markers anywhere in the tree).
    - `pnpm install` completes without error and `pnpm-lock.yaml` has no conflict markers (`grep -c '<<<<<<<' pnpm-lock.yaml` returns `0`).
  </acceptance_criteria>
  <verify>
    <automated>grep -q "CAMOFOX_URL" .github/workflows/_node-ci.yml && grep -q "HEROUI_AUTH_TOKEN" .github/workflows/_node-ci.yml && [ "$(grep -c 'CHROME_WS_ENDPOINT' .github/workflows/_node-ci.yml .github/workflows/pr-quality.yml | awk -F: '{s+=$2} END{print s}')" -eq 0 ] && ! ( git status --porcelain | grep -qE "^(UU|AA|DU|UD) " ) && echo OK</automated>
  </verify>
  <done>CI keeps CAMOFOX_URL + adds HEROUI_AUTH_TOKEN + drops CHROME_WS_ENDPOINT; the lockfile is regenerated; zero conflict markers remain tree-wide.</done>
</task>

<task type="auto">
  <name>Task 3: Full-monorepo typecheck + lint + test sweep (db/queue/trpc under sg docker) and finalize the merge</name>
  <files>(no source edits — full-repo typecheck/lint/test sweep + final commit)</files>
  <read_first>
    - .planning/phases/20-incorporate-upstream-v0-19-0-beta/20-RESEARCH.md (Validation Architecture per-plan gate for 20-05 = full monorepo; baselines; §4 all hard-constraint proof commands)
    - CLAUDE.md (build is director-owned; do NOT run docker:build; isolation suites are the security gate)
  </read_first>
  <action>
    Run the full-monorepo gate: `pnpm typecheck` (all packages) and `pnpm lint` (all packages) — both must be clean. Run the full test sweep, routing the db/queue/trpc testcontainer suites through `sg docker`: `sg docker -c 'pnpm test'` (or, if `pnpm test` does not pick up the docker group, run the non-container suites with `pnpm test` and the db/queue/trpc suites individually under `sg docker -c 'pnpm --filter @norish/<pkg> test'`). Re-run the consolidated hard-constraint proof one final time on the fully resolved tree: Camoufox (`grep -rE "playwright|chrome-headless|CHROME_WS_ENDPOINT" packages/api/src packages/api/package.json docker/ .github/ --include="*.ts" --include="*.json" --include="*.yml"` → zero; `ls packages/api/src/playwright.ts` absent), seed-config sync functions present (`grep -n "syncWorkOSProvider\|syncAIConfigFromEnv\|syncVideoConfigFromEnv\|syncAuthTogglesFromEnv" packages/api/src/startup/seed-config.ts` → all present), and HOUSE-06 isolation green (`sg docker -c 'pnpm --filter @norish/db test -- --testPathPattern="households.isolation"'`). Commit the final tooling/lockfile resolution (`chore(20-05): ...`) so the merge is fully resolved + committed. Then write the plan-level DIRECTOR NOTE in the SUMMARY: the integration branch is now a candidate for the director-owned `pnpm docker:build` (detached + polled, per CLAUDE.md) — but do NOT run the build in this task.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0 across all packages.
    - `pnpm lint` exits 0 across all packages.
    - Full test sweep green: non-container suites via `pnpm test` exit 0, and `sg docker -c 'pnpm --filter @norish/db test'`, `sg docker -c 'pnpm --filter @norish/queue test'`, `sg docker -c 'pnpm --filter @norish/trpc test'` each exit 0 (baselines: db 99, trpc 256+, queue 77; web 383+, auth 129+).
    - Camoufox proof: `grep -rE "playwright|chrome-headless|CHROME_WS_ENDPOINT" packages/api/src packages/api/package.json --include="*.ts" --include="*.json" | wc -l` returns `0`; `ls packages/api/src/playwright.ts 2>/dev/null && echo FAIL || echo PASS` prints `PASS`.
    - seed-config proof: `grep -E "syncWorkOSProvider|syncAIConfigFromEnv|syncVideoConfigFromEnv|syncAuthTogglesFromEnv" packages/api/src/startup/seed-config.ts | wc -l` returns >= 4.
    - HOUSE-06 isolation green: `sg docker -c 'pnpm --filter @norish/db test -- --testPathPattern="households.isolation"'` exits 0.
    - `git status --porcelain` is clean (merge fully committed; no conflict markers; no stray weakening).
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm lint && sg docker -c 'pnpm --filter @norish/db test' && sg docker -c 'pnpm --filter @norish/queue test' && sg docker -c 'pnpm --filter @norish/trpc test' && ( grep -rE "playwright|chrome-headless" packages/api/src packages/api/package.json --include="*.ts" --include="*.json" | grep -q . && exit 1 || echo OK )</automated>
  </verify>
  <done>Full monorepo typecheck + lint + test green (db/queue/trpc under `sg docker`); all hard-constraint proofs (Camoufox, seed-config, HOUSE-06 isolation) pass; the merge is fully resolved + committed; the branch is a candidate for the director build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow env → runtime scraper config | CI env (CAMOFOX_URL vs CHROME_WS_ENDPOINT) decides which scraper the pipeline exercises; must stay Camoufox. |
| regenerated lockfile → installed dependency set | `pnpm install` resolves the dependency graph; a mis-resolved catalog could pull unexpected versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05-CI | Tampering (supply chain) | CHROME_WS_ENDPOINT reintroduced into CI; playwright/chrome refs surviving | mitigate | Keep CAMOFOX_URL, drop CHROME_WS_ENDPOINT, re-run the consolidated Camoufox grep proof across packages/api + docker/ + .github/ (Task 2 + Task 3). |
| T-20-05-LEAK | Information Disclosure | HOUSE-06 per-cookbook isolation across the fully merged tree | mitigate | Final `sg docker -c 'pnpm --filter @norish/db test -- --testPathPattern="households.isolation"'` green (Task 3); the adversarial weaken→red→revert was performed at the db (20-01) and tRPC (20-03) boundaries. |
| T-20-05-CFG | Tampering | config-as-code env sync lost in the merge | mitigate | seed-config proof: all 4 sync functions present in `seed-config.ts` (Task 3). |
| T-20-05-SC | Tampering | lockfile regeneration pulling unexpected/unlisted packages | accept | `pnpm install` regenerates from the catalog deterministically; @heroui-pro/react is on public npm (research §1); no `[ASSUMED]`/`[SUS]` package introduced. |
</threat_model>

<verification>
- Zero conflict markers tree-wide; all module-boundary imports migrated; CI on Camoufox.
- Full `pnpm typecheck` + `pnpm lint` clean; full test sweep green (db/queue/trpc under `sg docker`).
- All hard-constraint proofs pass: Camoufox (no playwright/chrome), seed-config sync functions present, HOUSE-06 isolation green.
- DIRECTOR NOTE (not an executor gate): with this plan green, the integration branch is a candidate for the director-owned `pnpm docker:build` (detached + polled per CLAUDE.md) — required before 20-06 beta provisioning, but never an executor task acceptance criterion.
</verification>

<success_criteria>
- Every conflict resolved + committed; the merge is complete on `integ/upstream-0.19.0`.
- Full monorepo typecheck/lint/test green; all D-09 hard constraints proven on the final tree.
- The branch is build-candidate-ready for the director.
</success_criteria>

<output>
Create `.planning/phases/20-incorporate-upstream-v0-19-0-beta/20-05-SUMMARY.md` when done.
</output>
