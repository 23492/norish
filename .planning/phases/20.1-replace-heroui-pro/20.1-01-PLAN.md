---
phase: 20.1-replace-heroui-pro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/dashboard/recipe-view-mode-toggle.tsx
  - apps/web/components/Panel/Panel.tsx
  - apps/web/components/shared/import-from-image-modal.tsx
  - apps/web/components/ui/drop-zone.tsx
  - apps/web/__tests__/components/panel.test.tsx
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "recipe-view-mode-toggle, Panel, and import-from-image-modal import NOTHING from @heroui-pro/react"
    - "Panel's 13 consumers are untouched — the Sheet→Drawer swap is contained inside Panel.tsx"
    - "Drag-drop AND file-picker still work in the import-from-image modal (react-aria DropZone/FileTrigger, same onDrop/getDropOperation logic)"
    - "@norish/web typecheck EXIT 0 and the web vitest suite passes"
  artifacts:
    - path: "apps/web/components/Panel/Panel.tsx"
      provides: "Bottom panel on free @heroui/react Drawer (placement bottom, handle, close trigger, header/body/footer slots, nested support)"
      contains: "Drawer"
    - path: "apps/web/components/ui/drop-zone.tsx"
      provides: "Styled DropZone compound on react-aria-components DropZone + FileTrigger"
      contains: "FileTrigger"
---

<objective>
Swap the three non-carousel pro usages to already-installed free components: Segment→ToggleButtonGroup (already applied in the working tree — verify + commit), Sheet→Drawer inside Panel.tsx, pro DropZone→react-aria-components DropZone/FileTrigger behind a local styled wrapper.

Purpose: eliminate the paid dependency from the app shell (all bottom-sheet panels) and the import modal with zero new npm deps and zero consumer-file churn.
Output: 3 pro import sites gone; typecheck + web tests green.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-replace-heroui-pro/20.1-CONTEXT.md
@.planning/phases/20.1-replace-heroui-pro/20.1-RESEARCH.md (§2.1, §2.2, §2.4, §3 pitfalls 1-4)
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify + keep the applied Segment→ToggleButtonGroup swap</name>
  <files>apps/web/components/dashboard/recipe-view-mode-toggle.tsx</files>
  <action>
    The swap is ALREADY APPLIED in the working tree (2026-07-15, RESEARCH §2.1): free ToggleButtonGroup/ToggleButton with selectionMode="single", disallowEmptySelection, selectedKeys=[viewMode], Separator on the 2nd button, sizes/labels/sr-only spans preserved. Re-verify against the measurement-system-selector.tsx pattern; confirm no @heroui-pro import remains in the file.
  </action>
  <acceptance_criteria>
    - `grep -c "heroui-pro" apps/web/components/dashboard/recipe-view-mode-toggle.tsx` returns 0.
    - `grep -c "ToggleButtonGroup" apps/web/components/dashboard/recipe-view-mode-toggle.tsx` >= 1.
  </acceptance_criteria>
  <done>The dashboard view-mode toggle runs on free @heroui/react.</done>
</task>

<task type="auto">
  <name>Task 2: Panel.tsx Sheet→Drawer</name>
  <files>apps/web/components/Panel/Panel.tsx</files>
  <read_first>
    - node_modules/@heroui/react/dist/components/drawer/drawer.d.ts (Root state/open props, Content placement, Backdrop variant)
    - apps/web/components/Panel/Panel.tsx (current Sheet slot usage lines ~140-170, nested prop)
  </read_first>
  <action>
    Replace the Sheet compound with Drawer 1:1: Root (map Panel's controlled open/onOpenChange to Drawer's state API per drawer.d.ts — adapt INSIDE Panel only), Backdrop (keep variant + z-[1000]), Content with placement="bottom", Dialog/Handle/CloseTrigger/Header/Heading/Body/Footer as-is. For `nested`, render a plain second Drawer.Root (free Drawer has no NestedRoot — RESEARCH §2.2); preserve dismissability and close-order. Keep all existing classNames (contentClasses/dialogClasses/bodyClasses/footerClasses) so the 20-04 timer-dock-style CSS fixes survive.
  </action>
  <acceptance_criteria>
    - `grep -c "heroui-pro" apps/web/components/Panel/Panel.tsx` returns 0.
    - `git diff --name-only | grep -c "Panel/consumers"` returns 0 (no consumer edits).
    - `pnpm --filter @norish/web exec tsc --noEmit` EXIT 0.
  </acceptance_criteria>
  <done>Every bottom-sheet panel in the app renders on the free Drawer.</done>
</task>

<task type="auto">
  <name>Task 3: import-from-image-modal DropZone → react-aria-components + local wrapper; fix panel test; commit</name>
  <files>apps/web/components/ui/drop-zone.tsx, apps/web/components/shared/import-from-image-modal.tsx, apps/web/__tests__/components/panel.test.tsx</files>
  <action>
    Create `components/ui/drop-zone.tsx`: RAC DropZone as the styled frame/area (getDropOperation/onDrop pass through — same signatures, RESEARCH §2.4), Icon/Label/Description as styled slots matching the current look, Trigger = FileTrigger(acceptedFileTypes, allowsMultiple, onSelect) wrapping a free @heroui/react Button. Update the modal: type the callbacks against RAC DropZoneProps instead of DropZoneAreaProps; keep handleDrop/getDropOperation/handleAddFiles logic byte-identical. Rewrite panel.test.tsx against real Drawer rendering (drop the vi.mock of @heroui-pro). Run `pnpm --filter @norish/web test` + typecheck; commit the plan's files as `feat(20.1-01): replace heroui-pro Segment/Sheet/DropZone with free equivalents`.
  </action>
  <acceptance_criteria>
    - `grep -rc "heroui-pro" apps/web/components/shared/import-from-image-modal.tsx apps/web/__tests__/components/panel.test.tsx` returns 0 per file.
    - `pnpm --filter @norish/web test` passes (baseline 410, panel tests rewritten not skipped).
    - `pnpm --filter @norish/web exec tsc --noEmit` EXIT 0.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "heroui-pro" apps/web/components/dashboard/recipe-view-mode-toggle.tsx apps/web/components/Panel/Panel.tsx apps/web/components/shared/import-from-image-modal.tsx && echo OK</automated>
  </verify>
  <done>Segment, Sheet, and DropZone pro usages are gone; web suite green.</done>
</task>

</tasks>
