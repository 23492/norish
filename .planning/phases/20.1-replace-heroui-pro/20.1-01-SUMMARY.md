---
phase: 20.1-replace-heroui-pro
plan: 01
subsystem: ui
tags: [heroui, react-aria-components, drawer, dropzone, toggle-button-group, vitest]

requires:
  - phase: 20-incorporate-upstream-v0-19-0-beta
    provides: "integ/upstream-0.19.0 branch carrying the @heroui-pro/react dependency to be replaced"
provides:
  - "recipe-view-mode-toggle, Panel, and import-from-image-modal free of @heroui-pro/react"
  - "Local styled DropZone compound on react-aria-components (apps/web/components/ui/drop-zone.tsx)"
  - "Panel bottom-sheet on the free @heroui/react Drawer (nested = second Drawer.Root)"
affects: [20.1-02, 20.1-03]

tech-stack:
  added: []
  patterns:
    - "Free @heroui/react Drawer as the app bottom-sheet (Sheet parity, placement on Content, isOpen/onOpenChange passthrough to react-aria DialogTrigger)"
    - "react-aria-components DropZone + FileTrigger behind a local styled compound; drop callbacks keep RAC signatures"

key-files:
  created:
    - apps/web/components/ui/drop-zone.tsx
  modified:
    - apps/web/components/dashboard/recipe-view-mode-toggle.tsx
    - apps/web/components/Panel/Panel.tsx
    - apps/web/components/shared/import-from-image-modal.tsx
    - apps/web/__tests__/components/panel.test.tsx

key-decisions:
  - "Free Drawer has no NestedRoot; nested panels render a plain second Drawer.Root (react-aria overlays stack). Cosmetic pro 'scale-back' dropped — not required for parity (RESEARCH 2.2)."
  - "Local DropZone.Trigger folds the old pro DropZone.Input (accept/multiple/onSelect) into a single FileTrigger + free Button; getDropOperation/onDrop pass through with RAC types verbatim."
  - "panel.test.tsx rewritten against the real Drawer render (dialog role + portaled content) instead of mocking @heroui-pro."

patterns-established:
  - "components/ui/ local wrapper layer for replacing paid primitives with already-installed free packages, zero new deps."

requirements-completed: [UPSTREAM-019]

duration: 20 min
completed: 2026-07-15
---

# Phase 20.1 Plan 01: Segment/Sheet/DropZone → free equivalents Summary

**The three non-carousel `@heroui-pro/react` usages (Segment, Sheet, DropZone) now run on already-installed free packages — `@heroui/react` ToggleButtonGroup/Drawer and `react-aria-components` DropZone/FileTrigger — with zero consumer-file churn and the full web suite green.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-07-15
- **Tasks:** 3 completed
- **Files modified:** 4 modified + 1 created

## Accomplishments
- `recipe-view-mode-toggle.tsx` verified on free `ToggleButtonGroup`/`ToggleButton` (Segment gone; matches the proven `measurement-system-selector` pattern).
- `Panel.tsx` swapped `Sheet` → free `Drawer` 1:1 (Backdrop/Content placement="bottom"/Dialog/Handle/CloseTrigger/Header/Heading/Body/Footer); controlled open mapped via `isOpen`/`onOpenChange` (pass through to react-aria `DialogTrigger`); `nested` retained as API + `data-nested` attribute, both roots now `Drawer.Root`. All 13 consumers untouched.
- New `apps/web/components/ui/drop-zone.tsx`: RAC `DropZone` drag frame + `FileTrigger` picker behind a styled compound (`Area`/`Icon`/`Label`/`Description`/`Trigger`); `import-from-image-modal.tsx` retyped against local `DropZoneAreaProps`, file-handling logic unchanged, unused `OCR_IMAGE_ACCEPT` removed.
- `panel.test.tsx` rewritten against the real Drawer (no more `@heroui-pro` mock).

## Task Commits

1. **Task 1: verify Segment→ToggleButtonGroup** — folded into `56b6d6eb`
2. **Task 2: Panel Sheet→Drawer** — folded into `56b6d6eb`
3. **Task 3: DropZone→RAC + wrapper, fix panel test, commit** — `56b6d6eb` (feat)

**Plan metadata:** this SUMMARY commit (docs).

## Files Created/Modified
- `apps/web/components/ui/drop-zone.tsx` — local styled DropZone compound on react-aria-components.
- `apps/web/components/Panel/Panel.tsx` — bottom-sheet on free Drawer.
- `apps/web/components/dashboard/recipe-view-mode-toggle.tsx` — free ToggleButtonGroup.
- `apps/web/components/shared/import-from-image-modal.tsx` — local DropZone, RAC-typed callbacks.
- `apps/web/__tests__/components/panel.test.tsx` — Drawer-based assertions.

## Verification
- `grep heroui-pro` over the three target components: 0 hits (plan-level verify: OK).
- `pnpm --filter @norish/web exec tsc --noEmit`: EXIT 0.
- `pnpm --filter @norish/web test`: 68 files / **410 tests passed** (panel tests rewritten, not skipped).

## Deviations from Plan

None — plan executed exactly as written. (The vitest `@heroui-pro` stub/alias intentionally remains in place: `media-carousel`/`image-lightbox`/`step-images` still import the pro Carousel until plan 20.1-02; the stub is removed in plan 20.1-03.)

## Next
Ready for **20.1-02** (local embla carousel replaces heroui-pro Carousel).

## Self-Check: PASSED
- `apps/web/components/ui/drop-zone.tsx` exists on disk.
- `git log --grep="20.1-01"` returns the feat commit `56b6d6eb`.
- All task acceptance_criteria re-run green; plan `<verify>` returns OK.
