---
phase: 20.1-replace-heroui-pro
plan: 02
type: execute
wave: 2
depends_on: ["20.1-01"]
files_modified:
  - apps/web/components/ui/carousel.tsx
  - apps/web/components/shared/media-carousel.tsx
  - apps/web/components/shared/image-lightbox.tsx
  - apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx
autonomous: true
requirements: [UPSTREAM-019]
must_haves:
  truths:
    - "A local embla-based Carousel compound (Content/Item/Previous/Next/Dots/Thumbnails/Thumbnail + useCarousel with selectedIndex) exists at apps/web/components/ui/carousel.tsx"
    - "media-carousel, image-lightbox, and step-images import the local carousel — zero @heroui-pro imports — with minimal call-site diffs (compound property aliases preserved)"
    - "Loop mode, swipe, arrows, dots, thumbnails, the --carousel-gap CSS variable, and selectedIndex-gated video playback all behave as before"
    - "@norish/web typecheck EXIT 0 and web tests pass"
  artifacts:
    - path: "apps/web/components/ui/carousel.tsx"
      provides: "Local carousel on embla-carousel-react (already a dep), based on 21st.dev @shadcn/carousel (id 813, MIT) + Dots/Thumbnails/selectedIndex extensions"
      contains: "useEmblaCarousel"
---

<objective>
Port the three carousel surfaces off pro Carousel onto a local embla compound built from the retrieved 21st.dev shadcn carousel source (RESEARCH §2.3 — full source in the research doc's retrieval, adaptations 1-7).

Purpose: the last pro COMPONENT usages disappear; recipe media, the lightbox, and cooking-mode step images run on embla directly.
Output: local ui/carousel.tsx + 3 migrated call sites; typecheck + tests green.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20.1-replace-heroui-pro/20.1-CONTEXT.md
@.planning/phases/20.1-replace-heroui-pro/20.1-RESEARCH.md (§2.3 incl. the 7 adaptations, §3 pitfalls 3/5/6)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build apps/web/components/ui/carousel.tsx</name>
  <files>apps/web/components/ui/carousel.tsx</files>
  <action>
    Start from the 21st.dev @shadcn/carousel source (RESEARCH §2.3; MIT — add a source-attribution comment). Apply the 7 adaptations: compound property aliases (Carousel.Content etc.), selectedIndex in context (selectedScrollSnap on select/reInit), Carousel.Dots (scrollSnapList→scrollTo buttons, active styling, className passthrough), Carousel.Thumbnails/.Thumbnail (scrollTo strip, data-selected), gap via var(--carousel-gap, 1rem) instead of -ml-4/pl-4, free @heroui/react Button for Previous/Next (isIconOnly, ghost), drop shadcn Button/Card/cn deps (use the repo's class-merge idiom). Export { Carousel, useCarousel } with `opts` passthrough to useEmblaCarousel.
  </action>
  <acceptance_criteria>
    - `grep -c "useEmblaCarousel" apps/web/components/ui/carousel.tsx` >= 1; no `@/lib/utils` or shadcn imports.
    - `grep -c "selectedIndex" apps/web/components/ui/carousel.tsx` >= 1.
    - `pnpm --filter @norish/web exec tsc --noEmit` EXIT 0.
  </acceptance_criteria>
  <done>Local carousel compound compiles with the full pro-equivalent surface.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 3 call sites + commit</name>
  <files>apps/web/components/shared/media-carousel.tsx, apps/web/components/shared/image-lightbox.tsx, apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx</files>
  <action>
    In each file change ONLY the import (`@heroui-pro/react` → `@/components/ui/carousel`); the compound aliases keep `Carousel.Content`/`.Item`/`.Previous`/`.Next`/`.Dots`/`.Thumbnails`/`.Thumbnail` and `useCarousel().selectedIndex` working. Fix any prop-shape deltas surfaced by typecheck at the call site (expected: none beyond className types). Run web typecheck + tests; commit as `feat(20.1-02): local embla carousel replaces heroui-pro Carousel`.
  </action>
  <acceptance_criteria>
    - `grep -rc "heroui-pro" apps/web/components/shared/media-carousel.tsx apps/web/components/shared/image-lightbox.tsx "apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx"` returns 0 per file.
    - `pnpm --filter @norish/web test` passes; typecheck EXIT 0.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "heroui-pro" apps/web/components/shared/media-carousel.tsx apps/web/components/shared/image-lightbox.tsx "apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx" && echo OK</automated>
  </verify>
  <done>All carousel surfaces run on the local embla compound.</done>
</task>

</tasks>
