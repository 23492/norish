---
phase: 20.1-replace-heroui-pro
plan: 02
subsystem: ui
tags: [carousel, embla, embla-carousel-react, heroui, recipe-media, lightbox, cooking-mode]

requires:
  - phase: 20.1-replace-heroui-pro
    provides: "plan 01 removed the non-carousel pro usages; carousel was the last pro COMPONENT surface"
provides:
  - "Local embla carousel compound at apps/web/components/ui/carousel.tsx (pro-equivalent API)"
  - "media-carousel, image-lightbox, step-images migrated off @heroui-pro/react Carousel"
affects: [20.1-03]

tech-stack:
  added: []
  patterns:
    - "Local compound carousel on embla-carousel-react with compound-property aliases (Carousel.Content/.Item/.Previous/.Next/.Dots/.Thumbnails/.Thumbnail) + useCarousel() context exposing selectedIndex"

key-files:
  created:
    - apps/web/components/ui/carousel.tsx
  modified:
    - apps/web/components/shared/media-carousel.tsx
    - apps/web/components/shared/image-lightbox.tsx
    - apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx

key-decisions:
  - "Built the local carousel from the shadcn/ui embla base (21st.dev id 813, MIT) with 7 adaptations; call sites changed by import only — compound aliases + useCarousel().selectedIndex keep the pro surface intact."
  - "Preserved the pro data-slot names (carousel-viewport-wrapper, carousel-viewport) so media-carousel's h-full arbitrary-variant CSS keeps working; spacing via var(--carousel-gap, 1rem) preserves [--carousel-gap:0px] overrides."
  - "Carousel.Thumbnails accepts scrollShadowSize for pro-API parity as a visual-only no-op (RAC/embla have no equivalent; not required for parity)."

patterns-established:
  - "useCarousel() throws outside its provider (guards media-carousel's selectedIndex child)."

requirements-completed: [UPSTREAM-019]

duration: 22 min
completed: 2026-07-15
---

# Phase 20.1 Plan 02: Local embla carousel replaces heroui-pro Carousel Summary

**The last `@heroui-pro/react` COMPONENT usage is gone: recipe media, the image lightbox, and cooking-mode step images now run on a local `embla-carousel-react` compound that mirrors the pro Carousel's compound API (Content/Item/Previous/Next/Dots/Thumbnails/Thumbnail + `useCarousel().selectedIndex`), migrated by import-only edits.**

## Performance

- **Duration:** ~22 min
- **Completed:** 2026-07-15
- **Tasks:** 2 completed
- **Files modified:** 3 modified + 1 created

## Accomplishments
- New `apps/web/components/ui/carousel.tsx`: embla-based provider with `opts`/`setApi`/`orientation` passthrough, arrow-key nav, `selectedIndex`/`scrollSnaps` in context, and the full compound (Content emitting `data-slot=carousel-viewport-wrapper`/`carousel-viewport`; Item; free `@heroui/react` Button Previous/Next with prop passthrough for `data-lightbox-interactive`; Dots from `scrollSnapList()`; Thumbnails/Thumbnail scroll-to strip with `data-selected`).
- Migrated the 3 call sites by import only (`@heroui-pro/react` → `@/components/ui/carousel`); no prop-shape deltas required.
- `media-carousel` video-playback gating (`useCarousel().selectedIndex`), lightbox thumbnails + `setApi` sync, and step-images dots/loop all preserved.

## Task Commits

1. **Task 1: build ui/carousel.tsx** — `6fbd7fdf`
2. **Task 2: migrate 3 call sites** — `6fbd7fdf` (feat)

**Plan metadata:** this SUMMARY commit (docs).

## Files Created/Modified
- `apps/web/components/ui/carousel.tsx` — local embla carousel compound.
- `apps/web/components/shared/media-carousel.tsx` — import swap.
- `apps/web/components/shared/image-lightbox.tsx` — import swap (uses `setApi`, Thumbnails).
- `apps/web/app/(app)/recipes/[id]/components/cookingmode/step-images.tsx` — import swap.

## Verification
- `grep heroui-pro` over the 3 call sites: 0 hits (plan-level verify: OK).
- `pnpm --filter @norish/web exec tsc --noEmit`: EXIT 0.
- `pnpm --filter @norish/web test`: 68 files / **410 tests passed**.
- `eslint` on carousel.tsx: 0 warnings/errors (autofixed import grouping).

## Deviations from Plan

None — plan executed exactly as written.

## Next
Ready for **20.1-03** (drop the `@heroui-pro/react` dependency + `HEROUI_AUTH_TOKEN` plumbing; prove the Next.js production build gate). No web source imports `@heroui-pro` anymore, so the vitest stub/alias is now dead and can be removed there.

## Self-Check: PASSED
- `apps/web/components/ui/carousel.tsx` exists on disk.
- `git log --grep="20.1-02"` returns feat commit `6fbd7fdf`.
- All task acceptance_criteria re-run green; plan `<verify>` returns OK.
