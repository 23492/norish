---
phase: 21-ui-polish-media-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/shared/image-lightbox.tsx
  - apps/web/components/shared/media-carousel.tsx
  - apps/web/components/ui/carousel.tsx
  - apps/web/components/navbar/mobile-nav.tsx
  - apps/web/components/calendar/visible-days.ts
  - apps/web/components/calendar/mobile/mobile-timeline.tsx
  - apps/web/components/calendar/desktop/desktop-timeline.tsx
  - apps/web/app/(app)/settings/admin/components/admin-settings-content.tsx
  - apps/web/app/(app)/settings/admin/components/ai-processing-card.tsx
autonomous: true
requirements: [MEDIA-UX-01, UI-POLISH-01]
must_haves:
  truths:
    - "Opening a recipe photo opens a lightbox that carries the FULL media set (images + videos), with counter, arrows and a thumbnail strip for mixed-media sets — videos are no longer dropped."
    - "The lightbox renders videos through the existing VideoPlayer and shows a video thumbnail (poster + play badge) in the strip."
    - "The mobile bottom-nav profile element is a clean circle (not an oval-in-a-pill); the name stays hidden (Phase 13 preserved)."
    - "The calendar (mobile + desktop) does not render empty past days; today and all future days always render; past days with planned items still render."
    - "Admin settings drop the env-backed AI/video config, the auth-providers card and the system/restart card; every UI-only (no-env) setting is preserved."
    - "pnpm --filter @norish/web typecheck EXIT 0, full pnpm typecheck EXIT 0, @norish/web tests green vs baseline, pnpm lint 0 errors, pnpm --filter @norish/web build EXIT 0."
---

<objective>
Phase 21 plannable slice (per 21-CONTEXT.md): media-aware lightbox (MEDIA-UX-01), clean mobile-nav avatar, hide empty past calendar days, and the recommended-default admin-settings reduction. Subtractive polish; new code only where media viewing is actively broken. Out of scope (untouched): the `unoptimized` fetch fix, calendar rows-of-7, the A1 toggle animation, permissions/isolation/realtime.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Media-aware lightbox (MEDIA-UX-01)</name>
  <files>apps/web/components/shared/image-lightbox.tsx, apps/web/components/ui/carousel.tsx, apps/web/components/shared/media-carousel.tsx</files>
  <action>
    Widen the lightbox to a discriminated media type. In image-lightbox.tsx export `LightboxMedia = LightboxImage | LightboxVideo` (image: {type?:"image";src;alt?}; video: {type:"video";src;alt?;poster?;duration?}) and type `images: LightboxMedia[]`. Branch each slide (carousel + single) on `media.type === "video"` → render the existing VideoPlayer (poster/duration) wrapped `data-lightbox-interactive`; else the existing next/image. Extend Carousel.Thumbnail (only consumer is the lightbox) with optional `type` + optional `src`: video thumbnails show the poster (or a dark fallback) with a play badge. In media-carousel.tsx add exported pure `buildLightboxMedia(items)` mapping ALL sorted items (order preserved, videos included), replace `lightboxImages`, drop the `if (item.type !== "image") return` guard in openLightboxForItem, index the lightbox by the slide index. Keep every `unoptimized` (out of scope).
  </action>
  <acceptance_criteria>
    - `grep -n "filter((item) => item.type === \"image\")" apps/web/components/shared/media-carousel.tsx` returns nothing.
    - `grep -c "buildLightboxMedia" apps/web/components/shared/media-carousel.tsx` >= 2.
    - `grep -c "VideoPlayer" apps/web/components/shared/image-lightbox.tsx` >= 1.
    - New unit test proves buildLightboxMedia keeps videos + order; `pnpm --filter @norish/web test` green.
  </acceptance_criteria>
  <done>Lightbox carries the whole media set; mixed sets get counter/arrows/thumbnails.</done>
</task>

<task type="auto">
  <name>Task 2: Clean mobile-nav profile element (no Phase-13 regression)</name>
  <files>apps/web/components/navbar/mobile-nav.tsx</files>
  <action>
    Replace the `h-13 … px-2` oval glass wrapper around NavbarUserMenu with a clean circular container matching the nav height (size-13, centered). No name label is introduced (Phase 13 stays). mobile-nav.test.tsx must stay green.
  </action>
  <acceptance_criteria>
    - `grep -n "px-2" apps/web/components/navbar/mobile-nav.tsx` no longer matches the user-menu wrapper.
    - `pnpm --filter @norish/web test __tests__/components/mobile-nav.test.tsx` green.
  </acceptance_criteria>
  <done>Profile control is a consistent circle; name still hidden.</done>
</task>

<task type="auto">
  <name>Task 3: Hide empty past calendar days</name>
  <files>apps/web/components/calendar/visible-days.ts, apps/web/components/calendar/mobile/mobile-timeline.tsx, apps/web/components/calendar/desktop/desktop-timeline.tsx</files>
  <action>
    Add `filterVisibleDays(days, calendarData, todayKey)` (drops days strictly before today that have no planned items; today + future always kept). Apply in both timelines to the eachDayOfInterval result BEFORE deriving keys/rows/virtualizer/todayIndex (desktop: before row padding). Add a unit test for the helper.
  </action>
  <acceptance_criteria>
    - `grep -c "filterVisibleDays" apps/web/components/calendar/mobile/mobile-timeline.tsx apps/web/components/calendar/desktop/desktop-timeline.tsx` >= 1 each.
    - Helper unit test green.
  </acceptance_criteria>
  <done>Empty past days are not rendered; today/future unaffected; past days with items remain.</done>
</task>

<task type="auto">
  <name>Task 4: Admin settings reduction (recommended default, zero regression)</name>
  <files>apps/web/app/(app)/settings/admin/components/admin-settings-content.tsx, apps/web/app/(app)/settings/admin/components/ai-processing-card.tsx</files>
  <action>
    Cut the settings that have a VERIFIED working env path: remove AuthProvidersCard + SystemCard from admin-settings-content; remove the `ai` (AIConfigForm) and `video` (VideoProcessingForm) accordion items from ai-processing-card (keep prompts + bulk-categorization). Delete the now-orphaned files (auth-providers/, restart-required-chip, system-card, restart-confirmation-modal, ai-config-form, video-processing-form). KEEP ContentDetectionCard, PermissionPolicyCard, GeneralCard, AdminShareLinksCard, prompts, timer-keywords — all UI-only or ❌ no-env. DEVIATION (documented): content-detection is NOT cut despite the inventory listing it env-backed — CONTENT_INDICATORS/UNITS_JSON/CONTENT_INGREDIENTS are declared in the env schema but consumed nowhere, so cutting would be a real regression.
  </action>
  <acceptance_criteria>
    - admin-settings-content.tsx no longer imports AuthProvidersCard or SystemCard.
    - ai-processing-card.tsx no longer imports AIConfigForm or VideoProcessingForm; still renders PromptsForm.
    - `pnpm --filter @norish/web typecheck` EXIT 0 (no dangling imports).
  </acceptance_criteria>
  <done>~60% less admin surface, every no-env setting preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Gates + docs + commits</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/phases/21-ui-polish-media-ux/21-01-SUMMARY.md</files>
  <action>Run all gates. Commit per task. Update ROADMAP checkboxes/status + STATE session log + write SUMMARY. Push main.</action>
  <acceptance_criteria>
    - typecheck (web + full 17), web tests, lint (0 errors), i18n:check (if strings changed), web build all pass.
  </acceptance_criteria>
  <done>Code-complete, documented, pushed.</done>
</task>

</tasks>
