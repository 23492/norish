---
phase: 22-realtime-fan-out-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared-server/__tests__/realtime/fan-out-harness.ts
  - packages/shared-server/__tests__/realtime/fan-out-isolation.test.ts
  - .planning/phases/22-realtime-fan-out-isolation/22-AUDIT.md
autonomous: true
requirements: [REALTIME-ISO-01]
must_haves:
  truths:
    - "A two-household / two-subscriber harness models the REAL channel topology: publish side from TypedRedisEmitter's own channel builders, receive side from createPolicyAwareIterables' household+broadcast+user triple"
    - "A test asserts household B's subscriber receives ZERO recipe-bearing events published for a household A recipe, and that test FAILS against the current tree"
    - "The failure is the real defect (delivery on the broadcast channel), not a harness artifact — proven by asserting the leaking channel name"
    - "NO production file is modified by this plan"
  artifacts:
    - path: "packages/shared-server/__tests__/realtime/fan-out-harness.ts"
      provides: "RecordingEmitter + subscriberChannels() modelling both sides of the bus"
      contains: "subscriberChannels"
    - path: ".planning/phases/22-realtime-fan-out-isolation/22-AUDIT.md"
      provides: "All 54 emit sites tabulated: file, line, event, payload shape, policy source, target key"
      contains: "TOTAL | 54"
---

<objective>
Prove the leak with a test before touching production code, and replace the ROADMAP's
estimated "34 of 54" with a measured, per-site audit.

Purpose: the fix in 22-02/22-03 is only trustworthy if a test went RED first. Success
criterion 3 requires the suite to go RED when the fix is reverted — that is only credible
if it is observed RED *before* the fix exists.
Output: a committed FAILING test + the audit table. Zero production diff.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-realtime-fan-out-isolation/22-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all 54 emit sites</name>
  <files>.planning/phases/22-realtime-fan-out-isolation/22-AUDIT.md</files>
  <action>
    For every `emitByPolicy(` occurrence in packages/queue/src + packages/trpc/src, record:
    file, line, event name, whether the payload carries a full DTO or ids only, the policy
    SOURCE expression, and the target-key expression. Group by file, subtotal queue (38) /
    trpc (16) / TOTAL 54. Explicitly flag every site whose policy source resolves to
    `getRecipePermissionPolicy()` and every site whose `householdKey` is the actor's rather
    than the recipe's — these are the two independent leak vectors in 22-CONTEXT.
  </action>
  <acceptance_criteria>
    - `grep -c "TOTAL | 54" .planning/phases/22-realtime-fan-out-isolation/22-AUDIT.md` >= 1
    - Row count in the table equals `grep -rho "emitByPolicy(" packages/queue/src packages/trpc/src --include=*.ts | wc -l`
  </acceptance_criteria>
  <done>Every emit site is accounted for with its policy source and target key.</done>
</task>

<task type="auto">
  <name>Task 2: Build the two-household / two-socket harness</name>
  <files>packages/shared-server/__tests__/realtime/fan-out-harness.ts</files>
  <action>
    Build a deterministic harness that models BOTH sides of the real bus without Redis:

    - `RecordingEmitter<TEvents>` implementing the TypedRedisEmitter surface used by
      emitByPolicy (`broadcast`, `emitToHousehold`, `emitToUser` + the matching
      `*Event` channel-name builders), recording `{ channel, event, data }`. Channel
      strings MUST be produced by the same `norish:<ns>:<scope>:...` shapes as
      `packages/shared-server/src/redis/pubsub.ts` — derive them by instantiating a real
      `TypedRedisEmitter` for the name builders so the harness cannot drift from production.
    - `subscriberChannels(namespace, userId, householdKey, event)` returning the SAME
      triple `createPolicyAwareIterables` subscribes to: household, broadcast, user.
      Reference `packages/trpc/src/helpers.ts:204-206` in a comment.
    - `deliveredTo(recorder, subscriber, event)` = recorded channels ∩ subscriber channels.

    Two fixture cookbooks: household A (`hh-a`, member `user-a`) and household B (`hh-b`,
    member `user-b`), with a recipe owned by `user-a` in `hh-a`.
  </action>
  <acceptance_criteria>
    - `grep -c "subscriberChannels" packages/shared-server/__tests__/realtime/fan-out-harness.ts` >= 1
    - The harness imports the real `TypedRedisEmitter` for channel-name construction (no hand-written channel literals in the publish path).
  </acceptance_criteria>
  <done>Both sides of the bus are modelled from production code, not restated.</done>
</task>

<task type="auto">
  <name>Task 3: Land the FAILING isolation test</name>
  <files>packages/shared-server/__tests__/realtime/fan-out-isolation.test.ts</files>
  <action>
    With the server-wide policy fixed at the LIVE value `{view:"everyone", edit:"household",
    delete:"household"}` and household A's own policy at `view:"household"`, assert for each
    of the recipe-bearing events `importStarted`, `imported`, `updated`, `ratingUpdated`,
    `created`:

      1. `deliveredTo(recorder, userB, event)` is EMPTY  (the isolation invariant);
      2. `deliveredTo(recorder, userA, event)` is NON-empty (the fix must not silence the
         legitimate recipient — guards against a fix that just stops emitting);
      3. no recorded channel matches `:broadcast:` (D-22-01).

    Add a `deliveredTo(userB)` diagnostic assertion naming the offending channel so the RED
    output identifies `norish:recipes:broadcast:imported` rather than just "expected 0 got 1".

    Run the suite and CONFIRM IT FAILS. Record the exact failing output in the SUMMARY.
    Commit as `test(22-01): failing two-household realtime fan-out isolation harness`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/shared-server test` exits NON-ZERO, failing in fan-out-isolation.test.ts.
    - The failure message names a `:broadcast:` channel.
    - `git diff --name-only main -- packages/shared-server/src packages/queue/src packages/trpc/src` is EMPTY (no production change).
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git diff --name-only main -- packages/shared-server/src packages/queue/src packages/trpc/src)" && echo NO_PROD_CHANGE</automated>
  </verify>
  <done>The leak is reproduced by an automated test, and no production code has moved.</done>
</task>

</tasks>

<self_check>
- Did the test actually run RED, with the output pasted into the SUMMARY? A test asserted
  to fail but never executed is worthless here.
- Is the "user A still receives it" assertion present? Without it, 22-02 could pass by
  deleting emissions.
- Zero production diff?
</self_check>
