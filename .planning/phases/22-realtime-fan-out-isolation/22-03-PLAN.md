---
phase: 22-realtime-fan-out-isolation
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified:
  - packages/trpc/src/routers/recipes/recipes.ts
  - packages/trpc/src/routers/recipes/shares.ts
  - packages/trpc/src/routers/recipes/helpers.ts
  - packages/trpc/src/routers/ratings/ratings.ts
  - packages/trpc/src/helpers.ts
  - packages/trpc/__tests__/realtime/router-fan-out-isolation.test.ts
  - .planning/phases/22-realtime-fan-out-isolation/22-VALIDATION.md
  - AGENTS.md
autonomous: true
requirements: [REALTIME-ISO-01]
must_haves:
  truths:
    - "All 16 tRPC emit sites resolve viewPolicy + ctx from the recipe's own cookbook via resolveRecipeRealtimeScope"
    - "A router-level isolation test proves member B never receives A's recipe-updated / rated / shared events"
    - "The `everyone`-is-not-broadcast rule (D-22-01) is documented where the next contributor will hit it"
    - "Full monorepo typecheck + lint + tests green under sg docker; revert-check RED confirmed"
  artifacts:
    - path: "packages/trpc/__tests__/realtime/router-fan-out-isolation.test.ts"
      provides: "Two-household router-level fan-out isolation suite"
      contains: "fan-out"
---

<objective>
Finish the fix across the tRPC routers, lock the `everyone` semantics in documentation, and
gate the whole phase on a full-monorepo green plus a verified revert-RED.

Purpose: 22-02 closed the queue path; the router path still keys emissions on the ACTOR's
active cookbook, which leaks on the cross-cookbook `saveShared` and rating flows.
Output: 16 migrated sites, a router-level suite, docs, full green.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-realtime-fan-out-isolation/22-CONTEXT.md
@.planning/phases/22-realtime-fan-out-isolation/22-AUDIT.md
@.planning/phases/22-realtime-fan-out-isolation/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the 16 tRPC emit sites</name>
  <files>packages/trpc/src/routers/recipes/recipes.ts, packages/trpc/src/routers/recipes/shares.ts, packages/trpc/src/routers/recipes/helpers.ts, packages/trpc/src/routers/ratings/ratings.ts</files>
  <action>
    Replace every `const policy = await getRecipePermissionPolicy()` + actor-ctx emit with
    `const { viewPolicy, ctx: emitCtx } = await resolveRecipeRealtimeScope(recipeId,
    { userId: ctx.user.id, householdKey: ctx.householdKey })`.

    Site-specific notes from 22-AUDIT:
    - `ratings.ts` — the rate/removeRating handlers only have `recipeId` in scope; that is
      exactly what the resolver takes. `emitRatingFailed` is actor-scoped (fail-closed path).
    - `shares.ts` — the cross-cookbook flows. The `saveShared` emission is about the NEWLY
      created recipe in the SAVER's cookbook; resolve against that new recipe id, not the
      source. Call this out in a comment — it is the subtlest site in the phase.
    - `helpers.ts` `emitRecipeFailure` — actor-scoped by design; pin it to household scope
      and drop the `getRecipePermissionPolicy` read.
    - `recipes.ts` — 9 sites; `create`/`update`/`delete`/`convert` all have the recipe id.
    Drop now-unused `getRecipePermissionPolicy` imports.
  </action>
  <acceptance_criteria>
    - `grep -rc "getRecipePermissionPolicy" packages/trpc/src` returns 0 across the 4 files.
    - `grep -rho "emitByPolicy(" packages/trpc/src --include=*.ts | wc -l` still equals 16.
    - `pnpm --filter @norish/trpc exec tsc --noEmit` EXIT 0.
  </acceptance_criteria>
  <done>No emit site anywhere in the repo reads the server-wide policy.</done>
</task>

<task type="auto">
  <name>Task 2: Router-level two-household isolation suite</name>
  <files>packages/trpc/__tests__/realtime/router-fan-out-isolation.test.ts</files>
  <action>
    Following the idiom of `packages/trpc/__tests__/recipes/permissions-integration.test.ts`
    (hoisted mocks around a REAL boundary), drive the real router emit paths for
    `recipes.update`, `ratings.rate`, and the shares flow with the server-wide policy at the
    live `view:"everyone"`. Reuse the 22-01 harness's `subscriberChannels` so both suites
    model the receive side identically. Assert: user B (household B) receives nothing;
    user A does; no `:broadcast:` channel is ever published to.

    ALSO pin the receive side: assert `createPolicyAwareIterables` still subscribes to the
    broadcast channel (it is not being removed in this phase) — so the test proves the
    isolation comes from the SEND side, which is where the invariant now lives.
  </action>
  <acceptance_criteria>
    - `sg docker -c 'pnpm --filter @norish/trpc test'` EXIT 0 (baseline 269 + the new file).
  </acceptance_criteria>
  <done>The router path is proven isolated at the same fidelity as the queue path.</done>
</task>

<task type="auto">
  <name>Task 3: Document D-22-01, then gate on full green + revert-RED</name>
  <files>AGENTS.md, .planning/phases/22-realtime-fan-out-isolation/22-VALIDATION.md</files>
  <action>
    (a) Add a short "Realtime scoping" rule to AGENTS.md: recipe-bearing realtime events are
    scoped by the RECIPE's own cookbook via `resolveRecipeRealtimeScope`; `view:"everyone"`
    NEVER means socket broadcast; `emitter.broadcast()` is reserved for genuinely global,
    non-resource events. State that new emit sites must go through the resolver — Phases
    24/26 add emit sites and this is the note that stops the regression.

    (b) Write 22-VALIDATION.md recording, with pasted command output: the 22-01 RED, the
    post-fix GREEN, and the revert-check RED->GREEN for BOTH weakenings — (i) restoring
    `emitter.broadcast` in `emitByPolicy`, and (ii) reverting one representative call site
    to `getRecipePermissionPolicy()` — each reverted byte-identical, neither committed.

    (c) Full gate: `pnpm typecheck`, `pnpm lint`, and the package suites (api / auth / web /
    trpc / queue / shared-server / db), db+queue under `sg docker`. Compare against the
    known-green baselines in STATE.md; investigate any delta rather than accepting it.

    Commit as `fix(22-03): scope realtime emissions per cookbook (trpc) + document everyone semantics`.
  </action>
  <acceptance_criteria>
    - `pnpm typecheck` EXIT 0 (17/17); `pnpm lint` EXIT 0 (0 errors).
    - `grep -rn "emitByPolicy" packages --include=*.ts | grep -v __tests__ | grep -c "getRecipePermissionPolicy"` == 0
    - 22-VALIDATION.md contains BOTH revert-check transcripts.
    - Tree clean; no weakening committed.
  </acceptance_criteria>
  <verify>
    <automated>! grep -rq "getRecipePermissionPolicy" packages/queue/src packages/trpc/src && echo NO_SERVERWIDE_EMIT_POLICY</automated>
  </verify>
  <done>Phase 22 success criteria 1-5 are all demonstrably true, with evidence on disk.</done>
</task>

</tasks>

<self_check>
- Are BOTH revert-checks transcribed, not just asserted?
- Does the `saveShared` site resolve against the newly-created recipe (the saver's), not the source?
- Did the emit-site counts hold at 38 + 16 = 54?
- Was anything deployed or any live config touched? It must not have been — this phase is code-only.
</self_check>
