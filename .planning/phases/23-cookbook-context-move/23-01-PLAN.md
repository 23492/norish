---
phase: 23-cookbook-context-move
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [CKBK-MOVE-01]
files_modified:
  - packages/shared/src/contracts/zod/recipe.ts
  - packages/db/src/repositories/recipes.ts
  - packages/trpc/src/routers/recipes/helpers.ts
  - packages/trpc/src/routers/recipes/recipes.ts
  - packages/trpc/__tests__/recipes/move-permissions.test.ts
---

<objective>
Server-side move-a-recipe-between-cookbooks, enforced in the tRPC/repo layer, with the
adversarial isolation suite written FIRST. No schema change.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Failing adversarial move-permission suite (tests-first)</name>
  <files>packages/trpc/__tests__/recipes/move-permissions.test.ts</files>
  <action>
    Mirror permissions-integration.test.ts's mock harness (real canAccessResource +
    assertRecipeAccess; mock getRecipeOwnerAndHousehold / getHouseholdPolicy / getConfig).
    Cover the D-23 matrix against the REAL assertRecipeMoveAllowed (added in Task 3),
    seeding BOTH edit=household AND the edit=everyone sibling:
    - non-member of source → FORBIDDEN (source gate)
    - member, not admin, not owner, edit∈{household,everyone} → FORBIDDEN moving another's recipe
    - source admin → allowed; owner → allowed
    - member cannot move INTO a household they are not a member of → FORBIDDEN
    - non-owner cannot move into Personal (dest=null) → FORBIDDEN
    - no-op (dest === current) → BAD_REQUEST
    Land RED first (assertRecipeMoveAllowed not yet exported).
  </action>
  <acceptance_criteria>
    - The suite fails before Task 3, passes after.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RecipeMoveInputSchema + moveRecipeToHousehold repo fn</name>
  <files>packages/shared/src/contracts/zod/recipe.ts, packages/db/src/repositories/recipes.ts</files>
  <action>
    RecipeMoveInputSchema = { id: uuid, destinationHouseholdId: uuid.nullable(), version: int+ }.
    moveRecipeToHousehold(id, destinationHouseholdId, version): update recipes.householdId,
    bump version, return MutationOutcome; catch the uq_recipes_url_household unique violation
    and throw a typed "DESTINATION_URL_CONFLICT" error.
  </action>
  <acceptance_criteria>
    - pnpm --filter @norish/db typecheck EXIT 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: assertRecipeMoveAllowed + move procedure</name>
  <files>packages/trpc/src/routers/recipes/helpers.ts, packages/trpc/src/routers/recipes/recipes.ts</files>
  <action>
    assertRecipeMoveAllowed(ctx, id, dest): assertRecipeAccess(edit) → fetch owner+household →
    no-op guard → destination membership/owner gate (D-23-03). Return {sourceHouseholdId, ownerId}.
    move procedure: authed; resolve SOURCE realtime scope BEFORE move; moveRecipeToHousehold
    (map DESTINATION_URL_CONFLICT → TRPCError CONFLICT); emit `deleted {id}` to source; resolve
    DESTINATION scope after; emit `created {recipe: dashboardDto}` to destination. Register as
    recipes.move.
  </action>
  <acceptance_criteria>
    - move-permissions suite GREEN; pnpm --filter @norish/trpc typecheck EXIT 0
  </acceptance_criteria>
</task>

</tasks>
</content>
