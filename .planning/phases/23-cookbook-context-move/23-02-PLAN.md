---
phase: 23-cookbook-context-move
plan: 02
type: execute
wave: 2
depends_on: [01]
requirements: [CKBK-MOVE-01]
files_modified:
  - packages/shared-react/src/hooks/recipes/dashboard/use-recipes-mutations.ts
  - packages/shared-react/src/contexts/recipes/recipes-context.tsx
  - apps/web/context/recipes-context.tsx
  - apps/web/components/recipes/cookbook-chip.tsx
  - apps/web/components/recipes/move-recipe-modal.tsx
  - apps/web/components/recipes/readonly-recipe-sections.tsx
  - apps/web/app/(app)/recipes/[id]/recipe-page-desktop.tsx
  - apps/web/app/(app)/recipes/[id]/recipe-page-mobile.tsx
  - apps/web/config/site.ts
  - apps/web/components/navbar/navbar.tsx
  - apps/web/components/navbar/mobile-nav.tsx
  - apps/web/app/(app)/cookbooks/page.tsx
  - packages/i18n/src/messages/*/navbar.json
  - packages/i18n/src/messages/*/recipes.json
---

<objective>
Make the owning cookbook visible on the recipe detail (desktop + mobile), let an authorised
user tap it to move the recipe, and add a Cookbooks browser nav entry. i18n in all 12 locales.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: moveRecipe mutation through the shared factory</name>
  <files>use-recipes-mutations.ts, recipes-context.tsx (shared + web)</files>
  <action>
    Add moveRecipe(id, destinationHouseholdId, version) to createUseRecipesMutations
    (trpc.recipes.move; onSuccess invalidate lists + detail; onError toast). Thread through
    RecipesMutationsResult, the recipes context value, and the web wrapper.
  </action>
  <acceptance_criteria><![CDATA[pnpm --filter @norish/shared-react typecheck EXIT 0]]></acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Cookbook chip + move modal on the detail view</name>
  <files>cookbook-chip.tsx, move-recipe-modal.tsx, readonly-recipe-sections.tsx, recipe-page-{desktop,mobile}.tsx</files>
  <action>
    CookbookChip shows the recipe's cookbook name (or "Personal") from useHouseholdContext
    households; if the viewer canEdit and has >1 write destination it is a button opening
    MoveRecipeModal (a list of destinations = Personal + each membership except current).
    Render the chip in ReadonlyRecipeSummary so both desktop + mobile inherit it.
  </action>
  <acceptance_criteria><![CDATA[grep -q Personal apps/web/components/recipes/cookbook-chip.tsx]]></acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cookbooks nav entry + browser page</name>
  <files>site.ts, navbar.tsx, mobile-nav.tsx, cookbooks/page.tsx</files>
  <action>
    Add /cookbooks to siteConfig.navItems; extend navLabelKeys + the mobile-nav icon map
    (BookOpenIcon). Build /cookbooks: list the user's cookbooks (households list + Personal),
    each a switch-active affordance consistent with the Phase-2 switcher.
  </action>
  <acceptance_criteria><![CDATA[pnpm --filter @norish/web typecheck EXIT 0]]></acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: i18n — all 12 locales</name>
  <files>packages/i18n/src/messages/*/navbar.json, .../recipes.json</files>
  <action>
    Add navbar.nav.cookbooks + a recipes.move.* block to en, then all 11 other locales
    INCLUDING `no` (so no NEW gaps are introduced beyond `no`'s pre-existing 68).
  </action>
  <acceptance_criteria><![CDATA[i18n:check introduces no new missing keys vs the 68-key `no` baseline]]></acceptance_criteria>
</task>

</tasks>
</content>
