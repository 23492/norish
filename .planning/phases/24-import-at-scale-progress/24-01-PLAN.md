---
phase: 24-import-at-scale-progress
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [BULK-01, IMPORT-UX-01]
files_modified:
  - packages/shared/src/lib/helpers.ts
  - packages/shared/src/contracts/dto/queue.ts
  - packages/shared/src/contracts/index.ts
  - packages/shared-server/src/realtime/recipes.ts
  - packages/queue/src/recipe-import/progress.ts
  - packages/queue/src/recipe-import/worker.ts
  - packages/trpc/src/routers/recipes/recipes.ts
  - packages/trpc/src/routers/recipes/recipes-openapi-types.ts
  - packages/trpc/src/routers/recipes/subscriptions.ts
  - packages/queue/__tests__/recipe-import/bulk-fan-out-isolation.test.ts
  - packages/queue/__tests__/recipe-import/progress-isolation.test.ts
  - packages/shared-server/__tests__/realtime/fan-out-isolation.test.ts
---

<objective>
The BACKEND half: a bulk import fan-out procedure that reuses the single-import queue path
(preserving Phase-22.1 per-cookbook dedup + job-id scoping) with per-item outcome reporting,
plus a cookbook-scoped `importProgress` realtime event emitted from the worker — with the
adversarial isolation suites written to seed the LIVE `everyone` policy.

Landed in commit `21cccba8` (backend), retroactively documented here.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: parseBulkImportUrls + cap + RecipeImportStage contract</name>
  <files>packages/shared/src/lib/helpers.ts, packages/shared/src/contracts/dto/queue.ts, packages/shared/src/contracts/index.ts</files>
  <action>
    MAX_BULK_IMPORT_URLS = 25 (D-24-01). parseBulkImportUrls(text) → { urls, truncated }:
    extract http(s) tokens from free text (blog-index friendly), strip trailing prose
    punctuation, validate with httpUrlSchema, dedup with normalizeUrl, cap at the max
    (excess counted as truncated). RECIPE_IMPORT_STAGES = ["fetching","saving"] + the
    RecipeImportStage type; export the value from the contracts index.
  </action>
  <acceptance_criteria>
    - parseBulkImportUrls dedups by normalizeUrl and never returns more than the cap.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: importProgress event + worker emit + subscription</name>
  <files>packages/shared-server/src/realtime/recipes.ts, packages/queue/src/recipe-import/progress.ts, packages/queue/src/recipe-import/worker.ts, packages/trpc/src/routers/recipes/subscriptions.ts</files>
  <action>
    Add `importProgress: { recipeId; url?; stage: RecipeImportStage }` to
    RecipeSubscriptionEvents. New emitImportProgress(scope, payload) — a thin wrapper over
    emitByPolicy(recipeEmitter, scope.viewPolicy, scope.ctx, "importProgress", payload); it
    NEVER broadcasts (D-24-04). The worker reuses the scope it already resolved for the job
    and emits `fetching` before parse, `saving` before createRecipeWithRefs. Add
    onImportProgress to the recipes subscriptions router.
  </action>
  <acceptance_criteria>
    - emitImportProgress routes through emitByPolicy only; no emitter.broadcast() reachable.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: recipes.importFromUrls bulk fan-out procedure</name>
  <files>packages/trpc/src/routers/recipes/recipes.ts, packages/trpc/src/routers/recipes/recipes-openapi-types.ts</files>
  <action>
    recipeImportBulkInputSchema = { urls: url[].min(1).max(MAX_BULK_IMPORT_URLS), forceAI? }.
    importFromUrls: Promise.all over urls → addImportJob per URL (reusing the single-path
    dedup/job-id scoping, each job carrying ctx.household?.id), returning per-item status
    queued/exists/duplicate (+ existingRecipeId on exists). Cap enforced in the schema so
    direct API callers are bounded too.
  </action>
  <acceptance_criteria>
    - Each URL enqueues via the SAME addImportJob path; per-item outcome returned in order.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: adversarial isolation suites (seed the LIVE everyone policy)</name>
  <files>packages/queue/__tests__/recipe-import/bulk-fan-out-isolation.test.ts, packages/queue/__tests__/recipe-import/progress-isolation.test.ts, packages/shared-server/__tests__/realtime/fan-out-isolation.test.ts</files>
  <action>
    bulk-fan-out-isolation: drive the REAL addImportJob/generateJobId against a stateful
    in-memory queue with LIVE_SERVER_POLICY view:"everyone" — two cookbooks importing the
    same URL get distinct job ids (no collision); dedup stays within the actor's cookbook;
    a repeated URL within one cookbook's batch is a duplicate. progress-isolation: drive the
    REAL emitImportProgress + emitByPolicy, every case incl. the `everyone` sibling — proves
    it clamps to the household channel and never broadcasts. Add importProgress to the shared
    RECIPE_EVENTS fan-out matrix.
  </action>
  <acceptance_criteria>
    - All three suites green; every policy-seeding case carries the `everyone` sibling.
  </acceptance_criteria>
</task>

</tasks>
