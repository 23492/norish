---
phase: 24-import-at-scale-progress
plan: 02
type: execute
wave: 2
depends_on: [24-01]
requirements: [BULK-01, IMPORT-UX-01]
files_modified:
  - packages/shared-react/src/hooks/recipes/dashboard/use-recipes-cache.ts
  - packages/shared-react/src/hooks/recipes/dashboard/use-pending-recipes-query.ts
  - packages/shared-react/src/hooks/recipes/dashboard/use-recipes-mutations.ts
  - packages/shared-react/src/hooks/recipes/dashboard/use-recipes-subscription.ts
  - packages/shared-react/src/hooks/recipes/dashboard/use-recipes-query.ts
  - packages/shared-react/src/hooks/recipes/dashboard/index.ts
  - packages/shared-react/src/contexts/recipes/recipes-context.tsx
  - apps/web/context/recipes-context.tsx
  - apps/web/components/skeleton/recipe-card-skeleton.tsx
  - apps/web/components/dashboard/recipe-grid.tsx
  - apps/web/components/shared/import-recipe-modal.tsx
  - apps/web/__tests__/components/import-recipe-modal.test.tsx
  - apps/web/__tests__/hooks/recipes/use-recipes-mutations.test.ts
  - apps/web/__tests__/hooks/recipes/use-recipes-subscription.test.ts
  - packages/i18n/src/messages/*/common.json
---

<objective>
The FRONTEND half: consume the backend contract from 24-01 — a client-only import-stage
cache fed by the cookbook-scoped `importProgress` subscription, a real progress indicator on
the pending skeleton card (single AND bulk), a bulk `importRecipesFromUrls` mutation, and an
import modal that accepts many URLs with a per-item outcome summary. i18n in all 12 locales.

The shared-react data layer was in-flight (uncommitted) when the prior session was
interrupted; it was reconciled with the 24-01 contract and finished forward.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: client-only import-stage cache + progress subscription (shared-react)</name>
  <files>use-recipes-cache.ts, use-pending-recipes-query.ts, use-recipes-subscription.ts, use-recipes-query.ts, index.ts</files>
  <action>
    IMPORT_STAGES_QUERY_KEY — a queryFn-less client cache (staleTime/gcTime Infinity, no
    refetch), written by setImportStage/clearImportStage. onImportProgress subscription →
    setImportStage(recipeId, stage); removePendingRecipe clears the stage (import landed).
    usePendingRecipesQuery exposes importStages as a Map; threaded through useRecipesQuery.
  </action>
  <acceptance_criteria>
    - importStages reflects the last stage per pending recipe; cleared when it lands.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: bulk import mutation + context surface (shared-react)</name>
  <files>use-recipes-mutations.ts, recipes-context.tsx, index.ts</files>
  <action>
    importRecipesFromUrls(urls, forceAI?) → trpc.recipes.importFromUrls; onSuccess registers
    a pending skeleton per `queued` item (exists/duplicate get none — surfaced as a summary).
    Export BulkImportResult / BulkImportResultItem / BulkImportItemStatus; add importStages +
    importRecipesFromUrls to SharedRecipesContextValue.
  </action>
  <acceptance_criteria>
    - queued items become pending skeletons; the result is returned for a caller summary.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: progress indicator + bulk modal (apps/web)</name>
  <files>recipe-card-skeleton.tsx, recipe-grid.tsx, recipes-context.tsx, import-recipe-modal.tsx</files>
  <action>
    RecipeCardSkeleton gains an optional `stage` → an indeterminate spinner + stage label
    overlay. recipe-grid passes importStages.get(id) per pending skeleton. Web Ctx type adds
    importStages + importRecipesFromUrls. Import modal switches to a multiline TextArea,
    parses with parseBulkImportUrls (detected-count + over-cap preview), routes 1 URL through
    the existing single path and >1 through importRecipesFromUrls with a per-item summary toast.
  </action>
  <acceptance_criteria>
    - Single and bulk imports show the honest stage; the modal accepts many URLs.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: i18n (all 12 locales) + test-mock updates</name>
  <files>packages/i18n/src/messages/*/common.json, the three apps/web test files</files>
  <action>
    Add common.import.url.{bulkLabel,bulkPlaceholder,detected,overCap,bulk.*} +
    common.import.progress.{fetching,saving} to ALL 12 locales. Extend the trpc mocks in the
    three affected web tests for the new importFromUrls / onImportProgress procedures (and the
    pre-existing `move` mock gap from Phase 23) and add a TextArea to the modal test's heroui
    mock.
  </action>
  <acceptance_criteria>
    - i18n:check introduces ZERO new gaps; the three web test files are green.
  </acceptance_criteria>
</task>

</tasks>
