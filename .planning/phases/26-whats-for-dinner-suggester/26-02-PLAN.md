# 26-02 PLAN — Dinner suggester: dashboard card + rater bubble + i18n (DINNER-01, frontend)

**Requirement:** DINNER-01 (frontend half)
**DB:** none.

## Context (grounded in code)
- Dashboard hooks are built by `createDashboardRecipeHooks` (packages/shared-react) and
  surfaced to web via `apps/web/hooks/recipes/*` re-exports (see `useRandomRecipe`).
- `RecipeRaters` + `useRecipeRatersQuery` already render the RATE-01 gated rater list;
  `UserAvatar` + a static-star row are the reusable primitives for the thought-bubble.
- The home page (`apps/web/app/(app)/page.tsx`) renders `TodaysMeals` then the recipe
  library — the suggestion card slots between them.

## Decisions
- **D-26-05** rater bubble uses the already-gated `ratings.getRaters` (no new name path).
- **D-26-06** placement = dashboard, under Today's meals; "Another idea" cycles the returned
  set client-side (no refetch); renders nothing on an empty library.

## Tasks
1. `packages/shared-react/src/hooks/recipes/dashboard/use-dinner-suggestion.ts` (new):
   `createUseDinnerSuggestion` (`useQuery` on `recipes.dinnerSuggestion`) + `DinnerSuggestion`
   type; wire into `dashboard/index.ts` factory + type re-exports.
2. `apps/web/hooks/recipes/use-dinner-suggestion.ts` (new) + export from
   `apps/web/hooks/recipes/index.ts`.
3. `apps/web/components/dashboard/dinner-suggestion.tsx` (new): headline card (image, name,
   season chip, stars) + `RaterThoughtBubble` (avatar + "{name} rated this" + stars from
   `useRecipeRatersQuery`) + "Another idea"; hides when no suggestion / `showRatings` off.
4. `apps/web/app/(app)/page.tsx`: render `<DinnerSuggestion />` under `<TodaysMeals />`.
5. i18n: `recipes.dinner.*` (`title`, `anotherIdea`, `seasonalPick`, `openRecipe`,
   `raterThought`, `seasons.{spring,summer,autumn,winter}`) in ALL 12 locales; `i18n:check`.

## Gates (acceptance)
- `pnpm typecheck` 17/17 EXIT 0.
- `@norish/web` + `@norish/shared-react` suites green (no net-new failures).
- `pnpm lint` 0 errors. `pnpm --filter @norish/web build` EXIT 0.
- `i18n:check` EXIT 0, or exit 1 SOLELY on the pre-existing `no` gap (proven).

## Verify
`pnpm --filter @norish/web build` → EXIT 0; `pnpm i18n:check` → only pre-existing `no` gap;
`pnpm typecheck` → 17/17.

## Env note (hoisted-linker)
`node_modules/@norish/{shared-server,shared-react}` are root-owned injected copies. Existing
`src/**` files are hardlinked (edits live), but NEW files + `package.json` export changes go
STALE for cross-package runtime resolution (vitest/next). Re-sync after adding a new module:
overwrite claude-owned copies directly; create new files in the root-owned dirs via a root
container (`docker run --rm -v /opt/norish-src:/w alpine cp …` + `chown 1000:1000`).
