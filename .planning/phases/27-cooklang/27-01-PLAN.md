---
phase: 27-cooklang
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [COOK-01]
autonomous: true
cross_ai: false
files_modified:
  - packages/shared/src/contracts/zod/cook-tokens.ts
  - packages/shared/src/contracts/zod/index.ts
  - packages/shared/src/contracts/zod/recipe.ts
  - packages/shared/src/contracts/dto/recipe.d.ts
  - packages/shared/src/lib/ingredient-token.ts
  - packages/shared/src/cooklang/types.ts
  - packages/shared/src/cooklang/serialize.ts
  - packages/shared/src/cooklang/index.ts
  - packages/shared/package.json
  - packages/shared/__tests__/contracts/cook-tokens.test.ts
  - packages/shared/__tests__/cooklang/fixtures.ts
  - packages/shared/__tests__/cooklang/serialize.test.ts
  - packages/shared-react/src/text/ingredient-links.ts
  - packages/shared-server/src/cooklang/parse.ts
  - packages/shared-server/package.json
  - packages/shared-server/__tests__/cooklang/parse.test.ts
  - packages/shared-server/__tests__/cooklang/round-trip.test.ts
  - pnpm-lock.yaml

must_haves:
  truths:
    - "structuredToCooklang is importable from @norish/shared/cooklang, is pure, and emits canonical unit IDs into the %unit literal (D-8)."
    - "A .cook string produced by structuredToCooklang parses under the REAL @cooklang/cooklang WASM parser and yields the SAME per-step ingredient amounts and units it was given."
    - "parseCookSource returns a plain-JSON cookTokens projection with ingredient/timer indices already dereferenced, and returns null (never throws) on invalid input."
    - "FullRecipeSchema carries nullable cookSource + cookTokens that default to null, so every existing producer keeps parsing unchanged."
    - "No renderer, procedure, repository query, permission check or migration changed: the app behaves exactly as before this plan."
  artifacts:
    - packages/shared/src/cooklang/serialize.ts
    - packages/shared/src/contracts/zod/cook-tokens.ts
    - packages/shared-server/src/cooklang/parse.ts
    - packages/shared-server/__tests__/cooklang/round-trip.test.ts
  key_links:
    - "@norish/shared/cooklang -> @norish/shared/lib/unit-localization (real normalizeUnit; NOT a vendored stand-in)"
    - "@norish/shared-server/cooklang/parse -> @cooklang/cooklang@^0.18.7 (WASM, MIT), a REAL pnpm workspace dependency present in pnpm-lock.yaml"
    - "@norish/shared/contracts/zod/cook-tokens -> FullRecipeSchema.cookTokens (nullable, default null, unwired)"
---

<objective>
**Phase 27 — WAVE 1 of 7: "Serializer + parser read-model" (`27-PLAN.md` §7, row W1).**

Productionize the committed spike serializer into `@norish/shared`, make
`@cooklang/cooklang` a real workspace dependency, add a server-side `parse → cookTokens`
utility, and add nullable `cookSource` / `cookTokens` to the recipe contracts.

**Purpose:** put the two halves of the Cooklang round-trip — the writer (serializer) and the
reader (parser projection) — into the real package graph, proven against the REAL WASM parser,
so W2 can wire them into the write path and the read DTO without discovering integration
problems mid-migration.

**Output:** a pure `structuredToCooklang` in `@norish/shared/cooklang`; a
`parseCookSource`/`toCookTokens` util in `@norish/shared-server/cooklang/parse`; a
`CookTokens` zod contract; nullable `cookSource`/`cookTokens` on `FullRecipeSchema`; a
round-trip test suite against `@cooklang/cooklang@^0.18.7`.

**W1 IS PURELY ADDITIVE. The app must not change behaviour.** Existing renderers keep using
the heuristic `SmartInstruction` / `applyIngredientLinkMarkup` path — W1 deletes nothing from
the runtime. No migration (DB stays at 41). No consumer reads the new DTO fields.

**Hard scope fence — these belong to W2–W6 and MUST NOT be touched:** the `0041` migration,
`deriveProjectionTx`, any write-path wiring, extraction/prompt changes, the token renderer,
multi-timer, deleting the heuristic markup layer, deleting `unit-converter.ts`, the backfill.
Decisions D-27-W1-01..06 in `27-CONTEXT.md` are locked — implement them, do not relitigate.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-cooklang/27-CONTEXT.md
@.planning/phases/27-cooklang/27-PLAN.md
@.planning/phases/27-cooklang/27-DECISIONS.md
@.planning/phases/27-cooklang/waves/W0-SUMMARY.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CookTokens contract + nullable cookSource/cookTokens on the recipe DTO (interface-first, unwired)</name>
  <files>packages/shared/src/contracts/zod/cook-tokens.ts, packages/shared/src/contracts/zod/index.ts, packages/shared/src/contracts/zod/recipe.ts, packages/shared/src/contracts/dto/recipe.d.ts, packages/shared/__tests__/contracts/cook-tokens.test.ts</files>
  <read_first>
    packages/shared/src/contracts/zod/recipe.ts
    packages/shared/src/contracts/zod/steps.ts
    packages/shared/src/contracts/zod/index.ts
    packages/shared/src/contracts/dto/recipe.d.ts
    .planning/phases/27-cooklang/27-PLAN.md
  </read_first>
  <action>
    Define the read-model contract FIRST so tasks 2–4 build against a fixed shape (interface-first).

    New `cook-tokens.ts` in the zod contracts dir, mirroring the file style of its siblings
    (`steps.ts`, `recipe-ingredients.ts`). Three schemas, discriminated on `type`:
    a text token (`type:"text"`, `value`), an ingredient token (`type:"ingredient"` with
    `name`, nullable `amount`, nullable `unit` — `unit` is the CANONICAL norish unit ID per
    D-8, never a localized label), and a timer token (`type:"timer"` with nullable `name`,
    nullable `amount`, nullable `unit`). Compose them into a per-step token list schema that
    also carries the step `order` and an optional `section` heading name, and then into the
    recipe-level `CookTokens` array schema. Everything is plain JSON — no class instances, no
    parser indices, nothing that cannot cross superjson.

    Barrel-export from `contracts/zod/index.ts` in the existing style.

    Extend `FullRecipeSchema` with `cookSource` (nullable string) and `cookTokens` (nullable
    CookTokens array). BOTH MUST carry `.default(null)` — per D-27-W1-05, `FullRecipeSchema` is
    `.parse()`d on the hot read path at `packages/db/src/repositories/recipes.ts:234` and
    `:1182` and is the tRPC `.output()` at `packages/trpc/src/routers/recipes/recipes.ts:155,172`;
    a merely-nullable key would be REQUIRED and would break every existing producer. Do NOT
    source these from the drizzle table — `recipes.cook_source` does not exist until W2's `0041`.
    Do NOT add them to `FullRecipeInsertSchema` / `FullRecipeUpdateSchema` (that is W2's write path).

    Add the matching DTO types to `contracts/dto/recipe.d.ts` in the existing
    `z.output<typeof Schema>` style (`CookTokenDTO`, `CookStepTokensDTO`, `CookTokensDTO`).

    Test file asserts the shape and, critically, the backward-compatibility property.
  </action>
  <acceptance_criteria>
    - `FullRecipeSchema.parse(...)` on a payload that contains NEITHER `cookSource` NOR
      `cookTokens` succeeds and yields `cookSource === null` and `cookTokens === null`.
      (This is the regression that proves W1 did not break the read path.)
    - A well-formed `cookTokens` payload round-trips through the schema unchanged; a malformed
      token (`type:"nope"`, or an ingredient token missing `name`) is REJECTED.
    - `CookTokens` contains no parser index field and no class instance — it is plain JSON.
    - `pnpm --filter @norish/db test zodSchemas` and the existing
      `packages/shared/__tests__/contracts/version-contracts.test.ts` still pass unchanged.
    - `git diff` for this task touches ONLY the five files listed — no schema/migration file,
      no repository, no router.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/shared test cook-tokens &amp;&amp; pnpm --filter @norish/shared test version-contracts &amp;&amp; sg docker -c 'pnpm --filter @norish/db test zodSchemas' &amp;&amp; pnpm typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move structuredToCooklang into @norish/shared on the REAL unit helpers, with the spike fixtures as pure tests</name>
  <files>packages/shared/src/cooklang/types.ts, packages/shared/src/cooklang/serialize.ts, packages/shared/src/cooklang/index.ts, packages/shared/src/lib/ingredient-token.ts, packages/shared/package.json, packages/shared-react/src/text/ingredient-links.ts, packages/shared/__tests__/cooklang/fixtures.ts, packages/shared/__tests__/cooklang/serialize.test.ts</files>
  <read_first>
    .planning/phases/27-cooklang/spike/structured-to-cooklang.ts
    .planning/phases/27-cooklang/spike/fixtures.ts
    packages/shared/src/lib/unit-localization.ts
    packages/shared-react/src/text/ingredient-links.ts
    packages/shared/src/units/index.ts
    packages/shared/package.json
  </read_first>
  <behavior>
    - Emits `\n\n` between steps (single newlines merge steps in Cooklang) and `== Heading ==`
      for a norish `#`-prefixed step.
    - Emits `@name{qty%unit}` with the CANONICAL unit ID in `%unit`; `@name{qty}` when there is
      no unit; bare `@salt` for a single-word amount-less ingredient and `@sea salt{}` for a
      multi-word one.
    - Longest-ingredient-name-first matching: in "stir the brown sugar into the sugar syrup",
      "brown sugar" is tokenised as itself and NOT as "sugar".
    - An ingredient ref with no textual anchor in the step prose is APPENDED as a trailing token
      and reported with `placement:"appended"` (the honest failure mode, not a silent drop).
    - Emits YAML frontmatter carrying `norish.system: metric|us` (D-2).
    - A raw unit that is a known alternate (e.g. `"gr"`, `"EL"`) is normalized to its canonical
      ID before it is written into `%unit`.
    - Purity: the same input yields a byte-identical string on every call; no locale, clock,
      randomness, I/O or db handle is involved.
  </behavior>
  <action>
    Port `.planning/phases/27-cooklang/spike/structured-to-cooklang.ts` into
    `packages/shared/src/cooklang/`, split into `types.ts` (the input/output types, renamed off
    the `Spike*` prefix to norish-facing names) and `serialize.ts` (`structuredToCooklang` plus
    the `serializeWithReport` variant that returns the per-ingredient `LinkOutcome[]` — keep it,
    W5's confidence gate needs it). Barrel in `index.ts`. Add `"./cooklang": "./src/cooklang/index.ts"`
    to the `@norish/shared` package.json exports map (the package has explicit per-path exports;
    without the entry the module is not importable).

    Replace the spike's vendored stand-ins with the REAL helpers, per D-27-W1-03/04:
    - Units: import `normalizeUnit` from `@norish/shared/lib/unit-localization` and run every
      incoming unit through it (with the `UnitsMap` server-config passed in as an argument, the
      same way `normalizeUnit`/`formatUnit` are called everywhere else) so the emitted `%unit` is
      guaranteed canonical. Do NOT call `formatUnit` to build `%unit` — it returns a
      locale-dependent display string and would destroy the D-8 verbatim round-trip; `formatUnit`
      is the read-side counterpart and is exercised in Task 4. Make the units config an optional
      argument so the function stays usable (and identity-behaved) without it.
    - Name/amount helpers: `@norish/shared` CANNOT import `@norish/shared-react` (enforced by
      `pnpm deps:cycles`), so MOVE the two pure helpers `normalizeIngredientLinkName` and the
      private `formatTokenAmount` down into a new `packages/shared/src/lib/ingredient-token.ts`
      and have `packages/shared-react/src/text/ingredient-links.ts` import + RE-EXPORT
      `normalizeIngredientLinkName` from there so every existing consumer is unaffected. This is a
      move + re-export with ZERO behaviour change. Do NOT touch, change or delete
      `applyIngredientLinkMarkup`, `createIngredientLinkCandidates` or `IngredientLinkCandidate`
      — their removal is W6.

    Port `spike/fixtures.ts` to `packages/shared/__tests__/cooklang/fixtures.ts` (all five
    recipes and their per-step `expected` amounts — this is the eval material the round-trip
    suite in Task 4 also consumes). Write `serialize.test.ts` covering every `<behavior>` bullet
    as PURE string assertions — no parser, no WASM in this package (`@norish/shared` is consumed
    by `apps/mobile` and its vitest env is jsdom).

    Do NOT add any dependency to `@norish/shared`. Do NOT import `@norish/shared/units` here —
    the serializer converts nothing (that is W2's derive step).
  </action>
  <acceptance_criteria>
    - `structuredToCooklang` is importable as `@norish/shared/cooklang` and the export-map entry
      exists in `packages/shared/package.json`.
    - Every `<behavior>` bullet is asserted by at least one test, including an explicit
      longest-name-first case and an explicit `placement:"appended"` case.
    - Calling `structuredToCooklang` twice with the same input returns identical strings.
    - `packages/shared/package.json` `dependencies` gained NOTHING (no `@cooklang/*` in
      `@norish/shared` — it would ship WASM into the Expo bundle).
    - `pnpm deps:cycles` exits 0 (proves the helper move did not create a shared→shared-react edge).
    - The full `@norish/shared` suite has ZERO net-new failures against its 257-test W0 baseline,
      and the full `@norish/shared-react` suite still passes (proves the re-export is transparent).
    - `applyIngredientLinkMarkup` / `createIngredientLinkCandidates` are byte-identical to HEAD~.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/shared test &amp;&amp; pnpm --filter @norish/shared-react test &amp;&amp; pnpm deps:cycles &amp;&amp; pnpm typecheck &amp;&amp; pnpm --filter @norish/shared lint</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add @cooklang/cooklang as a real workspace dep and build the server-side parse → cookTokens util</name>
  <files>packages/shared-server/package.json, packages/shared-server/src/cooklang/parse.ts, packages/shared-server/__tests__/cooklang/parse.test.ts, pnpm-lock.yaml</files>
  <read_first>
    .planning/phases/27-cooklang/27-DECISIONS.md
    .planning/phases/27-cooklang/spike/structured-to-cooklang.test.ts
    .planning/phases/27-cooklang/e2e-harness/package.json
    packages/shared-server/package.json
    packages/shared-server/vitest.config.ts
    packages/shared/src/contracts/zod/cook-tokens.ts
    apps/web/tsdown.config.ts
  </read_first>
  <action>
    **Dependency.** Add `@cooklang/cooklang` at `^0.18.7` to `packages/shared-server`
    dependencies **with pnpm** (`pnpm --filter @norish/shared-server add @cooklang/cooklang@^0.18.7`,
    or edit + `pnpm install --lockfile-only` following the W0 `convert` precedent) so
    `pnpm-lock.yaml` genuinely gains the entry — it is currently absent from the lockfile and
    exists only inside the two standalone npm sandbox projects under `.planning/`. NEVER npm/yarn
    (CLAUDE.md). Before adding, verify provenance: confirm the published license is MIT and the
    package resolves to `cooklang/cooklang-rs` (`pnpm view @cooklang/cooklang@0.18.7 license repository`)
    — record both in the SUMMARY (T-27-SC).

    **Host justification (D-27-W1-01, already decided — implement, do not re-derive):**
    `@norish/shared-server`, NOT `@norish/api`. `@norish/api` is a leaf that itself depends on
    `@norish/trpc`, so the util would be unreachable from the trpc recipe procedures and the
    queue import worker that consume it in W2. `@norish/shared-server` is a dependency of trpc,
    queue, auth, api and web, and already hosts server-only recipe logic
    (`recipes/randomizer`, `recipes/dinner-suggester`). It must NOT go in `@norish/shared`,
    which `apps/mobile` bundles.

    **The util.** `packages/shared-server/src/cooklang/parse.ts`, exporting a
    `parseCookSource(cookSource)` that returns the parsed recipe projection and a
    `toCookTokens(...)` that produces a value conforming to Task 1's `CookTokens` contract.
    Use `new CooklangParser().parse(src)` → `[CooklangRecipe, report]` (no default export; the
    class is `CooklangParser`). Dereference each `item.index` into `recipe.ingredients[i]` /
    `recipe.timers[i]` — an item carries ONLY an index, so a token model that leaked the index
    would be useless to a client. Read quantities via `getQuantityValue` / `getQuantityUnit`.
    Project `CooklangRecipe` (a CLASS instance) into plain JSON. Carry `== Heading ==` section
    names onto the step tokens. Run the parsed `%unit` back through `normalizeUnit`
    (`@norish/shared/lib/unit-localization`) per D-8. Instantiate the parser lazily and reuse it
    (module-level singleton) so the WASM module is initialised once, not per request.

    **Failure mode is part of the contract:** invalid/empty/garbage input returns `null` and
    NEVER throws into a caller — W2 puts this on a request path. Use the existing logger pattern
    (`@norish/shared-server/logger`), never `console.log`.

    Add `"./cooklang/parse": "./src/cooklang/parse.ts"` to the shared-server exports map.

    **Retire the WASM risks empirically — do not assume (see `27-CONTEXT.md` `<risks>` 1 and 2).**
    `@cooklang/cooklang` is a `wasm-pack --target bundler` build whose `pkg/cooklang_wasm.js`
    does `import * as wasm from "./cooklang_wasm_bg.wasm"`; plain Node ESM needs
    `--experimental-wasm-modules`. In-repo evidence is contradictory (the serializer spike runs
    vitest with no flag; the e2e harness sets `NODE_OPTIONS=--experimental-wasm-modules`). RUN
    the test and observe. If a vitest-config change or a node flag is required for
    `@norish/shared-server`, LAND that configuration as part of this task — no TODO, no skipped
    test. Then confirm the server bundle leg with `pnpm --filter @norish/web build:server`
    (tsdown only, seconds — `noExternal` is `@norish/*`, so `@cooklang/cooklang` stays external
    and must resolve from node_modules). Record BOTH outcomes in the SUMMARY. The full
    `pnpm docker:build` in-image WASM confirmation is the DIRECTOR's job per CLAUDE.md — do not
    run it; flag it as a W1 exit item.

    **Hoisted-linker:** this task adds a NEW module plus a `package.json` export-map change to a
    root-owned injected workspace — exactly the shape that broke Phase 26. Re-sync
    `node_modules/@norish/shared-server` after the edit and re-run the cross-package checks.
  </action>
  <acceptance_criteria>
    - `pnpm-lock.yaml` contains `@cooklang/cooklang` resolved at 0.18.x, and
      `packages/shared-server/package.json` lists it in `dependencies`. No npm/yarn artifacts.
    - License confirmed MIT and repository confirmed `cooklang/cooklang-rs`, both recorded in the SUMMARY.
    - `parseCookSource` on a known-good `.cook` returns per-step tokens whose ingredient entries
      carry a dereferenced `name` + `amount` + canonical `unit` — NO raw parser index anywhere in
      the output, and the result is `structuredClone`-able plain JSON.
    - `parseCookSource("")`, `parseCookSource("@@@{{{")` and `parseCookSource(null as never)`
      return `null` and throw NOTHING.
    - Timers and `== Heading ==` sections appear in the projection.
    - The output validates against Task 1's `CookTokens` zod schema (assert this in the test —
      it is the seam that would otherwise silently drift).
    - **WASM viability is demonstrated, not asserted:** the test suite genuinely executes the
      real parser and passes, and `pnpm --filter @norish/web build:server` exits 0. Whatever
      config was needed to achieve that is committed.
    - `@norish/shared` did NOT gain the dependency; `pnpm deps:cycles` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/shared-server test cooklang &amp;&amp; grep -c '@cooklang/cooklang' pnpm-lock.yaml &amp;&amp; pnpm deps:cycles &amp;&amp; pnpm typecheck &amp;&amp; pnpm --filter @norish/web build:server</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Round-trip suite — serializer output through the REAL WASM parser, on the spike's eval fixtures</name>
  <files>packages/shared-server/__tests__/cooklang/round-trip.test.ts</files>
  <read_first>
    .planning/phases/27-cooklang/spike/structured-to-cooklang.test.ts
    .planning/phases/27-cooklang/spike/fixtures.ts
    .planning/phases/27-cooklang/27-EXPERIMENT.md
    packages/shared/__tests__/cooklang/fixtures.ts
    packages/shared/src/lib/unit-localization.ts
  </read_first>
  <action>
    This is the test that justifies the whole wave: it is exactly what the spike de-risked, now
    run against the REAL package graph with the REAL helpers substituted in. It lives in
    `@norish/shared-server` (D-27-W1-02) because that package has BOTH halves — it depends on
    `@norish/shared` and owns the `@cooklang/cooklang` dependency — and because its vitest
    environment is `node` (`@norish/shared`'s is jsdom, a poor host for a WASM node module).

    Port `spike/structured-to-cooklang.test.ts`, driving `structuredToCooklang` from
    `@norish/shared/cooklang` and the real `CooklangParser`. For each of the five ported
    fixtures assert:
    - the emitted `.cook` parses with no error and produces the expected number of non-heading
      steps (blank-line separation actually working);
    - **per-step amount fidelity**: for every step, the ingredient names, amounts and units the
      parser reports equal the fixture's `expected` entries. This is the D-8 verbatim-round-trip
      claim, tested rather than trusted;
    - the canonical unit ID survives the round trip UNCHANGED (`gram` in → `gram` out — the
      parser must not have localized, pluralized or converted it);
    - `== Heading ==` steps come back as section names, not as prose steps.

    Add the read-side counterpart assertion (D-27-W1-03): feed the parser's output unit + amount
    through the REAL `formatUnit(unit, locale, unitsConfig, amount)` from
    `@norish/shared/lib/unit-localization` and assert the expected human-readable label for at
    least two locales — proving the canonical-ID-in-`.cook` / localize-at-render split works
    end to end and that no localized string ever entered the `.cook`.

    Where a fixture is one of the known irreducibly-lossy split-amount / multi-step cases from
    `27-EXPERIMENT.md`, assert the DOCUMENTED behaviour (including `placement:"appended"`) and
    reference the experiment in a comment. Do NOT relax an assertion to make a fixture pass — a
    genuine round-trip regression is a finding about the helper substitution, and it must be
    surfaced, not accommodated.
  </action>
  <acceptance_criteria>
    - All five ported fixtures round-trip: structured → `.cook` → real WASM parser → the SAME
      per-step ingredient names, amounts and units.
    - At least one assertion proves a canonical unit ID (`gram`, `tablespoon`) survives verbatim.
    - At least one assertion proves `formatUnit` renders the parsed canonical unit correctly in
      two different locales.
    - Section headings and timers are asserted.
    - No assertion was weakened, skipped, `.todo`'d or `.skip`'d relative to the spike's suite;
      any behavioural difference versus the spike is called out explicitly in the SUMMARY.
    - The suite runs the REAL parser (no mock, no stub, no fake).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/shared-server test cooklang &amp;&amp; pnpm typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description | Crossed in W1? |
|----------|-------------|----------------|
| client → API (tRPC) | untrusted input crosses here | **No** — no procedure added or changed |
| viewer → recipe (HOUSE-06 per-cookbook read boundary) | `buildViewPolicyCondition` / `canAccessResource` / `emitByPolicy` | **No** — no query, policy, emit site or `where` clause touched |
| user recipe text → parser | untrusted text reaching the WASM parser | **Not yet** — the util has no caller in W1; first crossed in W2 |
| npm registry → build | third-party code entering the image | **YES** — one new dependency (`@cooklang/cooklang`) |

**Scoping statement (deliberate, and NOT a request to skip the gate):** W1 adds a pure
function, a pure parse projection over a string the caller already holds, and two contract
fields nobody reads. It adds no procedure, no emit site, no query, no route handler, no
permission check and no migration. The serializer and the parse util accept **no `ctx`, no
household id, no user id and no db handle** — if a task needs one, it has drifted out of scope.
HOUSE-06 and the `view:"everyone"` clamp are therefore untouched *by construction*, and the
executor must verify that claim against the diff rather than assume it.

**Adversarial revert-check (weaken the boundary → suite RED → revert byte-identical):
N/A THIS WAVE, as a reasoned exclusion rather than an omission** — there is no boundary
predicate in the W1 diff to weaken. It returns in force in **W2**, where `deriveProjectionTx`
first writes rows and `cookTokens` first reaches a scoped procedure. Record this N/A explicitly
in the SUMMARY so the phase-level security review can see it was considered.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-27-SC | Tampering | `pnpm add @cooklang/cooklang@^0.18.7` — a NEW dep shipping a compiled WASM binary (`pkg/cooklang_wasm_bg.wasm`) | mitigate | Verify before adding: published license is **MIT**, repository is **`cooklang/cooklang-rs`** (the official Rust parser), version is the `0.18.7` line the spike validated, and it is **NOT** the archived `cooklang-ts`. Pin via `pnpm-lock.yaml`; record license + repo + resolved version in the SUMMARY. Task 3 acceptance criterion. |
| T-27-01 | Denial of Service | `parseCookSource` on hostile/malformed `.cook` | mitigate (partial, deliberate) | The util returns `null` and never throws, so a malformed input cannot escalate into a request-path exception. Input-size limiting and rate considerations are **W2's** responsibility — that is where untrusted text first reaches the parser. Recorded so W2 inherits it rather than rediscovering it. |
| T-27-02 | Information Disclosure | `cookSource` / `cookTokens` on `FullRecipeSchema` | accept | Both default to `null` and no producer sets them in W1, so the DTO carries no new data. They inherit the recipe's existing per-cookbook scope the moment W2 populates them — no new read path is created here. |
| T-27-03 | Tampering | Moving `normalizeIngredientLinkName` / `formatTokenAmount` down into `@norish/shared` | mitigate | Move + re-export with zero behaviour change; the full `@norish/shared-react` suite must stay green and `applyIngredientLinkMarkup` / `createIngredientLinkCandidates` must be byte-identical to HEAD~ (Task 2 acceptance criteria). |
</threat_model>

<verification>
## Phase gates (all must be green before the SUMMARY is written)

- `pnpm typecheck` → **17/17 EXIT 0**.
- `pnpm --filter @norish/shared test` → zero net-new failures vs the 257-test W0 baseline.
- `pnpm --filter @norish/shared-react test` → green (proves the helper move is transparent).
- `pnpm --filter @norish/shared-server test cooklang` → green, running the REAL WASM parser.
- `pnpm deps:cycles` → EXIT 0 (no forbidden workspace edge, no cycle).
- `pnpm --filter @norish/shared lint` and `pnpm --filter @norish/shared-server lint` → **0 errors**
  (warnings at the existing baseline; new files contribute 0).
- `pnpm --filter @norish/web build:server` → EXIT 0 (tsdown; proves the external
  `@cooklang/cooklang` resolves in the server bundle).
- `pnpm i18n:check` → unchanged; W1 adds **no user-facing strings**, so the only failure allowed
  is the pre-existing `no`-locale gap. Zero new gaps.
- **Not run by the executor:** `pnpm docker:build`. Per CLAUDE.md the build is the director's
  job. The in-image WASM confirmation is a **W1 exit item for the director**, flagged in the
  SUMMARY, not a task here.

## Additive-safety checks (the never-broken guarantee)

- `git diff --stat` against the plan's start touches ONLY the files in `files_modified`.
- `git diff` contains **no** file under `packages/db/src/migrations/` (DB stays at migration 41),
  **no** change to `packages/db-schema`, **no** change to any file under
  `apps/web/app/(app)/recipes/`, `apps/web/components/recipe/`, `apps/mobile/`, and **no**
  deletion in `packages/shared-react/src/text/ingredient-links.ts`.
- The heuristic render path, `unit-converter.ts`, the timer-keyword scan and the extraction
  prompts are all untouched.
</verification>

<success_criteria>
- `structuredToCooklang` lives in `@norish/shared/cooklang`, is pure, uses the REAL
  `normalizeUnit`/`unit-localization` module (not the spike's stand-ins), and emits canonical
  unit IDs into `%unit` per D-8.
- `@cooklang/cooklang@^0.18.7` (MIT, WASM) is a real pnpm workspace dependency of
  `@norish/shared-server`, present in `pnpm-lock.yaml`, with its license and provenance recorded.
- `@norish/shared-server/cooklang/parse` turns a `.cook` string into a plain-JSON `cookTokens`
  projection with indices dereferenced, and returns `null` rather than throwing on bad input.
- `FullRecipeSchema` carries nullable `cookSource` + `cookTokens` defaulting to `null`, and every
  existing producer still parses without supplying them.
- The round-trip suite proves structured → `.cook` → REAL WASM parser preserves per-step amounts
  and canonical units, on the spike's five eval fixtures.
- The app's behaviour is unchanged: nothing deleted, nothing rewired, no migration, renderers
  still on the old path.
- Both WASM risks (vitest, server bundle) are retired **empirically**, with the outcomes and any
  required configuration recorded in the SUMMARY; the docker-image leg is handed to the director.
</success_criteria>

<output>
Create `.planning/phases/27-cooklang/27-01-SUMMARY.md` when done.

Also append a short `waves/W1-SUMMARY.md`-style note ONLY if the phase's wave-summary convention
(established by `waves/W0-SUMMARY.md`) is being continued — otherwise the plan SUMMARY is the
single record.

Commit per task using the repo's Conventional Commit style, scope `27-01`
(e.g. `feat(27-01): CookTokens contract + nullable cookSource/cookTokens on the recipe DTO`).
</output>
</content>
