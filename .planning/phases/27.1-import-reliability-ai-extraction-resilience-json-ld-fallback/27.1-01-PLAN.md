---
phase: 27.1-import-reliability-ai-extraction-resilience-json-ld-fallback
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [IMPORT-REL-01, IMPORT-REL-02]
autonomous: true
cross_ai: false
files_modified:
  - packages/api/src/ai/features/recipe-extraction/normalizer.ts
  - packages/api/src/ai/recipe-parser.ts
  - packages/api/src/ai/image-recipe-parser.ts
  - packages/api/src/video/normalizer.ts
  - packages/api/src/parser/index.ts
  - packages/queue/src/api-handlers.ts
  - packages/api/__tests__/ai/features/recipe-extraction/normalizer.test.ts
  - packages/api/__tests__/server/parser/ai-retry.test.ts

must_haves:
  truths:
    - "A metric-only extraction — 10 metric ingredients, 0 US ingredients, metric steps, no US steps — IMPORTS. It is no longer rejected as `Recipe extraction failed - missing required fields` (D-27.1-01 sub-cause 1, D-27.1-03)."
    - "The absent measurement half is MIRRORED, not invented and not dropped: after mirroring, `recipeIngredient.us` deep-equals `recipeIngredient.metric` and `recipeInstructions.us` deep-equals `recipeInstructions.metric`, so the recipe still lands with BOTH systems populated and the dual-system DTO contract downstream is unchanged."
    - "Mirroring is SYMMETRIC. A US-only extraction mirrors us -> metric, so the relaxed rule (`name` + metric ingredients + metric instructions) holds for either single-system shape rather than only the observed one."
    - "NO OTHER VALIDATION IS WEAKENED. A nameless output, an empty object, `null`, and an output with both systems empty each still fail with the SAME error string and the same `VALIDATION_ERROR` code as today."
    - "THE RETRY GETS 100 000 OUTPUT TOKENS, CLAMPED TO A MEASURED PROVIDER CEILING. `AI_RETRY_OUTPUT_TOKEN_TARGET` is 100 000 by explicit product decision (Kiran, 2026-07-28 — token cost is not a constraint); `AI_PROVIDER_MAX_OUTPUT_TOKENS` is 393 216, MEASURED against the live key, so `Math.min` resolves to 100 000 and the figure is used exactly. The clamp exists because a `max_tokens` above a provider's cap is rejected HTTP 400, which would turn the retry into a guaranteed hard failure."
    - "A transient AI failure is retried EXACTLY ONCE per input shape, and only for transient codes. `AI_DISABLED`, `AUTH_ERROR` and `INVALID_INPUT` are never retried — retrying a missing key or a disabled feature burns tokens and latency for a guaranteed second failure."
    - "The retry — and ONLY the retry — runs with raised output-token headroom, so reasoning-token exhaustion (D-27.1-01 sub-cause 2) gets a genuinely different attempt rather than an identical coin flip. A healthy configuration is untouched, because the raised budget is only ever reached after an attempt has already failed (D-27.1-06)."
    - "`packages/shared-server/src/config/defaults.ts` is NOT modified. It is inert on live — `getGenerationSettings` reads `maxOutputTokens` from the DB `ai_config` row, not from the default (D-27.1-06, finding F-2)."
    - "SCOPE FENCE: no schema change, no migration, no prompt change, no dependency change (`git diff pnpm-lock.yaml` EMPTY), no JSON-LD fallback (that is 27.1-02), no worker or client change (that is 27.1-03)."
  artifacts:
    - path: packages/api/src/ai/features/recipe-extraction/normalizer.ts
      provides: "The symmetric measurement-system mirror and the relaxed extraction validator"
      exports: ["mirrorMeasurementSystems", "validateExtractionOutput"]
    - path: packages/api/src/parser/index.ts
      provides: "The single-retry envelope around AI extraction, with raised headroom on the retry only"
      contains: "RETRYABLE_AI_EXTRACTION_CODES"
    - path: packages/api/src/ai/recipe-parser.ts
      provides: "An optional per-call output-token override, used only by the retry"
      exports: ["extractRecipeWithAI"]
    - path: packages/api/__tests__/ai/features/recipe-extraction/normalizer.test.ts
      provides: "Proof that metric-only succeeds and mirrors, and that empty/nameless still rejects"
    - path: packages/api/__tests__/server/parser/ai-retry.test.ts
      provides: "Proof of exactly-one retry, transient-only retry, and raised headroom on the retry"
  key_links:
    - from: "packages/api/src/ai/recipe-parser.ts"
      to: "mirrorMeasurementSystems"
      via: "applied to the raw model output BEFORE validate/normalize/buildCook, so all three read the same mirrored object"
      pattern: "mirrorMeasurementSystems\\("
    - from: "packages/api/src/parser/index.ts"
      to: "packages/api/src/ai/recipe-parser.ts (extractRecipeWithAI)"
      via: "the retry passes an outputTokenFloor the first attempt does not"
      pattern: "outputTokenFloor"
---

<objective>
Close sub-causes 1, 2 and 3 of the intermittent import failure inside the AI extraction path:
stop discarding complete single-system recipes, and give a transient failure one genuinely different
second chance instead of none.

Purpose: today a correct, complete metric recipe is thrown away because the model did not also emit a
US half, and a single reasoning-token overrun ends the whole import. Both are silent to the user.
Output: a relaxed + mirroring normalizer, and a one-shot retry with raised headroom, both unit-proven.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-CONTEXT.md
@packages/api/src/ai/features/recipe-extraction/normalizer.ts
@packages/api/src/ai/recipe-parser.ts
@packages/api/src/parser/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror the absent measurement half; relax the validator to name + metric</name>
  <files>packages/api/src/ai/features/recipe-extraction/normalizer.ts, packages/api/src/ai/recipe-parser.ts, packages/api/src/ai/image-recipe-parser.ts, packages/api/src/video/normalizer.ts, packages/api/__tests__/ai/features/recipe-extraction/normalizer.test.ts</files>
  <read_first>
    packages/api/src/ai/features/recipe-extraction/normalizer.ts (validateExtractionOutput at 167-195; normalizeExtractionOutput at 208-306 — note it reads `output.recipeIngredient.us` at 250-253 and `output.recipeInstructions.us` at 222; buildCookFromExtraction at 422-494 — note it reads `output.recipeInstructions?.[systemUsed]` and `output.recipeIngredient?.[systemUsed]` at 430-431)
    packages/api/src/ai/schemas/recipe.schema.ts (RecipeExtractionOutput — the exact optionality of recipeIngredient / recipeInstructions and of their metric / us halves)
    packages/api/src/ai/recipe-parser.ts (lines 90-131 — the validate -> normalize -> buildCookFromExtraction sequence, all three reading `jsonLd`)
    packages/api/src/ai/image-recipe-parser.ts (lines 115-145 — the identical sequence)
    packages/api/src/video/normalizer.ts (lines 95-145 — the identical sequence)
    packages/api/__tests__/ai/features/recipe-extraction/normalizer.test.ts (the existing fixture shape and assertion style to mirror)
  </read_first>
  <behavior>
    - `mirrorMeasurementSystems` on a metric-only output returns an output whose `recipeIngredient.us` deep-equals `recipeIngredient.metric` and whose `recipeInstructions.us` deep-equals `recipeInstructions.metric`.
    - `mirrorMeasurementSystems` on a US-only output mirrors the other way: `recipeIngredient.metric` deep-equals `recipeIngredient.us`, `recipeInstructions.metric` deep-equals `recipeInstructions.us`.
    - The two lists mirror INDEPENDENTLY: an output with both ingredient systems but only metric instructions mirrors the instructions and leaves the ingredients untouched.
    - `mirrorMeasurementSystems` on an output where both systems are populated returns it unchanged (deep-equal to the input).
    - `mirrorMeasurementSystems` does not mutate its argument — the caller's object is deep-equal before and after.
    - `mirrorMeasurementSystems(null)` returns `null`; an output missing `recipeIngredient` entirely does not throw.
    - `validateExtractionOutput` on a MIRRORED metric-only output is `{ valid: true }`, and its `details` still report the real `usIngredients` / `usSteps` counts of the object it was handed.
    - `validateExtractionOutput` on a metric-only output that was NOT mirrored is also `{ valid: true }` — the rule is `name` + `recipeIngredient.metric.length` + `recipeInstructions.metric.length`, nothing more.
    - Still INVALID, each with the unchanged `"Recipe extraction failed - missing required fields"` string: no `name`; `name` but zero metric ingredients; `name` but zero metric instructions.
    - Still INVALID with the unchanged `"AI returned empty response"` string: `null`, and `{}`.
  </behavior>
  <action>
    (a) In `packages/api/src/ai/features/recipe-extraction/normalizer.ts`, export a new PURE function

        `export function mirrorMeasurementSystems(output: RecipeExtractionOutput | null): RecipeExtractionOutput | null`

    It returns `null` for `null`. Otherwise it returns a NEW object (shallow clone, with new
    `recipeIngredient` / `recipeInstructions` objects) — never a mutation of its argument, because
    `getExtractionLogContext` and the callers' own logging read the same reference.

    For `recipeIngredient` and for `recipeInstructions` INDEPENDENTLY: if exactly one of the two halves
    is a non-empty array and the other is absent or empty, copy the non-empty half's array onto the
    empty side (a fresh array; the element references may be shared, the elements are not mutated). If
    both are non-empty, or both are empty/absent, leave that pair exactly as it was.

    Header comment states D-27.1-03 and D-27.1-01 sub-cause 1 with the observed live shape
    (`metricIngredients: 10, usIngredients: 0`), and states WHY mirroring beats rejecting: the recipe is
    complete and correct in one system, and the dual-system DTO downstream only needs both halves to be
    populated, not to differ.

    (b) In the same file, relax `validateExtractionOutput`'s required-fields condition to

        `!output.name || !output.recipeIngredient?.metric?.length || !output.recipeInstructions?.metric?.length`

    Delete the two `us` clauses. Change NOTHING else: the empty-response branch, both error strings, and
    the whole `details` object (including `usIngredients` and `usSteps`, which stay as observability) are
    untouched. Add a comment naming D-27.1-03 and stating that the `us` half is supplied by
    `mirrorMeasurementSystems`, which runs BEFORE this.

    (c) At each of the THREE extraction call sites, apply the mirror to the raw model output ONCE, and
    pass the mirrored value to validate, normalize AND `buildCookFromExtraction` — all three must see the
    same object, or `buildCookFromExtraction` can read an empty `output.recipeInstructions[systemUsed]`
    when `normalized.systemUsed` resolves to the half the model omitted, and silently mint no `.cook`:
      - `packages/api/src/ai/recipe-parser.ts` (~line 94): replace `const jsonLd = result.output;` with a
        mirrored binding and use it at lines ~97, ~103, ~111 and ~129.
      - `packages/api/src/ai/image-recipe-parser.ts` (~line 119) — same treatment.
      - `packages/api/src/video/normalizer.ts` (~line 100) — same treatment.
    Keep each call site's existing variable naming and the existing non-null assertions; this is a
    two-line change per site, not a refactor.

    (d) Extend `packages/api/__tests__/ai/features/recipe-extraction/normalizer.test.ts` with a
    `describe("mirrorMeasurementSystems")` and a `describe("validateExtractionOutput")` covering every
    line of `<behavior>`. The metric-only fixture must reproduce the LIVE failure shape — 10 metric
    ingredients, 0 US, metric instructions, 0 US instructions — and assert `valid: true` plus
    `details.metricIngredients === 10`. Do not weaken or delete an existing assertion in this file.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/api test` reports **436 baseline + the new tests, 0 failed** (baseline measured 2026-07-28 at HEAD `a1e51a7c`: 436 passed / 31 files). Record the exact new total in the SUMMARY.
    - A test named for the live shape asserts that an output with 10 metric ingredients and 0 US ingredients is `valid: true` AFTER mirroring AND `valid: true` WITHOUT mirroring.
    - A test asserts `mirrorMeasurementSystems` output has `recipeIngredient.us` `toEqual` `recipeIngredient.metric` for a metric-only input, and the symmetric assertion for a US-only input.
    - A test asserts the input object passed to `mirrorMeasurementSystems` is `toEqual` a structuredClone taken before the call — proof of non-mutation.
    - Tests assert the three still-invalid cases return error `"Recipe extraction failed - missing required fields"` and the two empty cases return `"AI returned empty response"` — the exact strings, `toBe`.
    - `grep -n "recipeIngredient?.us?.length\|recipeInstructions?.us?.length" packages/api/src/ai/features/recipe-extraction/normalizer.ts` prints NOTHING inside `validateExtractionOutput` (the `details` object may still reference `?.us?.length`; verify by reading, not by a bare count).
    - `grep -c "mirrorMeasurementSystems" packages/api/src/ai/recipe-parser.ts packages/api/src/ai/image-recipe-parser.ts packages/api/src/video/normalizer.ts` prints a non-zero count for EACH of the three files.
    - `pnpm exec tsc --noEmit` run from inside `packages/api` is clean. (`pnpm typecheck` passes `--noCheck` for this package and proves nothing here.)
    - `pnpm --filter @norish/api lint` — **0 errors**; warnings at the measured baseline.
    - `git diff --stat packages/shared-server/src/config/defaults.ts` is EMPTY (D-27.1-06).
    - `git diff --stat pnpm-lock.yaml` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/api test &amp;&amp; pnpm --filter @norish/api lint &amp;&amp; pnpm --filter @norish/api exec tsc --noEmit</automated>
  </verify>
  <done>A complete metric-only extraction imports with a mirrored US half; a nameless or genuinely empty extraction still rejects with byte-identical errors; all three extraction call sites read one mirrored object.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One retry on transient AI failure, with raised output headroom on the retry only</name>
  <files>packages/api/src/parser/index.ts, packages/api/src/ai/recipe-parser.ts, packages/queue/src/api-handlers.ts, packages/api/__tests__/server/parser/ai-retry.test.ts</files>
  <read_first>
    packages/api/src/parser/index.ts (tryExtractWithAI at 69-95 — it already holds `result.code` and discards it; extractWithAIPreference at 101-129; the useAIOnly throw at 242-248; the structured-failure AI fallback at 255-270)
    packages/api/src/ai/recipe-parser.ts (extractRecipeWithAI's signature and the `...settings` spread into generateText at ~line 84-92)
    packages/shared-server/src/ai/providers/factory.ts (getGenerationSettings at 215-223 — maxOutputTokens comes from getAIConfig(true), i.e. the DB ai_config row; and the GenerationSettings type)
    packages/shared-server/src/ai/types/result.ts (AIErrorCode — the full union; mapErrorToCode's classification rules)
    packages/queue/src/api-handlers.ts (QueueApiHandlers.extractRecipeWithAI at 61-67 — the handler contract that must stay satisfiable)
    packages/api/__tests__/server/parser/import-flow.test.ts (how this suite mocks `extractRecipeWithAI` and drives `parseRecipeFromUrl` — mirror that harness exactly)
  </read_first>
  <behavior>
    - A first attempt returning `{ success: false, code: "PROVIDER_ERROR" }` is followed by exactly ONE more call to `extractRecipeWithAI` for that same input; a second failure returns `null` and there is no third call.
    - A first attempt returning `{ success: false, code: "EMPTY_RESPONSE" }` retries; so does `"TIMEOUT"`, `"NETWORK_ERROR"`, `"RATE_LIMIT"` and `"UNKNOWN"`.
    - A first attempt returning `{ success: false, code: "AI_DISABLED" }` does NOT retry — exactly one call.
    - `"AUTH_ERROR"` does NOT retry. `"INVALID_INPUT"` does NOT retry.
    - `"VALIDATION_ERROR"` DOES retry: after Task 1 the validator only rejects genuinely unusable output, and the observed failures were a truncated/empty generation classified through the same code.
    - A first attempt that SUCCEEDS results in exactly one call and no retry.
    - The retry call receives an `outputTokenFloor` argument; the first call does not.
    - `extractRecipeWithAI` with no `options` argument produces generation settings byte-identical to today's (`maxOutputTokens` exactly `getGenerationSettings()`'s value).
    - `extractRecipeWithAI` with `{ outputTokenFloor: N }` produces `maxOutputTokens = Math.max(configuredValue ?? 0, N)`, and leaves `temperature` and `abortSignal` untouched.
    - `parseRecipeFromUrl` on a URL whose AI extraction fails transiently TWICE for the JSON-LD input and TWICE for the full-HTML input still throws `AI extraction failed` in the `useAIOnly` branch — the retry adds attempts, it does not swallow a final failure.
  </behavior>
  <action>
    (a) In `packages/api/src/ai/recipe-parser.ts`, add a trailing OPTIONAL parameter
    `options?: { outputTokenFloor?: number }` to `extractRecipeWithAI`. Where the settings are built,
    replace the bare `...settings` spread with a spread plus an explicit override applied ONLY when
    `options?.outputTokenFloor` is set:

        maxOutputTokens = Math.max(settings.maxOutputTokens ?? 0, options.outputTokenFloor)

    Never lower a configured value; never set the key at all when no floor was passed. Comment states
    D-27.1-06: the DB `ai_config` row is the source of truth, an unconditional floor could exceed a
    provider's output cap, so the raised budget is reachable only on a retry that follows a real failure.

    The optional trailing parameter keeps `QueueApiHandlers.extractRecipeWithAI`
    (`packages/queue/src/api-handlers.ts:61-67`) satisfiable unchanged. Verify that with a typecheck; if
    the handler type must be widened, widen it with the SAME optional parameter and nothing else.

    (b) In `packages/api/src/parser/index.ts`, above `tryExtractWithAI`, declare

        const RETRYABLE_AI_EXTRACTION_CODES = new Set<AIErrorCode>([...])

    holding exactly: `PROVIDER_ERROR`, `EMPTY_RESPONSE`, `TIMEOUT`, `NETWORK_ERROR`, `RATE_LIMIT`,
    `VALIDATION_ERROR`, `UNKNOWN`. Import the `AIErrorCode` type from
    `@norish/shared-server/ai/types/result`. A comment must state, per code, why it is or is not in the
    set — `AI_DISABLED`, `AUTH_ERROR` and `INVALID_INPUT` are deterministic and retrying them burns
    tokens and latency for a guaranteed second failure.

    Also declare the retry's headroom as THREE named constants, so the product intent and the
    provider-cap clamp are separately legible and a future provider swap cannot silently inherit a
    DeepSeek-specific number:

        const AI_RETRY_OUTPUT_TOKEN_TARGET = 100_000;
        const AI_PROVIDER_MAX_OUTPUT_TOKENS = 393_216;
        const AI_RETRY_OUTPUT_TOKEN_FLOOR = Math.min(
          AI_RETRY_OUTPUT_TOKEN_TARGET,
          AI_PROVIDER_MAX_OUTPUT_TOKENS
        );

    `AI_RETRY_OUTPUT_TOKEN_TARGET = 100_000` is an EXPLICIT PRODUCT DECISION by Kiran (2026-07-28):
    token cost is not a constraint here, and a large-HTML reasoning run must not exhaust its budget
    mid-extraction. It supersedes the 32 000 this plan originally carried. The comment must state the
    evidence it answers: DeepSeek spent 470 reasoning tokens on a 104-token prompt (D-27.1-01), a
    dual-system recipe extraction emits several thousand output tokens on top of that, and the live
    `ai_config` budget is 10 000.

    `AI_PROVIDER_MAX_OUTPUT_TOKENS = 393_216` is the CLAMP, and it exists because a request whose
    `max_tokens` exceeds the provider's per-request maximum is REJECTED OUTRIGHT — which would turn the
    retry, the thing that is supposed to rescue a failed extraction, into a guaranteed hard failure. Its
    comment must carry the measurement verbatim, because it is a measured fact and not a guess
    (probed against the live key on 2026-07-28, `api.deepseek.com/chat/completions`):

      - `deepseek-v4-pro` at `max_tokens: 100000` -> HTTP 200, `finish_reason: "stop"`.
      - `deepseek-v4-flash` at `max_tokens: 100000` -> HTTP 200, `finish_reason: "stop"`.
      - `deepseek-v4-pro` at `max_tokens: 393216` -> HTTP 200.
      - `deepseek-v4-pro` at `max_tokens: 393217` -> HTTP 400,
        `{"error":{"message":"Invalid max_tokens value, the valid range of max_tokens is [1, 393216]","type":"invalid_request_error"}}`

    So the ceiling is **393 216**, comfortably above 100 000: `Math.min` resolves to **100 000** and
    Kiran's figure is used EXACTLY, unclamped. The clamp is dead code against today's provider and is
    written for the NEXT one — the comment must say so, and must say that a provider swap requires
    re-measuring `AI_PROVIDER_MAX_OUTPUT_TOKENS` rather than assuming it.

    State that this headroom is applied to a RETRY ONLY, never to the first attempt, and that
    `packages/shared-server/src/config/defaults.ts` stays untouched (D-27.1-06 / finding F-2 —
    `getGenerationSettings` reads `maxOutputTokens` from the DB `ai_config` row, so a defaults change is
    inert on live).

    Note for the executor: `getGenerationSettings` also returns
    `abortSignal: AbortSignal.timeout(config.timeoutMs)` (live default 300 000 ms), which still bounds
    the retry regardless of the token budget. Do not touch it.

    (c) Rewrite the body of `tryExtractWithAI` to make at most TWO attempts. Keep its signature and its
    `Promise<ExtractedRecipe | null>` return type — the retry belongs here rather than at the
    `parser/index.ts:242` throw site because this one edit covers BOTH the `useAIOnly` branch and the
    structured-failure AI fallback, whereas wrapping the throw site would cover only one and would
    re-run the JSON-LD-input attempt as well.

    Attempt 1: exactly as today. On `result.success` return `result.data`.
    On failure: keep the existing `log.warn` (add an `attempt` field). If `result.code` is NOT in
    `RETRYABLE_AI_EXTRACTION_CODES`, return `null` immediately.
    Attempt 2: call `extractRecipeWithAI(...)` again with the SAME arguments plus
    `{ outputTokenFloor: AI_RETRY_OUTPUT_TOKEN_FLOOR }`. Log it at `info` with `{ url, attempt: 2, code }`
    BEFORE the call so the retry is observable in the container logs. On success return `result.data`;
    on failure log `warn` with `attempt: 2` and return `null`.

    Do NOT change `extractWithAIPreference`, and do NOT change the `throw new Error("AI extraction failed")`
    at line ~245 — 27.1-02 owns that line.

    Note the resulting ceiling in the comment: `extractWithAIPreference` already tries two INPUT SHAPES
    (JSON-LD, then full HTML), so a single job attempt now makes at most 4 model calls, and BullMQ's
    `attempts: 3` (`packages/queue/src/config.ts`) bounds a job at 12. That is the accepted cost of
    turning a hard failure into a retryable one.

    (d) Create `packages/api/__tests__/server/parser/ai-retry.test.ts`, mirroring `import-flow.test.ts`'s
    mocking harness. Mock `extractRecipeWithAI` with `vi.fn()`, drive `parseRecipeFromUrl` with
    `forceAI: true` (or a mocked `shouldAlwaysUseAI` returning true) so the `useAIOnly` branch runs, and
    assert CALL COUNTS and the argument shape of each call. Cover every line of `<behavior>`; use a
    `it.each` over the retryable and non-retryable code sets so a future code addition cannot silently
    become retryable. Test `extractRecipeWithAI`'s own floor arithmetic by mocking `getGenerationSettings`
    and asserting the object handed to `generateText`.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/api test` reports **436 baseline + Task 1's tests + this file's tests, 0 failed**. Record the exact new total in the SUMMARY.
    - A test named for exactly-one-retry asserts `mockExtractRecipeWithAI` was called `toHaveBeenCalledTimes(2)` for a transient first failure and `toHaveBeenCalledTimes(1)` for `AI_DISABLED` and for `AUTH_ERROR`.
    - A test asserts the SECOND call's last argument `toEqual({ outputTokenFloor: 100000 })` and the FIRST call's last argument is `undefined`.
    - A test asserts `AI_RETRY_OUTPUT_TOKEN_FLOOR === Math.min(AI_RETRY_OUTPUT_TOKEN_TARGET, AI_PROVIDER_MAX_OUTPUT_TOKENS)` and that it resolves to `100000` against today's constants — so a future ceiling drop below 100 000 fails the assertion loudly instead of silently shrinking the retry.
    - `grep -c '393_216\|393216' packages/api/src/parser/index.ts` is >= 1 AND the surrounding comment names the provider, the probe date (2026-07-28) and the verbatim HTTP 400 message — the clamp must read as a measured fact, not a guess.
    - An `it.each` covers all 7 retryable codes and all 3 non-retryable codes explicitly by name.
    - A test asserts `maxOutputTokens` handed to `generateText` is the configured value when no floor is passed, and `Math.max(configured, floor)` when one is — including the case where the configured value is ALREADY above the floor (the floor must not lower it).
    - A test asserts `parseRecipeFromUrl` still throws `AI extraction failed` when every attempt fails.
    - `grep -vE '^\s*(//|\*|/\*)' packages/api/src/parser/index.ts | grep -c "AI_DISABLED\|AUTH_ERROR\|INVALID_INPUT"` prints `0` — the non-retryable codes are named in the comment, and the runtime check is set membership, not a negative branch list.
    - `pnpm --filter @norish/queue typecheck` and `pnpm exec tsc --noEmit` from inside `packages/api` are both clean — proves the optional parameter did not break `QueueApiHandlers`.
    - `pnpm --filter @norish/api lint` — **0 errors**; warnings at baseline.
    - `git diff --stat packages/shared-server/src/config/defaults.ts` is EMPTY.
    - `git diff --stat pnpm-lock.yaml` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/api test &amp;&amp; pnpm --filter @norish/api lint &amp;&amp; pnpm --filter @norish/api exec tsc --noEmit &amp;&amp; pnpm --filter @norish/queue typecheck</automated>
  </verify>
  <done>A transient AI extraction failure gets exactly one more attempt with raised output headroom; a deterministic failure gets none; a final failure still throws.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote recipe page → `extractRecipeWithAI` | Untrusted third-party HTML enters the prompt. |
| AI provider → `validateExtractionOutput` | Untrusted, model-authored JSON enters the recipe DTO. |
| `parseRecipeFromUrl` → AI provider (network egress) | Retries multiply outbound calls and token spend. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-01-01 | Tampering | `validateExtractionOutput` | mitigate | The relaxation removes ONLY the two `us` length clauses. `name` and both metric clauses stay, the empty-response branch stays, and `normalizeRecipeFromJson` + `FullRecipeInsertSchema.safeParse` in `createRecipeWithRefs` remain the actual write-path validators. Pinned by the three still-invalid tests. |
| T-27.1-01-02 | Tampering | `mirrorMeasurementSystems` | mitigate | The mirror COPIES an existing half; it never synthesises a value and never converts units, so no fabricated quantity can enter a recipe. Non-mutation is asserted against a `structuredClone` taken before the call. |
| T-27.1-01-03 | Denial of Service | AI retry loop | mitigate | Retry count is a hard 1, gated on a closed set of codes. The resulting ceiling (4 model calls per job attempt, 12 per job under BullMQ `attempts: 3`) is stated in code and pinned by call-count assertions. `AbortSignal.timeout(config.timeoutMs)` from `getGenerationSettings` still bounds every call. |
| T-27.1-01-04 | Denial of Service | `outputTokenFloor` exceeding a provider's per-request cap | mitigate | A `max_tokens` above the provider's maximum is rejected HTTP 400, which would convert the retry — the mechanism meant to RESCUE a failed extraction — into a guaranteed hard failure. Mitigated by clamping the 100 000 target to a MEASURED ceiling: `deepseek-v4-pro` and `deepseek-v4-flash` accept 100 000 (HTTP 200, `finish_reason: "stop"`), and the API states its own range as `[1, 393216]` (HTTP 400 at 393 217), probed against the live key 2026-07-28. `Math.min` therefore resolves to 100 000 unclamped today; the clamp is written for the next provider and a swap requires re-measuring it. The raised budget is still reachable only AFTER an attempt has already failed, and `AbortSignal.timeout(config.timeoutMs)` still bounds the call (D-27.1-06). |
| T-27.1-01-05 | Information Disclosure | retry logging | mitigate | The new log lines carry `url`, `attempt` and `code` only — the same fields the existing `log.warn` carries. No recipe prose, no ingredient name, no prompt body (T-27-05). |
| T-27.1-01-06 | Elevation of Privilege | per-cookbook isolation | accept | This plan touches no permission check, no `householdId`, no realtime emit and no query. `grep -c "householdId\|everyone\|emitByPolicy"` over the diff is 0. No isolation surface is reached. |
| T-27.1-01-SC | Tampering | npm/pip/cargo installs | mitigate | NO package is installed by this plan. `git diff --stat pnpm-lock.yaml` EMPTY is an acceptance criterion on both tasks, so the Package Legitimacy Gate has nothing to audit. |
</threat_model>

<verification>
- `pnpm --filter @norish/api test` — 436 baseline + new tests, 0 failed.
- `pnpm --filter @norish/api lint` — 0 errors.
- `pnpm exec tsc --noEmit` from inside `packages/api`; `pnpm --filter @norish/queue typecheck`.
- `git diff --stat pnpm-lock.yaml packages/shared-server/src/config/defaults.ts` EMPTY.
- `git diff --stat packages/db packages/db-schema` EMPTY — no schema or migration touched.
</verification>

<success_criteria>
A metric-only extraction imports with a mirrored US half. A nameless or empty extraction still rejects
with byte-identical errors. A transient AI failure gets exactly one retry, at raised output headroom;
a deterministic failure gets none; a final failure still throws. No config default, no dependency, no
schema and no isolation surface is touched.
</success_criteria>

<output>
Create `.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-01-SUMMARY.md` when done.
Record: the measured pre-change and post-change `@norish/api` test totals, the lint warning baseline,
and confirmation that `defaults.ts` and `pnpm-lock.yaml` are untouched.
</output>
