---
phase: 27.1-import-reliability-ai-extraction-resilience-json-ld-fallback
plan: 04
type: execute
wave: 1
depends_on: []
requirements: [IMPORT-REL-05, SETUP-04]
autonomous: true
cross_ai: false
files_modified:
  - docker/docker-compose.fork.yml
  - docker/docker-compose.beta.yml
  - tooling/fork-stack/README.md

must_haves:
  truths:
    - "THE LIVE STACK'S SHAPE IS NOW IN THE REPO. `docker/docker-compose.fork.yml` mirrors `/opt/norish/docker-compose.fork.yml` (services `norish` / `db` / `redis`, container names `norish-app` / `norish-db` / `norish-redis`, image tag `norish:live`, port 3000, the `norish_default` network) and ADDS the in-stack `camofox` service. Until this plan, the file that runs production existed nowhere under version control."
    - "CAMOUFOX IS IN-STACK. The `camofox` service builds from the VENDORED `docker/camofox` source (SETUP-04: no external image, no registry pull — the published 1.8-1.11 images regressed on Akamai), is reachable as `http://camofox:9377` from inside the app's network, and `norish` declares `depends_on: camofox`."
    - "`CAMOFOX_URL` REMAINS AN EXPLICIT OVERRIDE. It is written as `${CAMOFOX_URL:-http://camofox:9377}`, so the in-stack service is the default and an operator can still point at an external Camoufox (e.g. LXC 105) by setting the variable — the behaviour CLAUDE.md describes, and the behaviour `packages/config/src/env-config-server.ts:249-252` already defaults to."
    - "THE ARCHITECTURE DEFECT IS NAMED IN THE FILE. A header comment records that live worked only because `CAMOFOX_URL` pointed off-stack at LXC 105 while `norish-app` sat on `norish_default` and `norishp2-camofox-1` sat on `norishp2_default`, unreachable from it — an undocumented single point of failure. The Camoufox SERVICE was healthy throughout; the defect was topology, not the browser (D-27.1-08)."
    - "NOTHING LIVE IS TOUCHED. This plan writes repo files and a runbook only. No task runs `docker compose`, `docker run`, `docker stop`, `pct exec` or any ssh command against the live host. Live cutover is the director's separate, deliberate step (CLAUDE.md)."
    - "NO CHROME. The compose introduces no `chrome-headless` service, no Playwright/Chromium image and no boot-time bundle patch (CLAUDE.md hard constraint)."
    - "THE BETA STACK STOPS MIRRORING THE SPOF. `docker/docker-compose.beta.yml:38`'s `${CAMOFOX_URL:-http://192.168.2.26:9377}` hardcoded the off-stack address as the DEFAULT; the default becomes the in-stack service and the LXC-105 address survives only as a documented, commented override (finding F-3)."
    - "SCOPE FENCE: no application code, no schema, no dependency (`git diff pnpm-lock.yaml` EMPTY), no change to `docker/Dockerfile` or `docker/camofox/**`, and no change to `docker/docker-compose.example.yml`, which ALREADY defines camofox correctly at lines 139-159."
  artifacts:
    - path: docker/docker-compose.fork.yml
      provides: "The repo-tracked shape of the live fork stack, with the in-stack camofox service"
      contains: "camofox"
    - path: tooling/fork-stack/README.md
      provides: "The operator runbook for adopting the in-stack camofox on live, with the reachability proof and the rollback"
  key_links:
    - from: "docker/docker-compose.fork.yml"
      to: "docker/camofox"
      via: "build context — the vendored camofox-browser v1.4.1 source, per SETUP-04"
      pattern: "context: \\./camofox"
    - from: "docker/docker-compose.fork.yml (norish service)"
      to: "the camofox service"
      via: "CAMOFOX_URL default plus depends_on"
      pattern: "CAMOFOX_URL: \\$\\{CAMOFOX_URL:-http://camofox:9377\\}"
---

<objective>
Put the live stack's shape under version control and give it the in-stack Camoufox service the fork's
architecture has always assumed, without touching live.

Purpose: `packages/config` already defaults `CAMOFOX_URL` to the in-stack service and the tracked
example compose already defines it — but the file that actually runs production is untracked, defines
only norish/db/redis, and works solely because an env var points off-stack at another LXC. That is an
undocumented single point of failure, and it is invisible to anyone reading the repo.
Output: a repo-tracked fork compose with camofox, a corrected beta default, and an operator runbook.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-CONTEXT.md
@docker/docker-compose.example.yml
@docker/docker-compose.beta.yml
@docker/compose.base.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Track the fork stack in the repo, with camofox in it</name>
  <files>docker/docker-compose.fork.yml, docker/docker-compose.beta.yml</files>
  <read_first>
    docker/docker-compose.example.yml (the WHOLE file — the `camofox` service at 139-159 with its shm_size and healthcheck, `depends_on: camofox` at 120-123, `CAMOFOX_URL: http://camofox:9377` at 18, and the app's own healthcheck at 110-119; this is the shape to mirror, NOT to modify)
    docker/docker-compose.beta.yml (the whole file — the header-comment style that names the live file and the isolation invariants, and line 38's hardcoded off-stack CAMOFOX_URL default)
    docker/compose.base.yaml (the `camofox` build block at 14-21 — the minimal in-stack definition)
    docker/compose.local.yaml (the local camofox override at 7-8)
    docker/docker-compose.test.yml (the camofox service at 74-81 and the `depends_on: camofox` condition at 38 — the healthcheck-gated dependency form)
    docker/camofox/README.md and docker/camofox/VENDORED.md (what the vendored service is, which version is pinned, and what it needs at runtime)
    packages/config/src/env-config-server.ts (lines 248-254 — CAMOFOX_URL already defaults to http://camofox:9377, and CAMOFOX_API_KEY is optional)
    docker/beta.env.example (the env-file convention this stack uses for secrets)
  </read_first>
  <action>
    (a) Create `docker/docker-compose.fork.yml`, mirroring the LIVE `/opt/norish/docker-compose.fork.yml`
    and adding camofox. It must be a faithful record of what production runs, not an idealised rewrite:
      - service `norish`, `image: norish:live`, `container_name: norish-app`, `restart: always`,
        `ports: ["3000:3000"]`, the uploads volume, and `env_file` / `environment` following the
        beta file's convention (`${VAR}` / `${VAR:-default}`, secrets sourced from an untracked
        `/opt/norish/.env`, NEVER inlined). Reproduce the app healthcheck from
        `docker-compose.example.yml:110-119` verbatim.
      - services `db` (postgres:17-alpine, `container_name: norish-db`, its data volume) and `redis`
        (`container_name: norish-redis`, its data volume), matching the example file's images.
      - service `camofox`: `build.context: ./camofox` (the VENDORED source — SETUP-04 forbids an external
        image), `container_name: norish-camofox`, `restart: unless-stopped`, `CAMOFOX_PORT: "9377"`,
        `shm_size: "1gb"`, and the healthcheck from `docker-compose.example.yml:150-159` verbatim.
      - on `norish`: `CAMOFOX_URL: ${CAMOFOX_URL:-http://camofox:9377}` and `depends_on` including
        `camofox` — prefer the healthcheck-gated `condition: service_healthy` form used in
        `docker-compose.test.yml:38` so the app does not start against a browser that is not up yet.
      - a `networks` block naming the network the live stack already uses, so adopting this file does not
        rename the network out from under the running containers. **Read the live value out of
        `/opt/norish/docker-compose.fork.yml` if you can do so READ-ONLY; if you cannot, leave the block
        with an explicit `# OPERATOR: confirm against the live file` marker rather than guessing a name
        that would recreate every container.**

    A header comment must carry, in the style of `docker-compose.beta.yml:1-21`:
      - what this file is and which live path it mirrors;
      - the defect it fixes, stated plainly (D-27.1-08): live had NO camofox service; `norish-app` ran on
        `norish_default` while the only running Camoufox, `norishp2-camofox-1`, ran on `norishp2_default`
        and was unreachable from it; the stack worked solely because `CAMOFOX_URL` pointed off-stack at
        LXC 105 — an undocumented single point of failure. The Camoufox service itself was HEALTHY (a
        full open/act/evaluate/close probe against `http://192.168.2.26:9377` returned 200s and rendered
        a real recipe page with `hasRecipeLd: true`). This is an architecture fix, not a repair.
      - that `CAMOFOX_URL` remains an explicit override for reusing an external Camoufox;
      - a pointer to `tooling/fork-stack/README.md` for the adoption procedure;
      - **that applying this file to live is a SEPARATE, DELIBERATE step and is NOT performed by this
        phase.**

    (b) Finding F-3: in `docker/docker-compose.beta.yml`, change line 38 from
    `CAMOFOX_URL: ${CAMOFOX_URL:-http://192.168.2.26:9377}` so the DEFAULT is the in-stack service, and
    keep the LXC-105 address only as a commented, explained override. If the beta stack has no camofox
    service of its own, either add one on the `norish-beta` network (preferred — it preserves that file's
    stated isolation invariant that beta cannot reach live's network) or, if that conflicts with the
    file's documented purpose, leave the variable required and comment WHY. Do not silently keep a
    hardcoded host address as a default.

    Change NOTHING in `docker/docker-compose.example.yml` — it is already correct (lines 18, 120-123,
    139-159). Change nothing under `docker/camofox/**` and nothing in `docker/Dockerfile`.

    **DO NOT RUN ANY COMMAND AGAINST THE LIVE STACK.** No `docker compose up/down/restart`, no
    `docker run`, no `pct exec`, no ssh to the live host beyond a read-only inspection of the live compose
    file. Live is untouched mid-phase (CLAUDE.md).
  </action>
  <acceptance_criteria>
    - `docker compose -f docker/docker-compose.fork.yml config --quiet` EXIT 0 — the file parses and every interpolation resolves. Run it with `--env-file` pointed at a THROWAWAY temp env file, never at `/opt/norish/.env`.
    - `docker compose -f docker/docker-compose.fork.yml config --services` lists exactly: `camofox`, `db`, `norish`, `redis`.
    - `docker compose -f docker/docker-compose.beta.yml config --quiet` EXIT 0.
    - `grep -c 'context: ./camofox' docker/docker-compose.fork.yml` prints `1` — the vendored build, not an external image (SETUP-04).
    - `grep -cE 'image:.*(camoufox|camofox)' docker/docker-compose.fork.yml` prints `0` — no registry pull for the browser.
    - `grep -c 'CAMOFOX_URL: ${CAMOFOX_URL:-http://camofox:9377}' docker/docker-compose.fork.yml` prints `1`.
    - `grep -vE '^\s*#' docker/docker-compose.fork.yml docker/docker-compose.beta.yml | grep -ciE 'chrome|chromium|playwright'` prints `0` (CLAUDE.md hard constraint).
    - `grep -vE '^\s*#' docker/docker-compose.beta.yml | grep -c '192.168.2.26'` prints `0` — the off-stack address survives only in a comment.
    - `git diff --stat docker/docker-compose.example.yml docker/Dockerfile docker/camofox/` is EMPTY.
    - `git diff --stat packages/ apps/ pnpm-lock.yaml` is EMPTY — this plan touches no application code.
    - No secret is committed: `grep -nE 'MASTER_KEY:\s*[A-Za-z0-9+/=]{16,}|AI_API_KEY:\s*\S|WORKOS_API_KEY:\s*\S' docker/docker-compose.fork.yml` prints NOTHING; every secret is a `${VAR}` reference.
    - The SUMMARY records whether the live network name was read from the live file or left as an `# OPERATOR: confirm` marker.
  </acceptance_criteria>
  <verify>
    <automated>docker compose -f docker/docker-compose.fork.yml config --quiet &amp;&amp; docker compose -f docker/docker-compose.beta.yml config --quiet &amp;&amp; docker compose -f docker/docker-compose.fork.yml config --services</automated>
  </verify>
  <done>The live stack's shape, including an in-stack camofox built from the vendored source, is under version control and parses; `CAMOFOX_URL` still overrides; no live container was touched.</done>
</task>

<task type="auto">
  <name>Task 2: The operator runbook — how to adopt in-stack Camoufox, prove it, and roll back</name>
  <files>tooling/fork-stack/README.md</files>
  <read_first>
    tooling/beta/README.md (the runbook voice, structure and level of detail this repo uses for operator procedures — mirror it)
    docker/docker-compose.fork.yml (Task 1's output)
    CLAUDE.md (the Environment section — the `ssh proxmox-tunnel "pct exec 110 -- bash -lc ..."` form, the PATH caveat, and the rule that live cutover is a separate deliberate step)
    packages/api/src/camofox.ts (the REST surface the reachability probe must exercise: the open / evaluate / html / delete endpoints and the `{ ok, ... }` envelope, plus the readiness probe expression at ~line 27)
    docker/camofox/README.md (the service's own documented endpoints and its `/health`)
  </read_first>
  <action>
    Create `tooling/fork-stack/README.md`. It is a runbook for the DIRECTOR, not a task list for an
    executor, and it must state up front that nothing in it runs during phase 27.1.

    Sections:

    1. **What changed and why.** The topology defect in one paragraph (D-27.1-08): no camofox service in
       the live compose, `norish-app` on `norish_default`, `norishp2-camofox-1` on `norishp2_default`,
       reachable only via an off-stack `CAMOFOX_URL` at LXC 105. The service was healthy; the topology
       was the defect.
    2. **Pre-adoption checks.** Read-only: the live compose's current service list and network name; the
       current `CAMOFOX_URL`; that `docker/camofox` builds (`docker compose -f docker/docker-compose.fork.yml build camofox`).
    3. **Adoption.** Copy `docker/docker-compose.fork.yml` into `/opt/norish/`, diff it against the file
       already there and reconcile ANY live-only key by hand before applying — the repo file is a mirror,
       and a blind overwrite of production is not the procedure. Then bring up `camofox` alone, confirm
       healthy, and only then recreate `norish`.
    4. **The reachability proof.** The exact commands, with expected output, to prove camofox is reachable
       from INSIDE the app's network — a `docker exec norish-app` HTTP call to `http://camofox:9377/health`
       expecting 200, followed by a full open → evaluate → html → delete cycle against a real recipe URL
       expecting `hasRecipeLd: true`. State explicitly that a probe from the HOST is not sufficient: the
       original defect was that the host could reach it and the app container could not.
    5. **Rollback.** Re-set `CAMOFOX_URL` to the LXC-105 address and recreate `norish` — the off-stack
       service stays available as the fallback, which is exactly why the override is kept.
    6. **What NOT to do.** No `chrome-headless`, no Chromium/Playwright image, no boot-time bundle patch
       (CLAUDE.md). Do not delete the LXC-105 Camoufox until the in-stack service has served real imports.
    7. **Relationship to plan 27.1-05.** The empirical import gate runs against whatever `CAMOFOX_URL`
       resolves to at the time; record which one, because a gate passed against LXC 105 does not prove
       the in-stack service.
  </action>
  <acceptance_criteria>
    - `test -f tooling/fork-stack/README.md` EXIT 0.
    - The file contains all seven section headings named in the action.
    - `grep -c 'docker exec norish-app' tooling/fork-stack/README.md` is >= 1 — the reachability proof runs from INSIDE the app container, not from the host.
    - `grep -ciE 'chrome-headless|chromium|playwright' tooling/fork-stack/README.md` is >= 1 AND every occurrence is inside the "What NOT to do" section (verify by reading; record the confirmation in the SUMMARY).
    - `grep -c '192.168.2.26' tooling/fork-stack/README.md` is >= 1 — the rollback target is written down rather than lost.
    - The file states in its first 10 lines that nothing in it is executed during phase 27.1.
    - `git diff --stat packages/ apps/ docker/camofox/ pnpm-lock.yaml` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>test -f tooling/fork-stack/README.md &amp;&amp; grep -q 'docker exec norish-app' tooling/fork-stack/README.md &amp;&amp; git diff --quiet -- packages apps pnpm-lock.yaml</automated>
  </verify>
  <done>An operator can adopt in-stack Camoufox, prove reachability from inside the app container, and roll back — all written down, none of it executed by this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app container → camofox container | Internal service call across a docker network; previously crossed an LXC boundary. |
| camofox → the open internet | The browser fetches attacker-controlled pages on the app's behalf. |
| repo → production compose | A tracked file is copied onto the host that runs production. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-04-01 | Denial of Service | off-stack `CAMOFOX_URL` | mitigate | The in-stack service becomes the DEFAULT, removing a cross-LXC single point of failure; `depends_on` with `condition: service_healthy` stops the app starting against a browser that is not up. The off-stack address remains reachable as a documented override, so the fix adds a path rather than removing one. |
| T-27.1-04-02 | Tampering | supply chain of the browser image | mitigate | `build.context: ./camofox` — the VENDORED camofox-browser 1.4.1 source already in the tree (SETUP-04). No registry pull, no floating tag. Enforced by the criterion that no `image:` line names camoufox. |
| T-27.1-04-03 | Information Disclosure | secrets in a tracked compose file | mitigate | Every secret is a `${VAR}` reference resolved from the untracked `/opt/norish/.env`; an explicit grep for inline `MASTER_KEY` / `AI_API_KEY` / `WORKOS_API_KEY` values is an acceptance criterion. `docker compose config` is run against a THROWAWAY env file, never `/opt/norish/.env`, so no secret can reach a command log. |
| T-27.1-04-04 | Elevation of Privilege | camofox network exposure | mitigate | The service publishes NO host port — it is reachable only on the compose network by service name. The reachability proof is therefore run with `docker exec norish-app`, and the runbook states that a host-side probe is not sufficient evidence. |
| T-27.1-04-05 | Tampering | overwriting the live compose | mitigate | The runbook mandates a diff-and-reconcile against the file already on the host before applying, and stages `camofox` up first, `norish` second. The repo file carries an explicit `# OPERATOR: confirm against the live file` marker on the network block rather than guessing a name that would recreate every container. |
| T-27.1-04-06 | Tampering | reintroducing headless Chrome | mitigate | A grep asserting zero non-comment `chrome`/`chromium`/`playwright` matches in both compose files is an acceptance criterion (CLAUDE.md hard constraint). |
| T-27.1-04-07 | Elevation of Privilege | per-cookbook isolation | accept | This plan touches no application code, no permission check and no query. `git diff --stat packages/ apps/` EMPTY is an acceptance criterion on both tasks. |
| T-27.1-04-SC | Tampering | npm/pip/cargo installs | mitigate | NO package is installed. `git diff --stat pnpm-lock.yaml` EMPTY is an acceptance criterion on both tasks. The camofox image build uses the vendored `package-lock.json` already in the tree, unchanged. |
</threat_model>

<verification>
- `docker compose -f docker/docker-compose.fork.yml config --quiet` and `--services` (against a throwaway env file).
- `docker compose -f docker/docker-compose.beta.yml config --quiet`.
- The grep gates on `context: ./camofox`, the `CAMOFOX_URL` default, absent chrome/chromium/playwright, absent inline secrets.
- `git diff --stat packages/ apps/ docker/camofox/ docker/Dockerfile docker/docker-compose.example.yml pnpm-lock.yaml` EMPTY.
- No live container touched: the SUMMARY states explicitly that no `docker compose up/down/restart`, `docker run` or `pct exec` was executed.
</verification>

<success_criteria>
The live stack's shape is under version control, with Camoufox as an in-stack service built from the
vendored source and `CAMOFOX_URL` still an explicit override. The beta stack no longer defaults to the
off-stack address. An operator runbook explains adoption, an inside-the-container reachability proof,
and rollback. Nothing live was touched, and no headless-Chrome dependency was reintroduced.
</success_criteria>

<output>
Create `.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-04-SUMMARY.md` when done.
Record: the exact service list `docker compose config --services` printed, whether the live network name
was read from the live file or left as an operator marker, and an explicit statement that no live
container, network or volume was created, modified or restarted.
</output>
