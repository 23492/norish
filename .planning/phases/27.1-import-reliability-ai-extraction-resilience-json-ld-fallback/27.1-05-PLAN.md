---
phase: 27.1-import-reliability-ai-extraction-resilience-json-ld-fallback
plan: 05
type: execute
wave: 3
depends_on: ["27.1-01", "27.1-02", "27.1-03", "27.1-04"]
requirements: [IMPORT-REL-01, IMPORT-REL-02, IMPORT-REL-03, IMPORT-REL-04, IMPORT-REL-05]
autonomous: false
cross_ai: false
files_modified:
  - tooling/import-gate/run-import-gate.mjs
  - tooling/import-gate/urls.ah.txt
  - tooling/import-gate/urls.lekkerensimpel.txt
  - tooling/import-gate/README.md
  - .planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md

must_haves:
  truths:
    - "THIS PLAN RUNS AFTER THE IMAGE IS BUILT AND DEPLOYED (D-27.1-09). Task 1 (the harness) may be built at any time; Task 2 is a BLOCKING checkpoint that does not clear until the director confirms a new image is live; Tasks 3 and 4 exercise the LIVE stack. Running Task 3 against the pre-27.1 image proves nothing and is a gate failure, not a shortcut."
    - "THE BUILD IS THE DIRECTOR'S. No task here runs `pnpm docker:build`, `docker compose up`, a retag or a container swap. The executor drives HTTP and reads logs; the director builds, deploys and clears the checkpoint (CLAUDE.md)."
    - "BAR 1: `https://www.ah.nl/allerhande/recept/R-R951540/bonensalade-met-kip-en-avocado` imports successfully."
    - "BAR 2: at least 10 FURTHER ah.nl/allerhande recipes, spanning at least 6 DISTINCT categories drawn from {bakken/dessert, soep, vegetarisch, pasta, vlees, vis, salade, ontbijt}, import successfully."
    - "BAR 3, GATED ON BAR 2: only once 10 ah.nl recipes work, at least 5 recipes from `https://www.lekkerensimpel.com/` import successfully. Running bar 3 before bar 2 passes is a gate failure."
    - "EVERY URL IS EVIDENCED. Per URL the report records: fetch OK, JSON-LD present, WHICH PATH was taken (`ai` | `jsonld-fallback` | `structured`), metric ingredient count, US ingredient count, whether `cook_source` derived (plus `cook_confidence` and `cook_review_needed`), and — for anything that did not succeed — the failure mode VERBATIM, uncondensed and unparaphrased."
    - "A FAILURE IS VISIBLE. A deliberately unimportable URL produces a rendered, dismissible error card where the skeleton was, the card survives until dismissed, and a page reload leaves no skeleton behind. Confirmed by a human in a browser, not inferred from a log line."
    - "THE GATE IS NON-SKIPPABLE. It is its own plan, both checkpoints are `gate=\"blocking\"`, and `workflow.auto_advance` does not apply to them. A partial pass is a FAIL and is recorded as one — the report states PASS or FAIL explicitly, with the counts that decided it."
    - "A `409 CONFLICT` IS NOT A PASS. `importFromUrl` returns CONFLICT when the recipe already exists or is already queued; the harness records `conflict` as its own outcome and it counts toward NEITHER a pass nor a failure. The operator must supply URLs not already in the target cookbook."
    - "WHICH CAMOUFOX SERVED THE GATE IS RECORDED. A gate passed against the off-stack LXC-105 Camoufox does not prove the in-stack service (27.1-04); the report states the `CAMOFOX_URL` in force."
    - "NATIVE EXECUTION, DIRECT `.planning/` EDITS. `cross_ai: false` (D-27.1-10); the SUMMARY and this phase's ROADMAP/REQUIREMENTS lines are edited by hand — `gsd-tools query state.update-progress` and `state.record-session` corrupt this fork's free-form STATE.md (D-27.1-11)."
    - "SCOPE FENCE: this plan changes NO application code. `git diff --stat packages/ apps/` is EMPTY on every task. If the gate FAILS, the fix is a new plan, not an edit inside this one."
  artifacts:
    - path: tooling/import-gate/run-import-gate.mjs
      provides: "The dependency-free gate harness: queue each URL over the authenticated REST API, poll for the row, join the container log, emit JSON + markdown, exit non-zero unless every bar passes"
      contains: "--self-test"
    - path: tooling/import-gate/urls.ah.txt
      provides: "The mandated bar-1 URL plus the bar-2 set, each line carrying its category as a trailing comment"
    - path: .planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md
      provides: "The evidence report: the per-URL table, the three bar verdicts, the live image sha, the CAMOFOX_URL in force, and the overall PASS/FAIL"
  key_links:
    - from: "tooling/import-gate/run-import-gate.mjs"
      to: "POST /api/v1/recipes/import/url"
      via: "x-api-key authenticated queue call (packages/trpc/src/routers/recipes/recipes.ts:423-463)"
      pattern: "recipes/import/url"
    - from: "tooling/import-gate/run-import-gate.mjs"
      to: "GET /api/v1/recipes/{id}"
      via: "polls FullRecipeSchema for cookSource, cookConfidence and the per-system ingredient counts"
      pattern: "recipes/"
    - from: "tooling/import-gate/run-import-gate.mjs"
      to: "the parserPath log markers"
      via: "joins a docker-logs capture on url to recover the path taken and jsonLdNodeCount (27.1-02)"
      pattern: "parserPath"
---

<objective>
Prove, against the LIVE stack after deployment, that recipe import actually works — on the URL that
failed, on ten more across categories, and on a second site — and that when it does not work the user
sees a real error instead of nothing.

Purpose: every other plan in this phase is unit-proven. Unit tests did not catch this defect and would
not have; the director asked for an explicit empirical gate so the phase cannot be declared done on
green suites alone.
Output: a repo-tracked harness and a signed evidence report with an explicit PASS or FAIL.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-CONTEXT.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-02-SUMMARY.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-03-SUMMARY.md
@packages/trpc/src/routers/recipes/recipes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The gate harness — dependency-free, self-testing, and honest about conflicts</name>
  <files>tooling/import-gate/run-import-gate.mjs, tooling/import-gate/urls.ah.txt, tooling/import-gate/urls.lekkerensimpel.txt, tooling/import-gate/README.md</files>
  <read_first>
    packages/trpc/src/routers/recipes/recipes.ts (importFromUrlProcedure at 423-463 — POST /recipes/import/url, input `{ url, forceAI? }`, output the recipe UUID, and the CONFLICT it throws at 455-460; getProcedure at 142-169 — GET /recipes/{id}, output FullRecipeSchema, 404 when absent)
    packages/shared/src/contracts/zod/recipe.ts (FullRecipeSchema at 83-103 — the fields the poll reads: cookSource, cookConfidence, cookReviewNeeded, recipeIngredients[].systemUsed, steps[].systemUsed, systemUsed)
    packages/auth/src/auth.ts (line 623 — `apiKeyHeaders: ["x-api-key", "bearer"]`)
    packages/trpc/src/context.ts (lines 113-118 — how the x-api-key header reaches the context)
    apps/web/app/api/v1/[...openapi]/route.ts (the mount point, so the base path is right)
    tooling/beta/README.md and tooling/beta/clone-beta-db.sh (the house style for an operator script + README in tooling/)
    .planning/phases/27.1-.../27.1-02-SUMMARY.md (the EXACT parserPath marker strings 27.1-02 emitted — the log join must match them, not a guess)
    packages/api/src/camofox.ts (redactUrl — the url form that actually appears in the log lines, which the join must account for)
  </read_first>
  <action>
    Create `tooling/import-gate/run-import-gate.mjs` — Node ESM, **zero new dependencies** (global
    `fetch`, `node:fs`, `node:process` only; `git diff pnpm-lock.yaml` must stay EMPTY).

    Configuration, all from env so no secret is ever an argv: `GATE_BASE_URL`, `GATE_API_KEY`,
    `GATE_URLS_FILE`, `GATE_OUT_JSON`, `GATE_OUT_MD`, optional `GATE_LOG_FILE` (a
    `docker logs norish-app` capture), optional `GATE_POLL_TIMEOUT_MS` (default 180000),
    `GATE_POLL_INTERVAL_MS` (default 3000), `GATE_MIN_CATEGORIES` (default 6), `GATE_MIN_URLS`.

    URL-file format: one URL per line; blank lines and `#`-only lines ignored; an optional trailing
    ` # <category>` on each line. Parse the category out — the bar-2 check needs distinct categories and
    nothing else in the system knows a URL's category.

    Per URL, in order:
      1. `POST ${GATE_BASE_URL}/api/v1/recipes/import/url` with `x-api-key` and `{ url }`.
         - 2xx -> the returned UUID is the recipeId.
         - 409 -> outcome `conflict`. Record it and move on. **A conflict counts toward NEITHER a pass
           nor a failure** — it means the recipe already exists or is already queued in the target
           cookbook, so the URL proves nothing. Make this loud in the output; an operator must not be
           able to read a wall of conflicts as a pass.
         - anything else -> outcome `queue-error`, with the status code and the response body VERBATIM.
      2. Poll `GET ${GATE_BASE_URL}/api/v1/recipes/{recipeId}` every `GATE_POLL_INTERVAL_MS` until 200 or
         `GATE_POLL_TIMEOUT_MS`. A 404 is expected while the job runs. On timeout the outcome is
         `timeout` — the import never produced a row, which is exactly the "eternal skeleton" symptom.
      3. On 200, derive from the `FullRecipeSchema` body: `name`, `systemUsed`,
         `cookSourceDerived: body.cookSource !== null`, `cookConfidence`, `cookReviewNeeded`,
         `metricIngredients` / `usIngredients` (count `recipeIngredients` by `systemUsed`),
         `metricSteps` / `usSteps`. Outcome `ok`.
      4. If `GATE_LOG_FILE` is set, join on the URL to recover `parserPath`, `jsonLdNodeCount`, `usedAI`
         and `cookMinted` from 27.1-02's markers — **use the exact marker strings recorded in
         `27.1-02-SUMMARY.md`**, and account for `redactUrl` if the logged url is redacted. When no log
         file is given, those columns read `unknown` rather than being silently omitted; the report must
         never imply evidence it does not have.

    Output: a JSON array to `GATE_OUT_JSON`, and a markdown table to `GATE_OUT_MD` with one row per URL
    and exactly these columns — `url | category | outcome | fetch OK | JSON-LD | path | metric ing |
    us ing | cook_source | confidence | review | failure (verbatim)`.

    Bar evaluation, printed as a summary block and reflected in the exit code:
      - `ok` count, `conflict` count, and every non-`ok` non-`conflict` outcome listed individually;
      - distinct categories among the `ok` rows;
      - EXIT 0 only when `ok >= GATE_MIN_URLS` AND distinct categories `>= GATE_MIN_CATEGORIES` AND no
        row is `queue-error` or `timeout`. Otherwise EXIT 1 with the reason on stderr.

    Add a `--self-test` mode: run the pure helpers (URL-file parsing incl. the category comment, the
    log join, the per-system counting, the bar evaluation) against inline fixtures, assert with
    `node:assert/strict`, print a pass count and exit non-zero on any failure. No test framework, no new
    dependency, still an automated gate.

    Never log or write `GATE_API_KEY`. Redact it if it would ever appear in an error body.

    Create `tooling/import-gate/urls.ah.txt` with the MANDATED bar-1 URL as its first line:
    `https://www.ah.nl/allerhande/recept/R-R951540/bonensalade-met-kip-en-avocado # salade`.
    Populate the remaining bar-2 lines with REAL ah.nl/allerhande recipe URLs spanning at least 6 of the
    named categories — fetch them from ah.nl's own category listings at authoring time and record in the
    SUMMARY how they were chosen. Do not invent URL shapes; a 404 at gate time is an authoring failure.
    Create `tooling/import-gate/urls.lekkerensimpel.txt` the same way with at least 5 real URLs.

    Create `tooling/import-gate/README.md`: what the gate proves, the exact invocation (including how to
    mint an API key for a throwaway account in a throwaway cookbook), the three bars and their order,
    how to capture `GATE_LOG_FILE`, and how to read a `conflict`.
  </action>
  <acceptance_criteria>
    - `node tooling/import-gate/run-import-gate.mjs --self-test` EXIT 0 and prints a non-zero assertion count.
    - `node --check tooling/import-gate/run-import-gate.mjs` EXIT 0.
    - `git diff --stat pnpm-lock.yaml` is EMPTY and `grep -c "require(\|from \"\(?!node:\)" tooling/import-gate/run-import-gate.mjs` confirms by inspection that the only imports are `node:` builtins — record the confirmation in the SUMMARY.
    - `grep -vE '^\s*#' tooling/import-gate/urls.ah.txt | grep -c .` prints **11 or more**, and its FIRST line is exactly `https://www.ah.nl/allerhande/recept/R-R951540/bonensalade-met-kip-en-avocado # salade`.
    - `grep -vE '^\s*#' tooling/import-gate/urls.ah.txt | sed 's/.*# //' | sort -u | grep -c .` prints **6 or more** — at least six distinct categories are actually present in the file.
    - `grep -vE '^\s*#' tooling/import-gate/urls.lekkerensimpel.txt | grep -c .` prints **5 or more**.
    - Every URL in both files returns a non-404 HTTP status when HEAD-requested from the dev host (`while read -r u _; do [ -z "$u" ] && continue; printf '%s ' "$u"; curl -o /dev/null -s -w '%{http_code}\n' -I "$u"; done`). Record the status table in the SUMMARY. A 403 from bot protection is acceptable and expected on ah.nl; a 404 is an authoring failure and must be replaced.
    - `grep -c 'GATE_API_KEY' tooling/import-gate/README.md` is >= 1 and the key is never a command-line argument in any documented invocation.
    - `git diff --stat packages/ apps/` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>node --check tooling/import-gate/run-import-gate.mjs &amp;&amp; node tooling/import-gate/run-import-gate.mjs --self-test &amp;&amp; git diff --quiet -- packages apps pnpm-lock.yaml</automated>
  </verify>
  <done>A dependency-free, self-testing harness plus two vetted URL lists exist in the repo; no application code changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: DEPLOY GATE — the director confirms a new image is live</name>
  <what-built>
    Plans 27.1-01 through 27.1-04 are merged and green: the relaxed + mirroring normalizer, the
    single retry with raised output headroom, the JSON-LD fallback with its `parserPath` markers, the
    unconditional failed-emit with its rendered error card, and the repo-tracked fork compose.
    Task 1 of this plan has produced the gate harness and its URL lists.
    NOTHING has yet been proven against the live stack.
  </what-built>
  <how-to-verify>
    THE DIRECTOR performs this; the executor does not build and does not deploy (CLAUDE.md).

    1. Build: `pnpm docker:build` on LXC 110, run DETACHED (`nohup … &amp;` plus an exit-code sentinel) and
       polled — never a Monitor or `sleep` wait-loop in a subagent.
    2. Take a `pg_dump` backup and record its path and TOC object count, per the standing migration
       discipline. (27.1 adds no migration, so this is a precaution, not a prerequisite.)
    3. Deploy the new image and record: the NEW image sha256, the PREVIOUS image sha256, and the
       rollback tag.
    4. Confirm health: 0 restarts, the migration count UNCHANGED at 43 (this phase adds no migration —
       a change here means something else shipped), both public endpoints green, no new `level>=50`
       log lines.
    5. Record the `CAMOFOX_URL` the deployed container actually resolves
       (`docker exec norish-app printenv CAMOFOX_URL`, or its absence meaning the
       `http://camofox:9377` default) — a gate passed against the off-stack LXC-105 service does not
       prove 27.1-04's in-stack service.
    6. Mint an API key for a THROWAWAY account whose active cookbook is a THROWAWAY cookbook, so the
       gate's imports do not pollute a real one. Export it as `GATE_API_KEY` in the executor's
       environment, never on a command line.
    7. Start a log capture for the gate window: `docker logs -f norish-app > /tmp/27.1-gate.log &amp;`.

    Reply with: new sha, previous sha, rollback tag, migration count, resolved `CAMOFOX_URL`, the
    throwaway account + cookbook used, and the log-capture path.
  </how-to-verify>
  <acceptance_criteria>
    - The director's reply carries ALL SEVEN recorded values. A missing value is a blocked checkpoint, not a warning.
    - The reported migration count is **43** — unchanged. Any other value means something outside 27.1 shipped and the gate must not proceed.
    - The new image sha256 differs from `sha256:f1b6664ea600d693a9aa3764f922de48d56804793d3f66e72c4bb992845769b0` (the pre-27.1 live image). If it matches, 27.1 is NOT deployed and Task 3 would prove nothing.
    - The resolved `CAMOFOX_URL` is recorded verbatim.
  </acceptance_criteria>
  <resume-signal>Reply with the seven values, then "deployed" — or describe what blocked the deploy.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the gate against live and write the evidence report</name>
  <files>.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md</files>
  <read_first>
    tooling/import-gate/README.md (Task 1's output — the exact invocation)
    tooling/import-gate/run-import-gate.mjs (the outcome vocabulary, so the report uses the same words)
    .planning/phases/27.1-.../27.1-02-SUMMARY.md (the parserPath marker strings, for the log join)
    Task 2's checkpoint reply (the image sha, the resolved CAMOFOX_URL, the throwaway account, the log path)
  </read_first>
  <action>
    Run the three bars IN ORDER. Do not start a later bar before the earlier one has passed.

    **Bar 1** — run the harness over a URL file containing ONLY the mandated
    `R-R951540/bonensalade-met-kip-en-avocado`, with `GATE_MIN_URLS=1` and `GATE_MIN_CATEGORIES=1`.
    If it fails, STOP. Do not proceed to bar 2, and do not edit application code to make it pass —
    a failure here is a finding for a new plan, and this plan's scope fence forbids the fix.

    **Bar 2** — run the harness over `tooling/import-gate/urls.ah.txt` with `GATE_MIN_URLS=10`
    (the ten FURTHER recipes, i.e. excluding bar 1's) and `GATE_MIN_CATEGORIES=6`.

    **Bar 3, only if bar 2 exited 0** — run over `tooling/import-gate/urls.lekkerensimpel.txt` with
    `GATE_MIN_URLS=5` and `GATE_MIN_CATEGORIES=1`.

    Then write
    `.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md`
    containing:
      - a run header: date, live image sha256, previous sha256, migration count, resolved
        `CAMOFOX_URL`, the throwaway account/cookbook, and the harness invocation used for each bar;
      - one markdown table per bar, with the harness's exact columns — `url | category | outcome |
        fetch OK | JSON-LD | path | metric ing | us ing | cook_source | confidence | review |
        failure (verbatim)`;
      - a **path distribution** line: how many of the successful imports went `ai`, how many
        `jsonld-fallback`, how many `structured`. A gate in which most ah.nl imports took
        `jsonld-fallback` is a PASS by the letter of the bars but a SIGNAL that AI extraction is still
        failing routinely — call that out in the report rather than burying it in a table;
      - a **`cook_source` line**: how many successful imports derived one, the confidence distribution,
        and how many are flagged `cook_review_needed`;
      - every failure mode VERBATIM — the exact status code, the exact response body, and the exact log
        lines for that url. Do not paraphrase, do not condense, do not "clean up" a message;
      - an explicit **PASS** or **FAIL** per bar and overall, with the counts that decided each.

    If any bar FAILS, the report says FAIL, records everything observed, and the task ends. Do not
    retry-until-green, do not quietly swap a failing URL for an easier one, and do not touch application
    code. A gate that can be edited until it passes is not a gate.
  </action>
  <acceptance_criteria>
    - The harness EXIT code is recorded for each of the three bars in the report.
    - Bar 1: the mandated URL's row has `outcome = ok`.
    - Bar 2: `ok >= 10` among the FURTHER ah.nl URLs, and distinct categories among those `ok` rows `>= 6`. Both counts are printed in the report.
    - Bar 3: `ok >= 5` for lekkerensimpel, and the report states explicitly that bar 3 was run only after bar 2 exited 0.
    - Every row in every table has a non-empty value in EVERY column — `unknown` is permitted only for `path` / `JSON-LD` when no `GATE_LOG_FILE` was captured, and the report must then say so.
    - No row anywhere has `outcome = timeout`. A timeout is the eternal-skeleton symptom this phase exists to remove, and it is a FAIL regardless of the other counts.
    - `git diff --stat packages/ apps/ tooling/` is EMPTY for this task — the harness was authored in Task 1 and is NOT edited here.
    - `grep -c 'PASS\|FAIL' 27.1-IMPORT-GATE.md` is >= 4 (three bars plus an overall verdict).
    - `grep -ci "GATE_API_KEY=\|x-api-key: [A-Za-z0-9]" 27.1-IMPORT-GATE.md` prints `0` — no credential is written into the report.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md &amp;&amp; ! grep -q 'timeout' .planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-IMPORT-GATE.md &amp;&amp; git diff --quiet -- packages apps tooling</automated>
  </verify>
  <done>All three bars have been run in order against the deployed image and a per-URL evidence report exists with an explicit verdict.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: A failure is VISIBLE — browser confirmation, and a cross-cookbook spot-check</name>
  <what-built>
    27.1-03 made the worker's `failed` emit unconditional and fail-closed, and replaced the failed
    import's skeleton with a rendered, dismissible error card in all 12 locales. Task 3 has just proven
    the SUCCESS path against live. This checkpoint proves the FAILURE path, in a browser, because
    "the user saw nothing happen" is the defect this phase exists to fix and no log line can confirm
    it was fixed.
  </what-built>
  <how-to-verify>
    In a browser against the live URL, signed in as the throwaway account from Task 2:

    1. Import a URL that CANNOT yield a recipe — a plain non-recipe page (e.g. a news article or a site
       root). Watch the dashboard.
    2. Confirm a skeleton card appears, then is REPLACED by an error card — not merely removed, and not
       merely a toast. Note how long it took.
    3. Confirm the error card shows a reason and the source host, and that it is still there after the
       toast has auto-dismissed.
    4. RELOAD the page. Confirm no skeleton returns and the dashboard is not left with a phantom card.
    5. Dismiss the error card. Confirm it disappears and does not come back on the next reload.
    6. Re-import a URL that DOES work and confirm the recipe lands normally afterwards — the error state
       did not wedge the dashboard.
    7. **Isolation spot-check.** In a second browser profile, signed in as a DIFFERENT user whose active
       cookbook is a DIFFERENT cookbook, repeat step 1 in the first profile and watch the second.
       Confirm the second user does NOT receive the error card and does NOT receive the failure toast.
       **Expected, and NOT a 27.1 regression:** the second user MAY still see a pending SKELETON, because
       `recipes.getPending` returns every household's queued jobs under the live `view: "everyone"`
       policy. That is pre-existing finding **F-1 / PENDING-ISO-01**, documented in `27.1-CONTEXT.md` and
       deliberately out of this phase's scope. Record whether you observed it — that observation is the
       live evidence for scheduling PENDING-ISO-01. Do NOT fail this checkpoint for it; DO fail it if the
       second user receives the error CARD or the failure TOAST, which would mean the new surface leaks.
    8. Switch the UI language to Dutch (the live `DEFAULT_LOCALE`) and repeat step 1. Confirm the error
       card renders translated Dutch, with no raw key like `common.import.failure.title` and no English
       fallback.

    Attach or describe what you saw at steps 2, 3, 4, 7 and 8.
  </how-to-verify>
  <acceptance_criteria>
    - The reply confirms an error CARD (not only a toast) replaced the skeleton, and states the observed delay.
    - The reply confirms the card survives the toast's auto-dismiss and a page reload leaves NO skeleton.
    - The reply confirms dismissing the card is permanent for that session and across a reload.
    - The reply confirms a subsequent successful import still lands.
    - The reply states explicitly whether the second user received the error card or the failure toast. Either one is a FAIL. It also records whether the second user saw a pending skeleton (expected; PENDING-ISO-01 evidence, not a failure).
    - The reply confirms the Dutch card renders real translated text — no raw i18n key, no English fallback.
    - Any FAIL is appended to `27.1-IMPORT-GATE.md` verbatim before the phase is closed.
  </acceptance_criteria>
  <resume-signal>Reply "approved" with the observations for steps 2, 3, 4, 7 and 8 — or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate harness → live API | An automated client holds a real API key against production. |
| gate harness → live data | Imports write real rows into a real cookbook. |
| two live users → the new error surface | The cross-cookbook boundary is exercised for real, not in a mock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-05-01 | Information Disclosure | `GATE_API_KEY` | mitigate | The key is passed only by environment variable, never as argv (where it would land in shell history and process listings), never logged, and redacted from any captured error body. An acceptance criterion greps the committed report for credential-shaped strings and requires `0`. |
| T-27.1-05-02 | Tampering | live data | mitigate | The gate runs as a THROWAWAY account whose active cookbook is a THROWAWAY cookbook, so ~16 imported recipes never enter a real one. The director records both in Task 2 and can drop the cookbook afterwards. |
| T-27.1-05-03 | Elevation of Privilege | per-cookbook isolation, live | mitigate | Task 4 step 7 is a real two-user, two-cookbook adversarial observation of the NEW error surface — the live counterpart to `failed-emit-isolation.test.ts`. The pre-existing `getPending` leak (F-1) is named up front so it cannot be mistaken for either a pass or a 27.1 regression, and its observation is captured as evidence for PENDING-ISO-01. |
| T-27.1-05-04 | Repudiation | a gate edited until it passes | mitigate | The scope fence (`git diff --stat packages/ apps/` EMPTY on every task) plus the explicit "do not retry-until-green, do not swap a failing URL, do not touch application code" instruction. A failure ends the task with a FAIL report; the fix is a new plan. |
| T-27.1-05-05 | Denial of Service | live stack under gate load | mitigate | URLs run sequentially with a bounded poll, against a worker whose concurrency is 2 (`WORKER_CONCURRENCY`), so the gate adds at most the load of one user importing steadily. The harness does not parallelise. |
| T-27.1-05-06 | Tampering | deploying the wrong image | mitigate | Task 2 requires the new sha to DIFFER from the recorded pre-27.1 sha and the migration count to be unchanged at 43. Running Task 3 against the old image would produce a false PASS, so the check is a blocking acceptance criterion rather than a note. |
| T-27.1-05-SC | Tampering | npm/pip/cargo installs | mitigate | The harness is `node:`-builtins only; `git diff --stat pnpm-lock.yaml` EMPTY is an acceptance criterion. No package manager runs during the gate. |
</threat_model>

<verification>
- `node tooling/import-gate/run-import-gate.mjs --self-test` EXIT 0.
- The three bar runs, in order, with their exit codes recorded.
- `27.1-IMPORT-GATE.md` exists, has no `timeout` row, and carries an explicit PASS/FAIL per bar plus overall.
- Both blocking checkpoints answered with every required value.
- `git diff --stat packages/ apps/ pnpm-lock.yaml` EMPTY across the whole plan.
</verification>

<success_criteria>
Against the deployed image: the URL that failed imports; ten further ah.nl recipes across at least six
categories import; five lekkerensimpel recipes import. Every URL is evidenced with fetch, JSON-LD,
path, per-system ingredient counts, `cook_source` and — where relevant — the verbatim failure. A
deliberately failing import shows a rendered, dismissible, translated error card and never an eternal
skeleton, and that error never reaches a user in another cookbook.
</success_criteria>

<output>
Create `.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-05-SUMMARY.md` when done.
Record: the three bar verdicts and their counts, the path distribution, the `cook_source` distribution,
the live image sha and the resolved `CAMOFOX_URL`, both checkpoint replies, the PENDING-ISO-01 live
observation from Task 4 step 7, and — if the gate FAILED — exactly what failed, with no attempt made to
fix it inside this plan.
</output>
