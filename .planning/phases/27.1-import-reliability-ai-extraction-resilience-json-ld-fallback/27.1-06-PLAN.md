---
phase: 27.1-import-reliability-ai-extraction-resilience-json-ld-fallback
plan: 06
type: execute
wave: 1
depends_on: []
requirements: [PENDING-ISO-01, HOUSE-06]
autonomous: true
cross_ai: false
files_modified:
  - packages/trpc/src/routers/recipes/pending.ts
  - packages/trpc/__tests__/recipes/pending-isolation.test.ts

must_haves:
  truths:
    - "`recipes.getPending` NO LONGER HANDS EVERY USER EVERY HOUSEHOLD'S QUEUED IMPORTS. Under the LIVE server-wide `view: \"everyone\"`, user B in cookbook B receives ZERO of cookbook A's pending jobs — neither the `recipeId` nor the source `url`, both of which the DTO carries (`PendingRecipeDTO`, `packages/shared/src/contracts/dto/queue.ts:1-5`)."
    - "`everyone` FALLS THROUGH TO THE COOKBOOK CLAMP; it is not special-cased at all. The filter branches on ONE question — is the policy `owner`? — and every other level, `everyone` included, uses the same `job.data.householdKey === ctx.householdKey` clamp. There is no reachable branch in which `everyone` means `return true` (AGENTS.md: `a reachable everyone branch is the foot-gun`)."
    - "THE REMEDIATION MIRRORS THE THREE SIBLINGS, it does not invent a fourth shape. `emitByPolicy` (REALTIME-ISO-01, D-22-01) routes `everyone` and `household` down ONE clamped path and splits out only the narrower case; `buildViewPolicyCondition` (LIST-ISO-01) folds a seeded `everyone` into the active-cookbook branch with the comment `\"household\" (and a seeded \"everyone\", clamped)`; `addImportJob` (IMPORT-DEDUP-ISO-01) removed the policy read from the scope decision entirely. This fix is the same move."
    - "A PERSONAL SCOPE DEGRADES TO A PER-USER CLAMP FOR FREE. `ctx.householdKey` is `household?.id ?? user.id` (`packages/trpc/src/middleware.ts`), and a personal import's `job.data.householdKey` is that same user id, so a viewer with no active cookbook is clamped to their own jobs by the household comparison alone — exactly how `emitByPolicy`'s `isPersonalScope` branch behaves. No extra branch is added for it."
    - "NO SERVER-ADMIN EXEMPTION IS INTRODUCED. Today's `household` and `owner` branches have no admin escape hatch, so adding one would be a NEW widening invented by this plan rather than a defect fixed by it — and this query hydrates a dashboard skeleton, it is not an admin tool. Pinned by a test asserting an `isServerAdmin: true` caller is clamped identically. (`buildViewPolicyCondition` and `config.tags` DO exempt admins; that difference is deliberate and stated in the code comment.)"
    - "THE LEGITIMATE CASE STILL WORKS. A user's OWN running import still appears in their own pending list under all three policy levels — the clamp must not cost users the skeleton the whole phase depends on (27.1-03 renders the error card in that same slot)."
    - "EVERY POLICY-SEEDING TEST HAS AN `everyone` SIBLING. The fork's rule exists because REALTIME-ISO-01, IMPORT-DEDUP-ISO-01 and LIST-ISO-01 each survived a green suite that seeded only `view: \"household\"` — the branch production actually runs was never executed. Every case here runs under `household`, `everyone` AND `owner`."
    - "THE SUITE WAS RED FIRST. The isolation test is written against the UNFIXED resolver, run, and observed to FAIL with the leak visible in the output, before a line of `pending.ts` changes. A test that has never failed proves nothing about the defect it claims to pin."
    - "THIS PLAN CHANGES THE BOUNDARY, so unlike 27.1-03 its `pending.ts` diff is NOT empty. 27.1-03's scope fence has been reworded to name 27.1-06 as the owner of that file; the two plans do not contradict each other."
    - "SCOPE FENCE: `getPending`'s filter is the ONLY behaviour changed. No schema, no migration, no dependency (`git diff pnpm-lock.yaml` EMPTY), no change to the `PendingRecipeDTO` shape, no change to `getPendingAutoTagging` / `getPendingAllergyDetection` / the four `is*` probes (Task 2 pins them, it does not alter them), no client change, no parser change, no worker change."
  artifacts:
    - path: packages/trpc/src/routers/recipes/pending.ts
      provides: "getPending's cookbook clamp — `everyone` folded into the household branch, `owner` split out"
      contains: "PENDING-ISO-01"
    - path: packages/trpc/__tests__/recipes/pending-isolation.test.ts
      provides: "The two-household adversarial suite driving the REAL router under household / everyone / owner, plus the regression pins for the sibling queries"
  key_links:
    - from: "packages/trpc/src/routers/recipes/pending.ts"
      to: "ctx.householdKey"
      via: "the cookbook clamp every non-owner policy level shares"
      pattern: "householdKey === ctx\\.householdKey"
    - from: "packages/trpc/__tests__/recipes/pending-isolation.test.ts"
      to: "@norish/trpc/routers/recipes/pending (pendingProcedures)"
      via: "createCaller against the REAL router — every leak in this family survived a suite that tested the layer below the defect"
      pattern: "createCaller"
---

<objective>
Close PENDING-ISO-01: stop `recipes.getPending` serving every authenticated user every household's
queued imports under the live `view: "everyone"` policy.

Purpose: this is a live cross-cookbook leak on the import surface — the exact surface this phase is
about — and CLAUDE.md makes per-cookbook isolation a hard constraint. The pending DTO carries the
`recipeId` AND the source `url`, so today a user in cookbook B can enumerate what cookbook A is
importing, in real time.
Output: a three-line clamp mirroring the fork's three existing remediations, plus the adversarial
two-household suite that should have existed when the family was first found.
</objective>

<execution_context>
@/opt/norish-src/.claude/gsd-core/workflows/execute-plan.md
@/opt/norish-src/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-CONTEXT.md
@packages/trpc/src/routers/recipes/pending.ts
@packages/trpc/__tests__/config/tags-isolation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED first — the two-household suite — then fold `everyone` into the cookbook clamp</name>
  <files>packages/trpc/__tests__/recipes/pending-isolation.test.ts, packages/trpc/src/routers/recipes/pending.ts</files>
  <read_first>
    packages/trpc/src/routers/recipes/pending.ts (getPending at 18-53 — the `switch (policy.view)` at 29-39 whose `case "everyone": return true` at 30-32 IS the defect; note the unreachable `return false` tail at 41)
    packages/trpc/__tests__/config/tags-isolation.test.ts (THE TEMPLATE — same defect family, same layer, same technique: `vi.mock("@norish/trpc/middleware")` down to a pass-through `publicProcedure` so the REAL resolver runs without a DB, the data source mocked, `createCaller(ctx).procedure()`, and a `LIVE_SERVER_POLICY` constant. Mirror it; do not invent a new harness.)
    packages/shared-server/src/realtime/policy.ts (emitByPolicy at 139-170 — REALTIME-ISO-01's remediation: ONE clamped path shared by `everyone` and `household`, the narrower case split out first, `isPersonalScope = ctx.householdKey === ctx.userId`)
    packages/db/src/repositories/recipes.ts (buildViewPolicyCondition at 446-492 — LIST-ISO-01's remediation, especially the comment at 465-467, `"household" (and a seeded "everyone", clamped)`, and the personal-view `everyone` branch at 474-479 which narrows to the viewer)
    packages/queue/src/recipe-import/producer.ts (addImportJob lines 27-42 — IMPORT-DEDUP-ISO-01's remediation: the literal `"household"` replacing a `getRecipePermissionPolicy()` read, and the comment explaining why)
    packages/trpc/src/middleware.ts (how `ctx.householdKey`, `ctx.householdUserIds`, `ctx.user` and `ctx.isServerAdmin` are built — in particular that `householdKey` is `household?.id ?? user.id`)
    packages/queue/src/contracts/job-types.ts (RecipeImportJobData — the `userId` / `householdKey` / `householdId` the filter reads)
    packages/shared/src/contracts/dto/queue.ts (PendingRecipeDTO at 1-5 — it carries `url`, which is why this leak discloses more than an opaque id)
    packages/trpc/__tests__/recipes/cook-tokens-isolation.test.ts (the neighbouring suite in the directory this file joins — match its file-header and naming style)
  </read_first>
  <behavior>
    - Under `view: "everyone"` (THE LIVE POLICY): a caller whose `householdKey` is cookbook A receives cookbook A's queued jobs and NONE of cookbook B's.
    - Under `view: "household"`: identical result to `everyone` — the two levels share one branch.
    - Under `view: "owner"`: a caller receives only jobs whose `job.data.userId` is their own user id, including one queued into a different cookbook, and none of another user's.
    - Under every level, the returned `PendingRecipeDTO[]` never contains a `url` belonging to another cookbook's job — asserted on the `url`, not only on the count, because the url is the disclosive field.
    - A caller with NO active cookbook (`householdKey === user.id`) receives their own personal jobs and none of cookbook A's — under all three levels, with no dedicated branch in the implementation.
    - A caller with `isServerAdmin: true` is clamped identically to a normal caller under all three levels — no admin escape hatch is introduced.
    - A user's OWN in-flight import is still returned to them under all three levels (the legitimate case the dashboard skeleton depends on).
    - The mapped DTO shape is unchanged: `{ recipeId, url, addedAt }` with `addedAt` from `job.timestamp`.
    - The queue is queried with exactly `["waiting", "active", "delayed"]`, unchanged.
  </behavior>
  <action>
    **STEP 1 — RED, before touching `pending.ts`.**

    Create `packages/trpc/__tests__/recipes/pending-isolation.test.ts` FIRST, mirroring
    `tags-isolation.test.ts` exactly:
      - `// @vitest-environment node`, then a file header naming PENDING-ISO-01, quoting the defective
        `case "everyone": return true`, and stating that live runs `view: "everyone"` so the leaking
        branch is the branch production executes.
      - `vi.mock("@norish/trpc/middleware", ...)` down to a pass-through `publicProcedure`, and
        `vi.mock("@norish/db/repositories/api-logs")`, exactly as the template does — the middleware is
        not under test and building it for real needs a DB.
      - `vi.mock("@norish/queue/registry")` so `getQueues().recipeImport.getJobs` returns a fixed
        two-household fixture: at least one job in cookbook A (user A), one in cookbook B (user B), and
        one personal job (householdKey === its own userId). Give each a DISTINCT, recognisable `url` so
        an assertion can name which cookbook leaked.
      - `vi.mock("@norish/shared-server/config/server-config-loader")` for `getRecipePermissionPolicy`,
        and a `LIVE_SERVER_POLICY = { view: "everyone", edit: "household", delete: "household" }`
        constant carrying the same "the LIVE server-wide policy" comment the template uses.
      - Drive the REAL router: `pendingProcedures.createCaller(ctx as never).getPending()`. Do NOT
        reimplement the filter in the test — every leak in this family survived a green suite that
        tested the layer below the defect.
      - Structure the cases with a `describe.each(["household", "everyone", "owner"])` so the sibling
        `everyone` case cannot be forgotten. Cover every line of `<behavior>`.

    RUN IT NOW, against the UNFIXED `pending.ts`. The `everyone` cases MUST FAIL, and the failure output
    must show cookbook B's url in cookbook A's result. **Capture that output verbatim — it goes in the
    SUMMARY.** A test that has never failed proves nothing about the defect it claims to pin, and this
    family's whole history is green suites that never executed the live branch.

    **STEP 2 — GREEN.**

    In `packages/trpc/src/routers/recipes/pending.ts`, replace the `switch (policy.view)` filter
    (lines 26-42) with a single clamp. `everyone` gets NO branch of its own — it falls through with
    `household`:

        const isOwnerScoped = policy.view === "owner";

        const filteredJobs = jobs.filter((job) =>
          isOwnerScoped
            ? job.data.userId === ctx.user.id
            : job.data.householdKey === ctx.householdKey
        );

    Keep `getRecipePermissionPolicy()`, the `getJobs([...])` call, the DTO mapping and both `log.debug`
    lines byte-identical.

    A comment above the filter must record, in the voice of the sibling fixes:
      - PENDING-ISO-01 and D-27.1-12;
      - that `everyone` answers "who may FETCH this if they ask, WITHIN the recipe's cookbook" and is
        never a licence to LIST every cookbook's queued imports — the same mistake REALTIME-ISO-01,
        IMPORT-DEDUP-ISO-01, LIST-ISO-01 and TAGS-ISO-01 each made;
      - that `PendingRecipeDTO` carries the source `url`, so the leak disclosed what other cookbooks were
        importing, not merely that they were importing;
      - that a viewer with no active cookbook has `householdKey === user.id` (middleware:
        `household?.id ?? user.id`) and a personal job carries that same key, so the household comparison
        clamps them to themselves with NO extra branch — the same reasoning as `emitByPolicy`'s
        `isPersonalScope`;
      - that NO server-admin exemption is added here, and WHY: the pre-existing `household` and `owner`
        branches had none, so adding one would be a new widening invented by the fix; `config.tags` and
        `buildViewPolicyCondition` DO exempt admins, and that difference is deliberate because this query
        hydrates a dashboard skeleton rather than serving an admin surface.

    **STEP 3 — ADVERSARIAL RE-VERIFICATION (CLAUDE.md, security-critical).**
    With the suite green, temporarily restore `case "everyone": return true`, confirm the `everyone`
    cases go RED again, then revert. Record the observed RED output in the SUMMARY. NEVER commit the
    weakening — `git status` must be clean of it before this task's commit.
  </action>
  <acceptance_criteria>
    - The SUMMARY contains the VERBATIM STEP 1 RED output, showing at least one `everyone` case failing with cookbook B's url present in cookbook A's result. A SUMMARY without a recorded RED run fails this task.
    - `pnpm --filter @norish/trpc test` reports **337 baseline + the new file's tests, 0 failed** (baseline measured 2026-07-28 at HEAD `ab43771c`: 337 passed / 32 files). Record the exact new total in the SUMMARY.
    - A `describe.each` runs EVERY case under `household`, `everyone` AND `owner`; the suite output names all three. A suite that seeds only `household` proves nothing (AGENTS.md).
    - A test asserts on the returned `url` values, not only on `length` — e.g. `expect(urls).not.toContain(COOKBOOK_B_URL)` — so a leak of the disclosive field cannot pass on a count check.
    - A test asserts an `isServerAdmin: true` caller gets the SAME clamped result as a normal caller, under all three levels.
    - A test asserts a caller's OWN in-flight job IS returned under all three levels.
    - `grep -vE '^\s*(//|\*|/\*)' packages/trpc/src/routers/recipes/pending.ts | grep -c '"everyone"'` prints `0` — `everyone` survives only in the explanatory comment; there is no reachable branch on it.
    - `grep -vE '^\s*(//|\*|/\*)' packages/trpc/src/routers/recipes/pending.ts | grep -c 'return true'` prints `0`.
    - `git diff packages/trpc/src/routers/recipes/pending.ts` shows a change ONLY inside `getPending` — `git diff -U0 packages/trpc/src/routers/recipes/pending.ts | grep -c '^[+-][^+-]'` is under 25, and no hunk touches `isNutritionEstimating`, `getPendingAutoTagging`, `isAutoTagging`, `isAutoCategorizing`, `getPendingAllergyDetection` or `isAllergyDetecting`.
    - The adversarial re-verification was performed, its RED output is recorded in the SUMMARY, and `git diff` at commit time contains no trace of the weakening.
    - `pnpm --filter @norish/trpc typecheck` and `pnpm --filter @norish/trpc lint` — **0 errors**; warnings at the measured baseline.
    - `git diff --stat packages/db packages/db-schema packages/queue packages/api apps/ pnpm-lock.yaml` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/trpc test &amp;&amp; pnpm --filter @norish/trpc lint &amp;&amp; pnpm --filter @norish/trpc typecheck</automated>
  </verify>
  <done>Under the live `view: "everyone"`, a user receives only their own cookbook's pending imports; `everyone` has no reachable branch of its own; the suite was RED before the fix and RED again when the fix was temporarily removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audit and pin the rest of the pending surface — tests only, no behaviour change</name>
  <files>packages/trpc/__tests__/recipes/pending-isolation.test.ts</files>
  <read_first>
    packages/trpc/src/routers/recipes/pending.ts (the SIX procedures Task 1 did not change: getPendingAutoTagging at 80-97 and getPendingAllergyDetection at 142-161, which already clamp with `job.data.userId === ctx.user.id || job.data.householdKey === ctx.householdKey`; and the four id-probes isNutritionEstimating at 58-74, isAutoTagging at 102-118, isAutoCategorizing at 120-136, isAllergyDetecting at 166-182, which apply NO ownership check at all)
    packages/trpc/__tests__/recipes/pending-isolation.test.ts (Task 1's output — extend it, do not create a second file)
    AGENTS.md (the `view: "everyone"` is never "no boundary" section — in particular "Authenticating is not authorizing", and the `canAccessResource` / `denyUnauthorizedRecipeMedia` precedent for per-resource route checks)
  </read_first>
  <behavior>
    - `getPendingAutoTagging` returns a recipe id whose job matches the caller's user id, and one whose job matches the caller's `householdKey`, and NOT one belonging to another user in another cookbook — under `household`, `everyone` AND `owner`.
    - `getPendingAllergyDetection` behaves identically.
    - Neither of those two reads `getRecipePermissionPolicy` at all: seeding a different policy does not change their result.
  </behavior>
  <action>
    Extend `packages/trpc/__tests__/recipes/pending-isolation.test.ts` with a
    `describe("the sibling pending queries are already clamped — regression pins")` block covering
    `getPendingAutoTagging` and `getPendingAllergyDetection`.

    **Change NO production behaviour in this task.** These two procedures already clamp correctly
    (`userId === ctx.user.id || householdKey === ctx.householdKey`) and, unlike `getPending`, never
    consult the server-wide policy — which is exactly why they escaped the defect. The pins exist so a
    future edit cannot quietly reintroduce the `getPending` shape here. Assert explicitly that seeding
    `everyone` does not widen either result, and that `getRecipePermissionPolicyMock` was NOT called by
    them.

    **DO NOT change the four `is*` probes.** Record them instead: `isNutritionEstimating`,
    `isAutoTagging`, `isAutoCategorizing` and `isAllergyDetecting` each answer
    `jobs.some(job => job.data.recipeId === input.recipeId)` with NO ownership check, so any
    authenticated caller can ask whether an ARBITRARY recipe id currently has a queued job. That is the
    "Authenticating is not authorizing" pattern AGENTS.md names, and it is a genuine finding — but it is
    a boolean about an id the caller must already possess, not a cookbook recipe leak, and the two
    sources that used to hand out other cookbooks' recipe ids (`getPending` and the pre-D-22-01
    broadcast) are both closed. Write it up in the SUMMARY as **finding F-5** with those exact file:line
    references and that severity reasoning, for the director to schedule separately. Do not fix it here
    and do not add a failing test for it.
  </action>
  <acceptance_criteria>
    - `pnpm --filter @norish/trpc test` — the Task 1 total plus this task's tests, **0 failed**. Record the exact total in the SUMMARY.
    - A test asserts `getPendingAutoTagging` returns the caller's own-user job id and the caller's own-household job id, and NOT another cookbook's — under all three policy levels.
    - The equivalent test exists for `getPendingAllergyDetection`.
    - A test asserts `getRecipePermissionPolicyMock` was NOT called during either of those two procedures — proof they are policy-independent by construction.
    - `git diff --stat packages/trpc/src/routers/recipes/pending.ts` shows NO change from this task: `git log --format=%s -- packages/trpc/src/routers/recipes/pending.ts | grep -c '(27.1-06)'` prints `1` (Task 1's commit only).
    - The SUMMARY contains finding **F-5** with the four procedure names, their line numbers, and the severity reasoning — no fix attempted.
    - `pnpm --filter @norish/trpc lint` — **0 errors**.
    - `git diff --stat packages/db packages/queue packages/api apps/ pnpm-lock.yaml` is EMPTY.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @norish/trpc test &amp;&amp; pnpm --filter @norish/trpc lint</automated>
  </verify>
  <done>The two already-clamped sibling queries are pinned under all three policy levels, the four unchecked id-probes are written up as finding F-5, and no production behaviour changed in this task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → `recipes.getPending` | An authenticated caller in cookbook B reaches a query whose data source spans every cookbook. |
| BullMQ queue → tRPC response | Job data authored by other households crosses into another user's response body. |
| server-wide `recipe_permission_policy` → a scope decision | A single global config value decides a per-cookbook boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27.1-06-01 | Information Disclosure | `getPending` under `view: "everyone"` | **mitigate (HIGH — live today)** | `everyone` loses its `return true` branch entirely and falls through to `job.data.householdKey === ctx.householdKey`, mirroring `emitByPolicy` (D-22-01) and `buildViewPolicyCondition` (LIST-ISO-01). Proven by a two-household suite that is RED before the fix, asserts on the disclosive `url` field rather than a count, and runs every case under `household` / `everyone` / `owner`. |
| T-27.1-06-02 | Information Disclosure | the source `url` in `PendingRecipeDTO` | mitigate | The leak disclosed WHAT other cookbooks were importing, not merely that they were. The suite asserts `expect(urls).not.toContain(COOKBOOK_B_URL)`, so a regression that leaks the url while keeping the count right cannot pass. |
| T-27.1-06-03 | Elevation of Privilege | a re-introduced `everyone` branch | mitigate | `grep -c '"everyone"'` over the non-comment body of `pending.ts` must print `0` — the level survives only in the explanatory comment. AGENTS.md's rule is that a reachable `everyone` branch IS the foot-gun, so the fix removes the branch rather than correcting it. |
| T-27.1-06-04 | Elevation of Privilege | inventing a server-admin exemption | mitigate | No admin branch is added; a test pins that `isServerAdmin: true` is clamped identically. The pre-existing `household` / `owner` branches had no exemption, so adding one would be a new widening introduced by a security fix — the worst place to introduce one. The divergence from `config.tags` / `buildViewPolicyCondition` is stated in the code comment so it reads as deliberate, not overlooked. |
| T-27.1-06-05 | Denial of Service | losing the legitimate pending skeleton | mitigate | An over-tight clamp would delete the user's own import skeleton — the slot 27.1-03 renders its error card into. Pinned by a test asserting the caller's own in-flight job IS returned under all three levels, and by the personal-scope case (`householdKey === user.id`). |
| T-27.1-06-06 | Repudiation | a test that never failed | mitigate | STEP 1 requires the suite to be authored and RUN against the unfixed resolver, with the verbatim RED output recorded in the SUMMARY; STEP 3 requires the fix to be temporarily removed and the suite observed RED again. Every member of this defect family survived a green suite that never executed the live branch. |
| T-27.1-06-07 | Information Disclosure | the four unchecked `is*` id-probes | **accept — raised as F-5** | `isNutritionEstimating` (:58-74), `isAutoTagging` (:102-118), `isAutoCategorizing` (:120-136) and `isAllergyDetecting` (:166-182) answer `jobs.some(j => j.data.recipeId === input.recipeId)` with NO ownership check. Accepted for now: the response is a boolean about an id the caller must ALREADY hold, not a recipe leak, and both sources that used to hand out other cookbooks' ids — `getPending` and the pre-D-22-01 broadcast — are now closed. Written up as finding F-5 with file:line for a separate scheduling decision; deliberately NOT fixed inside a plan whose scope the director set at one defect. |
| T-27.1-06-08 | Tampering | contradicting 27.1-03 | mitigate | 27.1-03's scope fence and its T-27.1-03-06 row previously asserted `pending.ts` EMPTY; both have been reworded to name 27.1-06 as the file's owner, and 27.1-03's proving command became commit-scoped (`git log --format=%s -- <path> \| grep -c '(27.1-03)'` prints `0`) so it holds regardless of which plan runs first. |
| T-27.1-06-SC | Tampering | npm/pip/cargo installs | mitigate | NO package is installed. `git diff --stat pnpm-lock.yaml` EMPTY is an acceptance criterion on both tasks. |
</threat_model>

<verification>
- `pnpm --filter @norish/trpc test` — 337 baseline + the new suite, 0 failed.
- The STEP 1 RED output and the STEP 3 adversarial RED output are both recorded verbatim in the SUMMARY.
- `pnpm --filter @norish/trpc lint` — 0 errors; `pnpm --filter @norish/trpc typecheck` clean.
- `grep -vE '^\s*(//|\*|/\*)' packages/trpc/src/routers/recipes/pending.ts | grep -c '"everyone"'` prints `0`.
- `git diff --stat packages/db packages/db-schema packages/queue packages/api apps/ packages/shared* pnpm-lock.yaml` EMPTY.
</verification>

<success_criteria>
Under the live server-wide `view: "everyone"`, `recipes.getPending` returns only the caller's own
cookbook's queued imports — no other cookbook's `recipeId` and, critically, no other cookbook's source
`url`. `everyone` has no reachable branch of its own; it falls through to the same clamp `household`
uses, exactly as the fork's three prior remediations do. The suite was RED before the fix and RED again
when the fix was temporarily removed, and every case runs under the live policy as well as the seeded
one. No server-admin exemption was invented, and the user's own import skeleton still works.
</success_criteria>

<output>
Create `.planning/phases/27.1-import-reliability-ai-extraction-resilience-json-ld-fallback/27.1-06-SUMMARY.md` when done.
Record: the verbatim STEP 1 RED output, the verbatim STEP 3 adversarial RED output and confirmation the
weakening was reverted, the post-change `@norish/trpc` test total, the exact diff of `getPending`'s
filter, and finding **F-5** (the four unchecked `is*` id-probes) with file:line references.
</output>
