/**
 * Stub for @heroui-pro/react in test environments.
 *
 * @heroui-pro/react requires a paid license token (HEROUI_AUTH_TOKEN) to
 * download and install the actual library files via its postinstall script.
 * In CI and local dev without a token, the package is installed as a stub
 * with no exports. This file provides a minimal stub so vitest can resolve
 * the module; individual tests that use @heroui-pro/react components must
 * mock this module via vi.mock("@heroui-pro/react", ...).
 */
export const Sheet = {};
