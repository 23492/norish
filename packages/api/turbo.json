{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "env": ["DATABASE_URL", "MASTER_KEY", "AUTH_URL", "CAMOFOX_URL", "CAMOFOX_API_KEY"]
    }
  }
}
